{
  "$description": "의도 이름 계층 — primitive 를 별칭으로 참조해 **용도 이름**을 붙인다. semantic 이 아니다: 테마 축이 없다(DECISIONS 0-16). `typography.json` 과 같은 자리이며 `COMPOSITION.sets.base` 에 등록돼 있다. 컴포넌트가 `--spacing-16` 을 직접 고르지 않게 하는 것이 목적이다(미결 17).",
  "control": {
    "$description": "컨트롤 치수 — **컴포넌트가 아니라 역할 단위**다. Button · Text Field · Search 가 같은 값을 공유해야 폼 한 줄에서 높이가 맞는다(`button.height` 로 지으면 Text Field 가 같은 값을 다시 정의한다). 전부 기존 primitive 별칭이며 **새 원시 값을 만들지 않았다.** 기본 단계는 `md` 다. 티어 이름을 쓰므로 빌드가 값 순서를 검사한다(DECISIONS 0-30).",
    "minHeight": {
      "$description": "컨트롤 최소 높이. **`height` 가 아니라 `min-height` 로 쓴다** — 고정 높이는 사용자가 행간·자간을 덮어썼을 때 글자를 자르고 WCAG 1.4.12(글자 간격)를 깬다. 이름이 사용법을 강제하도록 지었다. **한 눈금을 컴포넌트마다 다른 구간으로 쓴다** — Chip 은 `xs~md`(24·32·40), Button·Text Field 는 `sm~lg`(32·40·48)다. 같은 티어 이름이면 같은 물리 크기라 한 줄에 섞여도 높이가 맞는다(DECISIONS 0-59).",
      "xs": {
        "$type": "dimension",
        "$value": "{spacing.24}",
        "$description": "24px. **Chip 전용 단계다** — Button·Text Field 는 이 아래로 내려가지 않는다. WCAG 2.5.8 의 하한(24×24)과 **정확히 같아서 여유가 0 이다**: 이보다 작게 만들 수 없고, 이 높이의 컨트롤은 세로 여백을 더 줄 자리도 없다. 칩은 가로로 넓어 조작 영역 자체는 넉넉하다."
      },
      "sm": {
        "$type": "dimension",
        "$value": "{spacing.32}",
        "$description": "32px. 조밀한 툴바 · 테이블 행 내부 · Chip(sm). 8px 등차라 세로 리듬이 유지된다. WCAG 2.5.8(24×24 CSS px)에 **8px 여유**를 두고 만족한다 — 여유가 0 인 것은 `xs` 다."
      },
      "md": {
        "$type": "dimension",
        "$value": "{spacing.40}",
        "$description": "40px. 기본값. 폼 · 다이얼로그 · 일반 화면. 8px 등차라 세로 리듬이 유지된다."
      },
      "lg": {
        "$type": "dimension",
        "$value": "{spacing.48}",
        "$description": "48px. 모바일 주요 CTA · Bottom Sheet 하단 버튼. 8px 등차라 세로 리듬이 유지된다."
      }
    },
    "paddingInline": {
      "$description": "컨트롤 좌우 안쪽 여백. `minHeight` 와 같은 단계를 쓴다.",
      "xs": {
        "$type": "dimension",
        "$value": "{spacing.8}",
        "$description": "8px. `minHeight.xs` 와 짝이다."
      },
      "sm": {
        "$type": "dimension",
        "$value": "{spacing.12}",
        "$description": "12px. `minHeight.sm` 와 짝이다."
      },
      "md": {
        "$type": "dimension",
        "$value": "{spacing.16}",
        "$description": "16px. `minHeight.md` 와 짝이다."
      },
      "lg": {
        "$type": "dimension",
        "$value": "{spacing.20}",
        "$description": "20px. `minHeight.lg` 와 짝이다."
      }
    },
    "gap": {
      "$description": "컨트롤 안 아이콘과 라벨 사이 간격.",
      "xs": {
        "$type": "dimension",
        "$value": "{spacing.2}",
        "$description": "2px. `minHeight.xs` 와 짝이다."
      },
      "sm": {
        "$type": "dimension",
        "$value": "{spacing.4}",
        "$description": "4px. `minHeight.sm` 와 짝이다."
      },
      "md": {
        "$type": "dimension",
        "$value": "{spacing.6}",
        "$description": "6px. `minHeight.md` 와 짝이다."
      },
      "lg": {
        "$type": "dimension",
        "$value": "{spacing.8}",
        "$description": "8px. `minHeight.lg` 와 짝이다."
      }
    },
    "radius": {
      "$description": "컨트롤 모서리. 원형 버튼(Floating Button)은 `radius.circle` 을 직접 쓴다.",
      "sm": {
        "$type": "dimension",
        "$value": "{radius.6}",
        "$description": "6px. `minHeight.sm` 와 짝이다."
      },
      "md": {
        "$type": "dimension",
        "$value": "{radius.8}",
        "$description": "8px. `minHeight.md` 와 짝이다."
      },
      "lg": {
        "$type": "dimension",
        "$value": "{radius.12}",
        "$description": "12px. `minHeight.lg` 와 짝이다."
      }
    },
    "iconSize": {
      "$description": "컨트롤 안 아이콘의 한 변 — **`iconSize` 눈금의 하위 셋**이다(전체는 16·20·24·28·32, 28·32 는 컨트롤 밖 단독 아이콘용). 예전에는 `spacing` 을 별칭했는데, 아이콘 크기는 간격 눈금이 아니고 28 이 `spacing` 에 없어 5단계를 만들 수 없었다(DECISIONS 0-51). **아이콘은 `currentColor` 를 따르므로 색 토큰은 두지 않는다** — 컨테이너의 `color` 가 제어한다. **획 두께 토큰도 두지 않는다** — 상자를 줄이면 획이 같은 비율로 따라 줄어든다. 래퍼에 이 크기를 주고 SVG 는 100% 로 채운다 — 아이콘 세트가 바뀌어도 CSS 가 그대로 동작한다.",
      "sm": {
        "$type": "dimension",
        "$value": "{iconSize.16}",
        "$description": "16px. `minHeight.sm` 와 짝이다. 32px 컨트롤용. 위아래 8px 씩 남는다."
      },
      "md": {
        "$type": "dimension",
        "$value": "{iconSize.20}",
        "$description": "20px. `minHeight.md` 와 짝이다. 40px 컨트롤용. 기본."
      },
      "lg": {
        "$type": "dimension",
        "$value": "{iconSize.24}",
        "$description": "24px. `minHeight.lg` 와 짝이다. 48px 컨트롤용."
      }
    },
    "boxSize": {
      "$type": "dimension",
      "$value": "{spacing.20}",
      "$description": "선택 컨트롤의 네모·원 한 변 20px — Checkbox · Radio 가 공유한다. **`iconSize.20` 을 쓰지 않는다.** 값은 같지만 축이 다르다 — `iconSize` 는 SVG 래퍼 크기이고 이것은 컨트롤 그 자체의 상자다. 한쪽을 바꿀 때 다른 쪽이 따라 움직이면 안 된다(DECISIONS 0-55). **시각 크기이지 조작 영역이 아니다** — 조작 영역의 하한은 `minTarget`(24px)이고, 라벨까지 포함하면 실제로는 더 넓다. **티어를 두지 않았다** — 크기 단계 수요가 아직 확인되지 않았다(`label-xs` 의 `-strong` 을 두지 않은 것과 같은 원칙, 0-25). 필요해지면 그때 나눈다."
    },
    "trackWidth": {
      "$type": "dimension",
      "$value": "{spacing.40}",
      "$description": "손잡이가 오가는 트랙의 가로 40px — 지금은 Switch 하나가 쓴다. **세로와 손잡이는 새로 만들지 않았다** — 세로는 `minHeight.xs`(24), 손잡이는 `boxSize`(20)다. 이동 거리 16px 도 `spacing.16` 에 떨어진다: 40 − 20 − 2 − 2 = 16. **`minHeight.md`(40)를 쓰지 않는다.** 값은 같지만 그쪽은 세로 축이라, 컨트롤 높이를 바꾸면 스위치 가로가 따라 움직인다 — `iconSize.20` 을 `boxSize` 로 가르지 않았을 때와 같은 함정이다(0-55). **Slider(P2-7)도 트랙을 갖지만 치수가 다를 것이다** — 그때 티어로 나눌지 본다. 미리 나누면 추측이다."
    },
    "minInset": {
      "$type": "dimension",
      "$value": "{spacing.1}",
      "$description": "컨트롤 테두리 안쪽 최소 여백 1px — **KWCAG 6.1.3 (KR delta)**. 브라우저 UA 가 `button {padding: 1px 6px}` · `input {padding: 1px 2px}` 로 우연히 이 값을 주고 있었는데, 우리가 `padding-inline` 만 덮어 **세로는 UA 값이 남아 있었다**(0-48). 우연한 만족은 브라우저가 바뀌면 사라진다. `padding-block` 을 이 토큰으로 명시해 **의도해서** 만족시킨다. 이보다 큰 세로 여백을 이미 갖는 컨트롤(Text Area 10px)은 그대로 두면 된다 — **하한이다**. **주의: 6.1.3 은 그 여백이 포인터 조작에 반응하지 않을 것도 요구하는데, padding 만으로는 그 부분이 충족되지 않는다**(미결 25)."
    },
    "minTarget": {
      "$type": "dimension",
      "$value": "{spacing.24}",
      "$description": "조작 영역 하한 24px — WCAG 2.5.8. **KWCAG 6.1.3(대각 6.0mm)을 포함한다**: CSS 기준 픽셀(1px=1/96in)에서 24×24 의 대각은 8.98mm 이고, 6.0mm 대각을 만족하는 최소 정사각은 16.04px 다. **24 만 지키면 두 기준이 함께 닫힌다**(DECISIONS 0-30). 시각 크기가 이보다 작은 컨트롤은 히트박스를 이 값까지 넓힌다."
    },
    "$extensions": {
      "net.infobank.ds.foundation": [
        "Size"
      ]
    }
  },
  "transition": {
    "$description": "전환 묶음 — DTCG `transition`(9.5). `duration` + `delay` + `timingFunction` 셋이 **모두 필수**라 지연이 없어도 `{duration.0}` 을 넣는다. CSS 로는 `transition: <속성> var(--transition-*)` 처럼 쓴다. **의도 이름이 컴포넌트 문맥에서 나오므로 필요한 것만 만든다** — 지금은 Button 이 쓰는 `control` 하나다(0-27 · 0-30).",
    "control": {
      "$type": "transition",
      "$value": {
        "duration": "{duration.100}",
        "delay": "{duration.0}",
        "timingFunction": "{cubicBezier.standard}"
      },
      "$description": "컨트롤 상태 전환 — hover·press 오버레이, 보더 색. 100ms · standard. 가장 빈번한 전환이라 가장 짧다."
    },
    "$extensions": {
      "net.infobank.ds.foundation": [
        "Motion"
      ]
    }
  }
}

# 토큰 설계 기록

**이 문서의 역할** — 토큰이 *무엇인지*는 JSON 의 `$description` 에 있다. 여기에는 **왜 그렇게 정했는지**, **무엇을 검토했다 버렸는지**, **아직 안 정해진 것**을 적는다. 작업 규칙은 `../CLAUDE.md` 에 있다.

최종 갱신: 2026-08-25

---

## 0. 스펙 기준

### 0-1. 기준 버전은 2025.10 이다

**Design Tokens Format Module 2025.10** · **Color Module 2025.10** — 둘 다 *Final Community Group Report, 2025-10-28* 이고 "considered stable" 로 표시돼 있다. 고정 릴리스를 근거로 쓴다.

- <https://www.designtokens.org/tr/2025.10/format/>
- <https://www.designtokens.org/tr/2025.10/color/>

`/tr/drafts/` 는 계속 움직이는 초안이라 근거로 쓰지 않는다. 내용이 지금은 같아도 버전을 특정할 수 없으면 준수 여부를 검사할 수 없다.

**그동안 버전이 명시되지 않았다.** `CLAUDE.md` 는 "DTCG 스펙으로 작성한다"고만 적었고, 그 결과 준수 주장이 검사 불가능한 문장이었다. 2026-08-25 에 버전을 박았다.

### 0-2. 왜 2025.10 인가 — 파일이 이미 거기에 있었다

2025.10 과 이전 형식(2nd Editors' Draft)은 값 형식이 **양립하지 않는다.** 우리 파일 실태를 두 버전에 대조하면 이렇다.

| 값 형식 | 건수 | 2nd Editors' Draft | 2025.10 |
|---|---:|:---:|:---:|
| `dimension` = `{value, unit}` 객체 | 82 | ✗ 문자열 `"16px"` 요구 | ✓ |
| `$type: "number"` | 40 | ✗ **number 타입이 없음** | ✓ |
| `shadow` 내부 offset/blur 객체 | 10 | ✗ | ✓ |
| `color` = hex 문자열 | 101 | ✓ | ✗ |
| `$type` 없음 | 32 | ✓ JSON 타입 폴백 | ✗ |

132건이 2025.10 전용, 133건이 2nd ED 전용이다. 즉 색 전환은 새 요구가 아니라 **파일 내부 불일치를 없애는 작업**이다.

**검토했다 버린 것**

| 안 | 왜 버렸나 |
|---|---|
| 2nd ED 를 기준으로 삼고 색 유지 | `dimension` 82건을 객체 → 문자열로 되돌려야 하고, 그 버전에 `number` 가 없어 `opacity` 26 · `interaction` 9 · `layout.columns` 5 가 **무타입 토큰으로 강등**된다. 후퇴이고 작업량도 더 크다. |
| `$extensions` 로 비표준 타입을 선언 | **유효성을 만들지 못한다.** 스펙은 타입을 결정할 수 없는 토큰을 도구가 무효로 취급해야 한다고 규정하고, 값에서 타입 추론도 금지한다. 확장 데이터를 붙여도 무효는 무효다. |

**색을 2025.10 으로 옮기는 부수 이득** — `colorSpace: "oklch"` 가 정식 지원된다. 우리 팔레트는 OKLCH 로 설계했는데 변환 결과인 hex 만 토큰에 남아 L·C·H 는 이 문서와 스크래치패드에만 있었다. 전환하면 설계값이 토큰 안으로 돌아온다. 단 복원되는 값은 **색역 매핑 후**라 클리핑 전 의도는 아니다 — 대신 실제 렌더링 값과 어긋나지 않는다.

**전환 시점을 지금으로 잡은 이유** — 패키지가 `private` 이고 외부 소비자가 0이다. 나중에 하면 제품 앱까지 같이 고쳐야 한다.

### 0-3. 스펙 밖의 값은 예외로 표시한다

`gradient.solid` · `multiple` 8건은 `currentColor` 를 쓴다. CSS 런타임 상속값이라 `colorSpace`+`components` 를 요구하는 DTCG `color` 로 표현할 방법이 없다. 즉 이 8개는 디자인 토큰이 아니라 **CSS 레시피**다.

`$extensions."net.infobank.ds.cssRecipe": true` 로 표시하고, 컴포넌트 단계에서 토큰 밖으로 옮길지 재검토한다. 예외는 늘리지 않는다.

> **갱신(2026-09-01):** 이 8건 중 절반이 나머지 절반의 중복이었다. `multiple` 4개를 제거하고 `solid` 을 `fade` 로 고쳐 **예외가 4건으로 줄었다**. 근거는 0-15.

### 0-4. 배포 형태 — `@infobank/next-ds-tokens`

2026-08-25, 저장소 루트에 `package.json` 을 만들었다. 그전에는 `CLAUDE.md` 가 5곳에서 `npm run build:tokens` 를 지시했지만 `package.json` 이 없어 실행되지 않았다.

| 필드 | 값 | 이유 |
|---|---|---|
| `name` | `@infobank/next-ds-tokens` | |
| `version` | `0.1.0` | 토큰 미확정 단계 |
| `private` | `true` | 실수 발행 방지. 공개 배포는 나중에 결정 |
| `type` | `module` | `tokens.js` 가 `export const` 21개. 없으면 Node 가 CJS로 읽어 전부 문법 오류 |
| `files` · `exports` | `tokens.css` · `tokens.js` 만 | 공개 API 를 산출물 2개로 한정. JSON 원본 · `build.mjs` · `preview.html` 은 배포 제외 |

의존성은 0을 유지한다 — `build.mjs` 가 Node 표준 모듈만 쓴다. lockfile · `node_modules` 를 만들지 않는다.

**소비자별 형식 변환은 어댑터로 한다.** JSON 을 단일 원본으로 두고 `build.mjs` 가 `tokens.css`·`tokens.js` 를 뽑는 구조가 이미 그것이다. Figma 등은 출력 대상이 하나 더 붙는 것이고, **특정 소비자의 형식이 원본 형식을 결정하게 두지 않는다.**

### 0-5. 원시 스케일은 한 곳에만 둔다

2026-08-25, `interaction.opacity.*` 9건이 숫자를 직접 들고 있었고 그중 7건이 `opacity` 그룹과 값이 같았다. 오버레이 강도를 조정할 때 두 곳을 봐야 하고 어느 쪽이 원본인지 알 수 없는 상태였다.

9건 전부를 `{opacity.N}` 별칭으로 바꿨다. 스케일 밖이던 0.09 · 0.18 은 `opacity.9` · `opacity.18` 로 흡수했다. 이를 위해 `build.mjs` 에 `deref()` 를 넣어 `number`·`dimension` 의 primitive 내부 별칭을 해석하게 했다 — 그전에는 미해석이라 별칭을 쓰면 CSS 에 `{opacity.5}` 가 문자열로 나갔다.

검증: `tokens.css` 가 신설 2줄 외에 **바이트 단위로 동일**했다.

---

### 0-6. 이주 1차 — `ratio` · `layout.maxWidth` · `gradient`

2026-08-25. `$type` 을 결정할 수 없던 32건을 처리했다.

| 대상 | 건수 | 처리 | 결과 |
|---|---:|---|---|
| `layout.maxWidth.xs·sm·md` | 3 | **토큰 삭제** | `"none"` 은 값이 아니라 값의 부재다. `dimension` 은 키워드를 허용하지 않으므로 "제약 없음"은 토큰을 두지 않는 것으로 표현한다. `maxWidth` 는 `lg`·`xl` 만 남았다. 컴포넌트는 `lg` 이상에서만 `max-width` 를 건다. |
| `ratio.*` | 21 | `$type: "number"` | 폭÷높이. CSS `aspect-ratio` 는 단일 숫자를 받는다. 비율의 의미는 키(`16-9`)가 담는다. |
| `gradient.solid`·`multiple` | 8 | `cssRecipe` 예외 표시 | `currentColor` 는 표현 불가 — 0-3 참조. **2026-09-01 에 중복 4건을 제거하고 `gradient.fade` 4건으로 줄였다(0-15).** |

**`ratio` 값 형식이 바뀐 대가** — CSS 출력이 `16 / 9` 에서 `1.777778` 로 바뀌어 가독성이 내려갔다. 소수 6자리로 반올림했고 1920px 폭에서 오차는 0.0001px 미만이다. 대안으로 폭·높이를 `number` 두 개로 쪼개 빌드가 `16 / 9` 를 조립하는 방법이 있었으나, 토큰이 21 → 42개로 늘고 JS 소비자가 값을 직접 조립해야 해서 버렸다.

**남은 무효 토큰은 색 101건 + `gradient` 8건이다** (작업 전 133건 → 109건). `gradient` 8건은 예외로 확정했으므로 실질 대상은 색 101건이다.

**부수 수정** — `preview.html` 의 `valueOf` 에도 별칭 해석이 없어 0-5 작업 직후 `interaction.opacity` 9건이 `{opacity.5}` 문자열로 표시되고 있었다. `resolveAliasTok()` 을 넣어 고쳤다(기존 `derefPrim(prim, ref)` 과 이름이 겹쳐 한 번 깨뜨렸다).

---

### 0-7. 이주 2차 — 색 101건을 oklch 객체로

2026-08-25. hex 문자열이던 primitive 색 101건을 2025.10 형식으로 바꿨다.

```json
"600": {
  "$type": "color",
  "$value": { "colorSpace": "oklch", "components": [0.545847, 0.210951, 260.1367], "hex": "#1266E8" }
}
```

**설계값이 토큰으로 돌아왔다.** 팔레트를 OKLCH 로 설계했는데 그동안 변환 결과인 hex 만 남아 L·C·H 는 이 문서와 스크래치패드에만 있었다. 이제 hue 정리(1-4)가 값으로 확인된다 — `yellow.400` H=100.4° · `teal.600` H=179.9°.

**변환은 무손실이다.** hex → OKLCH 는 결정론적 가역 변환이라 101건 전부 왕복이 정확히 일치했다(소수 L·C 6자리, H 4자리). 무채색 4건(`#FFFFFF`·`#000000` 등 r=g=b)은 색상이 정의되지 않으므로 C=0 · H=`"none"` 으로 뒀다 — 스펙이 허용하는 표기다.

**`hex` 필드는 원본 문자열을 그대로 유지했다.** 소문자로 정규화했더니 `tokens.css` 에 대소문자 차이만 있는 diff 가 101줄 생겼다. 값은 같지만 통과 조건을 "diff 없음"으로 잡았으므로 원본 대소문자를 보존했다.

**검증** — `tokens.css` · `tokens.js` 모두 **바이트 단위로 동일**. AA 대비 재검증 144건 미달 0.

#### 새 위험과 그 대응

한 색이 `components` 와 `hex` **두 표현**을 갖게 됐다. 하나만 고치면 소비자가 어느 쪽을 읽느냐에 따라 다른 색이 나온다. `build.mjs` 에 `assertColorHexMatchesComponents()` 를 넣어 매 빌드마다 막는다.

| 검사 | 내용 |
|---|---|
| 형식 | `{colorSpace, components, hex}` 를 갖췄는가 · `colorSpace` 가 `oklch` 인가 |
| 일관성 | `components` 를 sRGB 로 역변환한 값이 `hex` 와 같은가 |

두 시나리오로 시험했다 — ① `hex` 만 바꿈 → 빌드가 `components 와 hex 가 어긋난다` 로 실패 ② 둘을 함께 다른 색으로 바꿈 → `verify.mjs` 가 값 변경으로 실패. 어느 쪽도 조용히 통과하지 않는다.

**소비 지점** — `build.mjs`(2곳) · `preview.html`(4곳) · `verify.mjs` · `surfaces.mjs` 에 `hexOf()` 를 넣어 객체에서 hex 를 꺼내게 했다. `verify.mjs` 섹션 C(무채색 무회귀)는 원본 JSON 비교에서 **해석된 hex 비교**로 바꿨다 — 형식 변경은 허용하되 색 값 변경·삭제·추가는 여전히 잡는다.

**프리뷰** — primitive 색 칩에 oklch 성분 줄을 추가했다. hex 만 보이면 전환의 이득이 화면에 안 나타난다.

---

### 0-8. 정합성 감사와 이주 3차 — 타이포·자간·반경

2026-08-25. 스펙 원문(Format 91KB · Color)을 내려받아 조항만으로 검사기를 새로 작성하고 4개 JSON 을 전수 감사했다. **오류 50건**이 나왔고, 모두 이전부터 있던 것이었다.

| 조항 | 건수 | 처리 |
|---|---:|---|
| 9.8 typography 하위 타입 | 33 | `lineHeight` 를 `dimension`(rem) → `number`(배수)로 전환 |
| 9.8 typography 필수 속성 | 12 | `compact` 6건에 `fontFamily`·`fontWeight` 보충 |
| 8.2 dimension 단위 | 4 | `letterSpacing` `em` 유지 + 예외 표시 |
| 8.2 dimension 단위 | 1 | `radius.circle` `%` → CSS 문자열 + 예외 표시 |

**통과한 것** — 이름 규칙(`$` 시작·`{}`·`.` 금지) · `shadow` 5속성 · `color` 101건 · 별칭 154건 무결성 · `$extensions` 표기.

#### 행간을 배수로 바꾼 판단

스펙 9.8 은 `lineHeight` 를 **fontSize 배수인 `number`** 로 요구한다. 2-1 에서 세운 "2px 그리드를 원본으로" 는 절대값을 전제했으므로 정면 충돌이었다.

**전환을 택한 이유**

| | |
|---|---|
| 그리드는 렌더링에서 유지된다 | 배수 복원 오차 최대 **0.000008px**. 브라우저 레이아웃 격자(1/64px)보다 2000배 작다. 실제로 프리뷰에서 `body-md` 22px · `heading-md` 36px · `label-sm` 16px · `display-lg` 72px 로 확인했다 |
| CSS 상속이 옳아진다 | 3-1 에 기록한 rem 전환 시 상속 동작 변화가 원복된다. 무단위 행간이 CSS 정석이다 |
| 접근성 규칙과 단위가 맞는다 | `CLAUDE.md` 의 "본문 행간 ≥1.5" 가 배수 기준인데 토큰도 배수가 됐다 |

잃은 것은 "그리드에 정확히 떨어진다"는 **구성상 보장**이고, 화면상의 정렬이 아니다.

#### 이름을 `글자px-행간px` 로 한 이유

배수 값(`1.571429`)만으로는 2px 그리드에서 나왔다는 근거가 토큰에서 사라진다. 검토한 대안과 버린 이유는 이렇다.

| 안 | 버린 이유 |
|---|---|
| 역할 이름(`tight`/`normal`/…) | 배수가 **12종**이라 4~5개 티어로 못 담는다. 게다가 `16-24`와 `12-18`이 둘 다 1.5, `24-32`와 `12-16`이 둘 다 1.333333 이라 역할 이름은 **서로 독립인 결정을 하나로 합쳐버린다** |
| typography 에 배수 직접 기입 | 행간 하나를 2~3개 토큰이 공유한다(`body-md`·`body-md-strong`·`link-md`). 인라인하면 한 결정을 세 곳에서 고쳐야 하고 인라인 금지 규칙과도 충돌 |

토큰은 13개 → **14개**가 됐다. `lineHeight.26` 이 16px·18px 두 곳에 쓰여 `16-26`(1.625)·`18-26`(1.444444)로 갈라졌다.

**새 가드** — `assertLineHeightPairing()` 이 매 빌드에서 4가지를 검사한다: 키 형식 · `fontSize` 와의 짝 · `$type`=number · 배수↔px 복원. 세 시나리오로 고의 파손 시험을 했고 전부 실패로 잡혔다. 이름이 담은 "이 크기 전용"이라는 약속이 문서가 아니라 빌드로 강제된다.

#### `letterSpacing` 은 (이때는) 맞추지 않았다 — **2026-08-25 에 전환했다, 0-11 참조**

`em` 은 DTCG `dimension` 이 담지 못한다(px·rem 만). 그러나 자간은 **글자 크기에 비례해야** 하고, 밴드 하나가 여러 크기를 덮는다 — `heading` 은 24·28·32·36px 넷을 덮는다. `rem` 으로 바꾸면 4개가 **10개**로 늘고 밴드 구조가 사라진다. 스펙의 한계이지 우리 설계의 결함이 아니므로 `em` 을 유지하고 예외 표시했다.

`$type: "dimension"` 은 **남겨뒀다.** 빼면 typography 의 `letterSpacing` 참조가 무타입 토큰을 가리켜 위반이 4건에서 37건으로 번진다. 위반을 4건 안에 가두는 쪽을 택했다.

#### `compact` 은 토큰만 채우고 CSS 는 그대로

스펙은 소스 병합을 **토큰 단위 교체**로 정의한다(속성 단위 병합이 아니다). 그래서 `compact` 도 5속성을 다 갖춰야 한다. 다만 `build.mjs` 는 미디어쿼리에 `font-family`·`font-weight` 를 다시 쓰지 않는다(`full=false`) — **JSON 이 스펙을 지키고 빌드가 출력을 최적화**하는 것이 원본/산출물 분리에 맞다.

#### 남은 예외 13건

| 대상 | 건수 | 이유 |
|---|---:|---|
| `gradient.solid`·`multiple` | 8 | `currentColor` — 런타임 상속값. **2026-09-01 에 `gradient.fade` 4건으로 축소(0-15)** |
| `letterSpacing` | 4 | `em` — 글자 크기 상대 단위 |
| `radius.circle` | 1 | `%` |

**오류 0건 · 미표시 0건.** 셋 다 DTCG 타입 체계에 없는 CSS 개념이다.

#### 검토했으나 이번에 하지 않은 것 — Resolver Module

`compact` 을 자체 `$extensions` 로 옮기는 안을 검토하다가 **Resolver Module 2025.10** 을 확인했다. 도입부가 드는 예가 정확히 우리 두 경우다 — *Theming(light/dark)* 와 *Sizing(mobile/tablet/desktop)*. `sets`/`modifiers`/`contexts` 구조가 우리 파일 배치와 거의 1:1 이라, 도입하면 `semantic.light`↔`dark` 키 동기화나 `compact.X`↔`base.X` 이름 대응처럼 **지금은 아무것도 강제하지 않는 규약들이 규격으로 올라간다.**

미룬 이유는 **성숙도**다. Format·Color 는 *Final Community Group Report* 인데 Resolver 는 *Candidate Recommendation* 이다. "고정 릴리스만 근거로 쓴다"(0-1)의 예외를 만드는 일이라 별도 결정이 필요하다.

표준 경로가 있으므로 `$extensions` 로 자체 해결하지 않았다.

---

### 0-9. 파일 합성을 규격 개념으로 올리고 테마별 번들을 낸다

2026-08-25. 별칭 370건 중 **361건이 파일 경계를 넘는다**(semantic.light 98 · semantic.dark 98 · typography 165). 자기 파일 안에서 풀리는 건 `interaction.opacity` → `opacity` 9건뿐이다.

Format Module 의 별칭 해석은 **문서(파일) 단위**다 — 7.2.1 은 "Navigate to token"으로 문서 안을 탐색하고, JSON Pointer 의 `#/` 는 document root 다. 즉 `semantic.light.json` 만 읽는 도구에게 `{color.gray.900}` 은 해석 불가능한 참조다.

**단일 원본성 자체는 문제가 없었다.** 값은 primitive 리프에만 있고(인라인 0건) 산출물은 전부 생성물이다. 부족한 것은 **그 구조를 기계가 읽을 수 있게 선언한 것**이었다.

#### 한 것 — 개념 차용 + 번들

`build.mjs` 에 `COMPOSITION` 상수를 두고 파일 로딩·번들 생성을 여기서 구동한다.

```js
const COMPOSITION = {
  sets:      { base: ['primitive.json', 'typography.json'] },
  modifiers: { theme: { light: ['semantic.light.json'], dark: ['semantic.dark.json'] } },
  resolutionOrder: ['sets.base', 'modifiers.theme'],
}
```

그전에는 이 구성이 `prim`·`L`·`D`·`TYPO` 라는 **변수 이름에만** 있었다. "L 이 theme=light 컨텍스트"라는 사실이 코드 관례였다.

여기서 `dist/tokens.light.json` · `dist/tokens.dark.json` 을 낸다. 테마별 **단일 DTCG 2025.10 문서**로, 별칭 272건이 전부 문서 안에서 해석된다. 별칭을 값으로 풀지 않는다 — 참조 관계가 보존돼야 도구가 의미를 읽는다.

| | |
|---|---|
| 토큰 수 | 358개 = primitive 258 + typography 33 + semantic 67 |
| 키 충돌 | 없음 — `color` 는 primitive 15계열과 semantic 14그룹이 서로 겹치지 않는다 |
| 두 번들 차이 | 값이 다른 토큰 49개, 전부 semantic |
| 감사 | 오류 0 · 예외 26(13×2) |

**새 가드 2종** — `assertCompositionCoversSources()`(디스크 ↔ 등록 양방향) · `assertBundleSelfContained()`(번들 미해석 별칭 0건). 세 시나리오로 고의 파손 시험을 했고 전부 실패로 잡혔다.

#### Resolver 파일은 두지 않았다

| | |
|---|---|
| 성숙도 | Format·Color 는 *Final CG Report* 인데 Resolver 는 *Candidate Recommendation* 이다. 0-1 의 "고정 릴리스만 근거로 쓴다"의 예외가 된다 |
| 도구 지원 | Style Dictionary 도 Resolver 는 진행 중(🚧)이다. 파일을 둬도 오늘 읽는 도구가 없다 |
| **미루는 대가** | **없다.** 구성이 데이터로 있으므로 필요해지면 `COMPOSITION` 을 직렬화하면 된다 |

처음에는 resolver 파일을 손으로 관리하고 양방향 가드를 붙이는 안을 세웠으나, 구성을 데이터로 올리면 그 파일이 **생성 가능한 산출물**이 되므로 손으로 관리할 이유가 없다. 그 안은 버렸다.

#### 검토했다 버린 것 — 호환 번들

색을 hex 문자열, 크기를 `"16px"` 문자열로 낮춘 이전 형식 번들을 추가하는 안을 검토했다. 근거는 "오늘의 도구는 2025.10 객체 형식을 못 읽는다"였다.

**그 전제가 틀렸다.** [style-dictionary#1590](https://github.com/style-dictionary/style-dictionary/issues/1590) 확인 결과 **Color module · Dimension token types 는 이미 완료(✅)** 이고, 진행 중인 것은 Gradient · Duration · **Resolver** 다. 근거로 삼았던 penpot 이슈는 그 도구의 미구현이었을 뿐인데 도구 일반으로 일반화한 것이었다.

Figma 도 이유가 되지 않는다 — Figma Variables 는 자체 형식이라 어떤 형식으로 내보내도 플러그인 변환을 거친다. 형식을 낮춰도 어댑터는 그대로 필요하다.

버린 이유를 정리하면 이렇다.

| | |
|---|---|
| 전제 미검증 | 필요하다고 말한 소비자가 없다. 가정을 위해 짓는 일이었다 |
| 정보 손실 | 호환 번들에는 `hex` 만 남아 OKLCH 성분이 사라진다. 0-7 의 설계 근거가 그 산출물에서 소실된다 |
| 공식화 위험 | `exports` 에 올리면 소비자가 superseded 형식 위에 쌓고, 나중 마이그레이션을 우리가 떠안는다 |

**필요한 소비자가 실제로 나타나면 그때 빌드 타깃을 하나 더 만든다.** 그 시점엔 "누가 왜"가 분명해서 형식도 정확히 맞출 수 있다.

역설적으로 이 확인이 번들의 근거를 강화했다 — Style Dictionary 가 못 하는 것이 정확히 **Resolver(파일 합성)** 이고, 번들이 그 한 가지를 대신해 준다. 형식을 낮추는 게 아니라 형식은 정식으로 두고 합성만 미리 해서 넘기는 것이다.

#### `package.json` 공개 API

```
.            → tokens/tokens.js          (ESM export 21개)
./css        → tokens/tokens.css         (CSS 변수 224 + 클래스 33)
./dtcg/light → tokens/dist/tokens.light.json   ← 신설
./dtcg/dark  → tokens/dist/tokens.dark.json    ← 신설
```

배포 파일 5개, 210.9 kB. JSON 원본 · `build.mjs` · `preview.html` 은 여전히 제외한다.

---

### 0-10. 티어 스케일의 순서를 빌드가 지킨다 — `gradient.maskSize`

2026-08-25. 2-6 에서 미뤄둔 `gradient.maskSize` 이름 통일(`xsmall`~`xlarge` → `xs`~`xl`)을 하다가 **값이 이름 순서를 거스르고 있는 것**을 발견했다.

```
작업 전:  xsmall 24 · small 32 · medium 40 · xlarge 56 · large 64
                                              ↑ xlarge 가 large 보다 작다
```

JSON 기입 순서가 **이름 순서가 아니라 값 순서**(24·32·40·56·64)를 따르고 있었다. 값 간격은 8·8·**16**·8 로, 8단위 수열 `24 32 40 48 56 64` 에서 48이 빠진 모양이다.

**추정** — 원래 `xsmall/small/medium/large` = 24/32/40/64 네 단계였고, 나중에 56이 필요해져 **값 위치에 끼워 넣고 남은 이름 `xlarge` 를 붙인** 것으로 보인다. 단정할 수는 없다. git 이 없어 이력이 없고 이 문서에도 유래 기록이 없다.

#### 값을 지키고 이름을 고쳤다

```
xs 24 · sm 32 · md 40 · lg 56 · xl 64
```

**값 5개는 하나도 바꾸지 않았다.** 56과 64가 어느 이름에 붙는지만 오름차순으로 재배치했다.

**검토했다 버린 안** — 숫자 키(`maskSize.24 · 32 · …`)로 바꾸는 안을 한 번 적용했다가 되돌렸다. 근거로 "역할 기록이 없으니 티어 이름을 쓸 자격이 없다"를 들었는데, `xl > lg` 는 추측이 아니라 **티어 어휘의 정의**이므로 값을 지어내는 문제가 아니었다. 판단을 정정했다. 티어 이름 쪽이 `breakpoint`·`shadow`·`layout` 과 어휘가 통일된다.

#### 진짜 교훈 — 역할 기록이 없는 티어 스케일이 사고를 허용했다

꼬인 스케일이 하필 **DS 에서 유일하게 토큰별 `$description` 이 0인 티어 스케일**이었다.

| 스케일 | 키 | 토큰별 역할 기록 |
|---|---|---:|
| `breakpoint` | 티어 | 5/5 |
| `shadow`(semantic) | 티어 | 5/5 |
| `layout.*` | 티어 | 전부 있음 |
| **`gradient.maskSize`** | 티어 | **0/5** |

`breakpoint` 에 새 값을 끼우려면 "이건 태블릿 가로인가 데스크톱인가"를 정해야 해서 이름 충돌이 바로 드러난다. `maskSize` 는 정할 게 없어 아무 이름이나 붙여도 걸리지 않았다.

역할을 지어내 채우는 대신 **구조로 막았다.** `assertTierScalesOrdered()` 가 매 빌드에서 티어 스케일 6개의 값 순서를 검사한다. 복합 값(`shadow`)은 단일 수치가 없어 제외한다. 의도적 역순은 `TIER_EXEMPT` 에 이유와 함께 등록하고(현재 `layout.margin` 1건 — lg·xl 은 maxWidth 가 여백을 대신해 0), **예외가 더는 티어 스케일이 아니게 되면 그것도 실패**시켜 죽은 예외를 막는다.

시험 3종을 모두 통과했다 — 옛 상태 재현 · `breakpoint` 역전 · 죽은 예외.

#### 같은 유형을 전수 조사했다

스케일 58개를 5가지 흔적으로 훑었다(`anomaly.mjs`).

| 검사 | 결과 |
|---|---|
| ① 티어 이름 ↔ 값 순서 어긋남 | `layout.margin` 1건 — **의도**(`$description` 에 근거 있음), 가드에 예외 등록 |
| ② 기입 순서 ≠ 값 순서 | 4건 — 전부 **의도**(`fontWeight` 내림차순 · `lineHeight` 글자크기순 · `ratio` 가로/세로 구분) |
| ③ 규칙적 수열의 구멍 | `maskSize` 1건 — 48 누락 여부는 미확인 |
| ④ 티어 이름인데 역할 기록 0 | `maskSize` 1건 — 가드로 대체 보호 |
| ⑤ 같은 값을 가진 형제 | 10건 — 전부 **의도**(`lineHeight` 2쌍은 0-8 에 기록 · `layout` 은 "xs·sm 공통") |

**부수 발견 — `ratio` 에 같은 유형이 있다.** ②를 보다가 나왔다.

- `ratio.vertical` 4개(`1-1`·`3-4`·`2-3`·`1-2`)가 `ratio.horizontal` 안에 **키도 값도 동일하게** 또 있다. 21개 중 4개가 중복이라 고유 비율은 17개다.
- `ratio.horizontal` 이 **세로 비율 8개**(`4-5`·`3-4`·`2-3`·`10-16`·`golden-v`·`9-16`·`1-2`·`9-21`, 전부 값<1)를 담고 있다. 그룹 이름이 내용과 맞지 않는다.
- 여기도 `$description` 이 그룹 2개·토큰 21개 **전부 없다.** 같은 조건이 반복된다.

미결로 남긴다(§4). 미참조 토큰이라 소비자 영향은 없다.

**`anomaly.mjs` 의 한계** — 내림차순 정렬을 이상으로 잡고, 그룹 사이 중복은 잡지 못한다(`ratio` 중복은 ②의 부수 관찰로 나왔다).

---

### 0-11. 자간을 크기별 rem 으로 — 예외 4건 소멸

2026-08-25. 0-8 에서 "스펙이 표현하지 못하는 값"으로 남겨둔 `letterSpacing` 4건을 스펙 안으로 옮겼다.

#### 전환을 가능하게 한 두 가지 사실

**① 글자 크기 하나에 밴드가 하나뿐이다.** 실측하니 11개 크기가 각각 정확히 하나의 밴드에 대응했다 — 한 크기에 밴드가 둘 걸린 경우 **0건**.

```
12·14 → none        16·18·20 → title
24·28·32·36 → heading    40·60 → display
```

밴드가 "여러 크기를 하나로 덮는" 이득은 primitive 토큰 수에서만 났고, typography 쪽은 어차피 크기마다 다른 값이 필요했다.

**② `fontSize` 가 전부 `rem` 이라 `em` → `rem` 이 정확히 동등하다.** `em` 은 요소 글자 크기 상대, `rem` 은 루트 상대인데, 우리 글자 크기가 rem 이므로 둘이 같은 배율로 움직인다.

| 크기 | 밴드 | em | 계산 | rem |
|---:|---|---:|---|---:|
| 12·14 | none | 0 | — | 0 |
| 16 | title | −0.01 | ×1rem | −0.01 |
| 18 | title | −0.01 | ×1.125rem | −0.01125 |
| 20 | title | −0.01 | ×1.25rem | −0.0125 |
| 24 | heading | −0.015 | ×1.5rem | −0.0225 |
| 28 | heading | −0.015 | ×1.75rem | −0.02625 |
| 32 | heading | −0.015 | ×2rem | −0.03 |
| 36 | heading | −0.015 | ×2.25rem | −0.03375 |
| 40 | display | −0.02 | ×2.5rem | −0.05 |
| 60 | display | −0.02 | ×3.75rem | −0.075 |

**검증** — 33개 클래스의 계산된 자간 픽셀이 **오차 0으로 완전 일치**했다. `tokens.css` diff 는 letter-spacing 줄에만 걸렸다.

#### 설계 의도는 강화됐다

2-2 가 밴드를 만든 목적은 **"자간은 독립 축이 아니라 글자 크기의 종속 함수"** 를 표현하는 것이었다. 그런데 그 의도는 **문서에만 있고 강제되지 않았다** — `label-sm`(12px)에 `letterSpacing.display`(40px 이상용)를 물려도 빌드가 통과했다.

이제 키가 크기 자체(`letterSpacing.12` … `.60`)이고 `assertLetterSpacingPairing()` 이 검사한다.

| 검사 | 내용 |
|---|---|
| 키 ↔ fontSize | 짝이 어긋나면 실패 |
| 타입·단위 | `dimension` · `rem` 이 아니면 실패 |
| 파생 일치 | 값이 `trackingBands` 의 어느 밴드 × 크기로도 설명되지 않으면 실패 |
| 1:1 | 쓰이지 않는 자간 토큰이 남아 있으면 실패 |

시험 4종을 모두 통과했다. `lineHeight` 의 짝 검사(0-8)와 같은 형태라 두 하위 속성이 대칭이 됐다.

#### 대가

| | |
|---|---|
| 토큰 4 → **11개** | 크기를 추가하면 자간 토큰도 추가해야 한다. `lineHeight` 는 이미 그랬다 |
| 밴드 단위 수정 | `heading` 밴드를 조정하면 4개 토큰 + 확장을 함께 고쳐야 한다. 어긋난 상태로는 빌드가 통과하지 않는다 |
| 동작 차이 1건 | `.body-md` 를 적용한 뒤 **font-size 만 CSS 로 덮어쓰면** `em` 은 따라오지만 `rem` 은 따라오지 않는다. 권장하지 않는 패턴이다 |

#### 결과 — 참조가 있는 예외가 0이 됐다

| | 전 | 후 |
|---|---:|---:|
| 예외 총계 | 13 | **9** |
| 그중 **참조가 있는** 예외 | **4** | **0** |
| 번들 예외(13×2 → 9×2) | 26 | 18 |

남은 9건은 `gradient` 방향형 8 · `radius.circle` 1 이고 **참조가 0이라 무해**하다. 0-8 에서 제기한 "typography 33개로 번질 수 있다"는 연쇄 위험이 사라졌다 — 그 위험 자체는 스펙 명문이 아닌 추론이었지만, 불확실성을 없애는 값이 토큰 7개였다.

---

### 0-12. 불투명도 사다리 — 사실상 중복인 단계를 없앴다

2026-08-25. `opacity` 26개를 점검했다. 원래는 "눈금이 불규칙하다"고 판단했는데 **절대 간격으로 본 오류**였다. 알파는 상대 비율로 지각된다 — 4→5 는 +25%, 80→88 은 +10%. 비율로 재보면 사다리는 대체로 건강했다.

```
25단계 중 23개가 비율 1.057 ~ 1.333   ← 낮은 쪽 촘촘 / 높은 쪽 성김. 정상이다
```

이상한 것은 두 쌍뿐이었다.

| 단계 | 비율 | |
|---|---:|---|
| 60 → 61 | **1.017** | 사실상 같은 값 |
| 69 → 70 | **1.014** | 사실상 같은 값 |

#### 한 것

| | |
|---|---|
| `opacity.69` **삭제** | 참조 0회. 전 파일 재확인 후 제거. `69/70` 이상도 함께 사라짐 |
| `opacity.61` → `60` **병합** | 다크 `bg.transparent`·`transparentAlternative` 2건의 알파 참조를 `{opacity.60}` 으로 |

**26개 → 24개**, 모든 인접 비율이 1.057 이상이 됐다.

**실제 색 변화는 다크 2줄뿐이다.**

```
--color-bg-transparent              rgba(33,33,36,0.61) → rgba(33,33,36,0.6)
--color-bg-transparent-alternative  rgba(33,33,36,0.61) → rgba(33,33,36,0.6)
```

라이트는 전혀 바뀌지 않았고 **AA 대비 144건 미달 0** 으로 동일했다. 병합 방향은 두 쪽 다 시뮬레이션해 대비 결과가 같음을 확인한 뒤 골랐다 — 반대 방향(60→61)은 `ios.glassBackground` 만 건드려 "실질 무변경"이었으나, `60` 이 더 둥근 수이고 남는 사다리(`52·60·70·74·80·88`)가 읽기 좋아 이쪽을 택했다.

#### 검토했다 버린 안 — 소비자별 분리

`opacity` 를 색·그림자·상호작용 셋으로 쪼개는 안을 제안했다가 철회했다. 근거는 "`opacity.88` 하나가 텍스트 색과 다크 그림자에 동시에 물려 있다"였는데, **세 군데서 어긋났다.**

| | |
|---|---|
| `CLAUDE.md` 가 반대 | "여러 파운데이션이 참조하는 스케일(`opacity`)은 어느 한 곳에 귀속시키지 않는다" — 규칙이 `opacity` 를 이름으로 지목해 공유 스케일로 규정하고 있다 |
| 0-5 를 되돌림 | 오늘 `interaction.opacity` 중복을 없애 하나로 합쳤는데, 분리는 그 상태로 되돌아간다 |
| **공유는 결함이 아니다** | 색에서 훨씬 심한 공유가 정상으로 돌아간다 — `coolGray.500` 을 `line` 4종 + `fill` 3종 **7개 역할**이 쓴다. 그게 스케일이 하는 일이다 |

#### 새 규칙 — 인접 비율 1.03

`opacity` 그룹 `$description` 과 `verify.mjs` 에 "인접 단계 비율이 1.03 미만이면 사실상 중복" 규칙을 넣었다. 육안으로 구분되지 않으면서 사다리만 흐리는 단계를 막는다.

`verify.mjs` 갱신 4건 — 삭제 허용 목록(`61`·`69`, 이유 포함) · **되살아남 검사**(죽은 허용 방지) · 비율 규칙 · 다크 `bg` 알파 변경 등록. 시험 4종을 모두 통과했다.

---

### 0-13. `ratio` — 구조를 평탄화하고 값을 12개로

2026-08-27. 미결 12번(`ratio` 구조 어긋남)을 처리했다.

#### 무엇이 잘못돼 있었나

| | |
|---|---|
| 중복 | `vertical` 4개(`1-1`·`3-4`·`2-3`·`1-2`)가 `horizontal` 안에 **키도 값도 동일하게** 또 있었다. 토큰 21개 중 고유값은 17개 |
| 이름과 내용 불일치 | `horizontal` 이 **세로 비율 8개**(값<1)를 담고 있었다 |
| 기록 부재 | 그룹 2개·토큰 21개 전부 `$description` 이 없었다 |

#### 다른 시스템 조사 — 넷 다 평면이다

| 시스템 | 개수 | 키 방식 |
|---|---:|---|
| Tailwind CSS | 3 | 역할 — `square` · `video`(16/9) · `auto` |
| Bootstrap 5.3 | 4 | **`WxH`** — 1x1 · 4x3 · 16x9 · 21x9 |
| Open Props | 6 | 역할 — square · landscape · portrait · widescreen · ultrawide · golden |
| Carbon | 9 | **`WxH`** — 1x1 · 2x1 · 3x2 · 4x3 · 16x9 · 2x3 · 3x4 · 9x16 · 1x2 |

**가로/세로를 하위 그룹으로 나누는 곳이 하나도 없다.** 방향은 키가 말한다. 그래서 평탄화했다.

키 방식은 역할파와 `WxH` 파로 갈리는데, 우리 명명 원칙("값이 바뀌어도 이름이 맞는가")상 종횡비는 **이름이 곧 값**이므로 `WxH` 가 맞다 — 이미 그 형태였다.

#### 결과 — 21개(고유 17) → 12개, 평면

```
1-2 0.5 · 9-16 0.5625 · 2-3 0.666667 · 3-4 0.75 · 4-5 0.8 · 1-1 1
4-3 1.333333 · 3-2 1.5 · golden 1.618 · 16-9 1.777778 · 2-1 2 · 21-9 2.333333
```

CSS 변수가 `--ratio-horizontal-16-9` → `--ratio-16-9` 로 짧아졌다. `$description` 12/12 작성(이전 0/21).

**제거한 5개** — 전부 조사한 네 시스템 어디에도 없고 참조도 0이었다.

| | |
|---|---|
| `16-10`(1.6) | 구형 노트북 화면비 |
| `5-4`(1.25) | 인화지(8×10) 비율. 웹 용례 희박 |
| `10-16`(0.625) | `16-10` 의 역수 |
| `golden-v`(0.618047) | 황금비 역수. 실사용 사례 없음 |
| `9-21`(0.428571) | 울트라와이드 역수. 실사용 사례 없음 |

**남긴 판단 두 건** — `4-5`(0.8)는 채택 0곳이지만 소셜 세로 게시물 표준이라 유지했다. `golden`(1.618)은 숫자 키 사이의 유일한 명명 상수인데, "황금비"는 이름이 곧 값이라 규칙 위반이 아니다.

#### 새 가드

`assertRatioKeysMatchValues()` — 키 `가로-세로` 와 값 가로÷세로가 어긋나면 실패시킨다. `ratio` 는 참조가 없어 방향이 뒤집혀도 화면으로 드러나지 않기 때문이다. `golden` 은 `RATIO_EXEMPT` 에 이유와 함께 등록했고, 예외가 사라지면 그것도 실패시킨다. 시험 3종 통과.

**영향 없음** — 참조 0건이라 색·대비가 바뀌지 않았다. `tokens.css` diff 는 `ratio` 줄에만 걸렸다.

---

### 0-14. 상태 오버레이 — 강도만 있고 색이 없었다

2026-09-01. `primitive.interaction` 을 점검하다 발견했다.

#### 무엇이 비어 있었나

`interaction.opacity.*` 9건은 **얼마나 얹을지**(0.05·0.08·0.12…)만 정의하고, **무엇을 얹을지**는 어디에도 없었다. 불투명도만으로는 렌더링할 수 없다. 토큰별 `$description` 도 0/9 였다 — `maskSize`·`ratio` 를 꼬이게 한 것과 같은 조건이다.

#### 먼저 `color.fill` 과 중복인지 봤다 — 아니다

| | `interaction.opacity` | `color.fill` |
|---|---|---|
| 계층 | primitive | semantic |
| 산출 | **숫자** | **색**(rgba) |
| 축 | 강도 3 × 상태 3 = 9 | 강도 3 |
| 테마 | 무관 | 적응(라이트 8/5/16 · 다크 22/12/28) |
| 역할 | 상호작용 시 얹는 층 | 정지 상태의 면 |

값이 겹쳐 보이는 것(`fill.alternative` 0.05 = `normal.hovered` 0.05)은 둘 다 같은 `opacity` 사다리를 참조하기 때문이며 0-5 에서 의도한 것이다.

이때 **"참조 0건은 `interaction` 의 결함"이라고 먼저 적었던 것을 취소한다.** 세어보니 `color.fill` 0건 · `color.interaction` 0건 — semantic 잎 토큰은 원래 다른 토큰이 참조하지 않고 컴포넌트가 소비한다.

#### base 를 하나로 정할 수 없다는 것을 측정으로 확인했다

`fill` 을 따라 base 를 `coolGray.500` 으로 두면 어떻게 되는지 OKLab 밝기변화(ΔL)로 쟀다.

| 테마 | 면 | hover(0.05) ΔL | |
|---|---|---:|---|
| 라이트 | `bg.normal` #FFFFFF | −0.0209 | 어두워짐 ✔ |
| 라이트 | `primary.normal` #2E2F32 | **+0.0123** | 밝아짐 ✘ |
| 다크 | `bg.normal` #1A1B1C | +0.0176 | 밝아짐 ✔ |
| 다크 | `primary.normal` #F7F7F8 | **−0.0207** | 어두워짐 ✘ |

`primary` 는 **면이 테마와 반대로 뒤집힌다** — 라이트에서 `gray.800`(어두움), 다크에서 `gray.50`(밝음). 중립 회색 하나로는 버튼과 카드가 서로 반대로 움직인다. 오버레이 base 는 **테마가 아니라 얹히는 면의 밝기**를 따라야 한다.

#### 결정 — `color.interaction.overlay.{darken,lighten}` 6키

```
darken.{hovered,focused,pressed}   base {color.gray.1000}  ← 밝은 면 위
lighten.{hovered,focused,pressed}  base {color.gray.0}     ← 어두운 면 위
alpha = {interaction.opacity.normal.<상태>}
```

이름은 **하는 일**을 말한다(`onLight`/`onDark` 는 용도라 헷갈린다). base 가 두 테마에서 같은 것은 의도이며 `color.static` 이 같은 성격의 선례다.

**티어는 `normal` 만 올렸다.** `light`·`strong` 까지 올리면 18키 × 2파일 = 36항목인데 소비자가 0인 지금은 과하다. 두 티어는 primitive 숫자로 남아 있고 컴포넌트가 필요할 때 CSS 변수로 직접 고른다.

#### 검증 — 10개 면 전부에서 방향이 맞다

| 테마 | 면 | 고른 것 | hover ΔL | press ΔL |
|---|---|---|---:|---:|
| 라이트 | `bg.normal` / `elevated` | darken | −0.0388 | −0.0933 |
| 라이트 | `bg.*Alternative` | darken | −0.0361 | −0.0908 |
| 라이트 | `primary.normal` | lighten | +0.0394 | +0.0965 |
| 다크 | `bg.normal` | lighten | +0.0465 | +0.1108 |
| 다크 | `bg.normalAlternative` | lighten | +0.0540 | +0.1253 |
| 다크 | `bg.elevated` / `elevatedAlternative` | lighten | +0.0453 / +0.0525 | +0.1079 / +0.1184 |
| 다크 | `primary.normal` | darken | −0.0361 | −0.0908 |

`coolGray.500` 안(ΔL 0.017~0.023, 방향 2건 오류)보다 **강도가 고르고**(0.036~0.054) 방향 오류가 없다.

#### 빌드 수정 한 건

`opacityVal` 이 `{opacity.N}` 을 정규식으로만 받아서 `{interaction.opacity.normal.hovered}` 를 넣으면 `null.match` 로 죽었다. `deref`/`primNode` 를 재사용해 **별칭 체인을 끝까지 따라가고 숫자가 아니면 실패**하도록 바꿨다. 교체 후 산출물이 바이트 동일한 것으로 기존 42건에 영향이 없음을 확인했다.

#### DTCG 적합성 — 어기는 것 하나, 새로 생긴 것은 아니다

Format Module 2025.10 대조: 별칭 체인 ✔(별칭이 별칭을 가리켜도 되고 도구는 끝까지 따라가야 한다) · 이름 규칙 ✔ · `$type` ✔(잎마다 직접 단다) · `$description` ✔.

어기는 것은 **`$extensions` 안의 alpha** 다. 스펙은 확장 데이터를 "토큰 값을 이해하는 데 필수적이지 않은" 선택적 메타데이터로 제한하라고 하고(SHOULD), `$extensions` 안의 참조 해석은 정의하지 않는다. 제3자 도구는 `overlay.darken.hovered` 를 5% 검정이 아니라 **`#000000` 완전 불투명**으로 읽는다.

다만 이건 **이번에 생긴 게 아니다** — 알파 확장을 쓰는 토큰이 이미 42건(라이트 21 · 다크 21) 있고 `fill.normal` 도 같은 방식으로 읽힌다. 6키를 다르게 만들면 오히려 42건과 어긋난다. native `alpha` 를 쓰지 않는 이유(0~1 숫자만 받아 `{opacity.N}` 참조를 못 담음)도 Color Module 원문과 일치한다.

**미결로 남긴다** — 번들(`dist/`)은 해석된 산출물이라 거기서는 알파가 숫자다. 소스는 확장을 유지하고 **번들만 native `alpha` 로 내보내면** 48건 전부가 타 도구에서 제대로 읽힌다. 이번 범위 밖이라 손대지 않았다(미결 15).

#### 결과

토큰 421 → 433(semantic 색 67→73 × 2테마). `$description` 은 `interaction.opacity` 9건 + 하위 그룹 1건 + overlay 6건 + 그룹 1건을 새로 썼다. DTCG 오류 0 · 예외 9(소스)/18(번들) 변동 없음, AA 192건 미달 0 변동 없음.

#### 보강(2026-09-02) — 검토했다 버린 대안 둘, 그리고 재측정

컴포넌트 착수 문서를 검토하다 *"오버레이는 **테마별**로 바꿔야 한다"* 는 서술이 나왔다. **틀렸고**, 그 김에 다른 시스템이 어떻게 푸는지도 확인했다. 같은 질문이 또 나오지 않게 여기 남긴다.

**재측정 — "테마별"은 `primary` 에서 두 테마 모두 실패한다.** hover(α=0.05)를 실제 합성해 sRGB 상대휘도 변화를 다시 쟀다(위 표는 OKLab ΔL, 이번은 상대휘도라 절대값이 다르다. 방향과 우열은 같다).

| 테마 · 면 | 면 L | darken ΔL | lighten ΔL |
|---|---|---:|---:|
| 라이트 `bg.normal` | 1.000 | **0.1100** ✔ | 0.0000 |
| 라이트 `primary.normal` | 0.028 | 0.0026 ✘ | **0.0130** |
| 다크 `bg.normal` | 0.011 | 0.0008 | **0.0088** ✔ |
| 다크 `primary.normal` | 0.931 | **0.1022** | 0.0034 ✘ |

"라이트=darken · 다크=lighten" 규칙은 `primary` 에서 **라이트 5배 · 다크 30배** 약한 쪽을 고른다. 다크의 0.0034 는 사실상 안 보인다.

원인은 **같은 테마 안에 밝은 면과 어두운 면이 공존**한다는 것이다. 해석된 색 41개 중 16개가 테마 사이에서 밝기가 반전되는데 **방향이 서로 반대**다 — `bg.normal` 은 1.000→0.011(밝음→어두움), `primary.normal` 은 0.028→0.931(어두움→밝음). 테마는 면의 밝기를 결정하지 못한다.

**다른 시스템 — 테마로 고르는 곳은 없다.**

| 시스템 | 방향을 결정하는 것 |
|---|---|
| Material 3 | **콘텐츠(`on-`) 색.** state layer 는 그 컨테이너의 텍스트 색을 hover 8% · focus 12% · pressed 12% 로 얹는다. `on-primary` 는 정의상 `primary` 와 대비가 크므로 방향이 자동으로 맞는다 |
| Primer(GitHub) | **계산하지 않는다.** `--button-primary-bgColor-hover` 처럼 역할×상태 토큰을 명시하고 방향을 토큰마다 박는다 |
| 우리 | **면의 밝기.** `darken`/`lighten` 을 주고 면에 따라 고른다 |

셋 다 **면·컴포넌트를 따라가며 테마를 따라가지 않는다.** M3 방식은 우리와 결과가 같다 — `on-` 색은 그 면과 대비되도록 정의되므로 밝은 면엔 어두운 오버레이가 자동으로 걸린다. 표현만 다르다.

**그럼에도 M3 방식을 쓰지 않는 이유.** 모든 면에 짝 `on-` 색 토큰이 필요한데 우리에겐 그 계층이 없다 — `label.*` 과 `inverse.label` 뿐이고 면은 `bg` 4 · `primary` 3 · `fill` · `accent.bg` 7 등으로 훨씬 많다. 도입하면 semantic 이 크게 늘고 AA 검사 대상도 함께 는다. Primer 방식은 역할×상태 조합만큼 토큰이 폭증한다. **우리 방식은 토큰 6개로 끝난다** — 지금 규모에는 이게 맞다.

**남는 약점 둘.** 기록해 두고 컴포넌트 단계에서 본다.

1. **자동이 아니다.** 토큰은 `darken`·`lighten` 을 둘 다 주고 **컴포넌트 작성자가 고른다.** 잘못 고르는 것을 막는 검사가 없다. 미결 17(컴포넌트 토큰 계층)에서 `control-primary-overlay` 같은 의도 이름으로 고정하면 닫힌다.
2. **무채색이다.** M3 는 `on-` 색이라 색조를 따라가지만 우리는 `gray.0`/`gray.1000` 이라 유채색 면 위에서도 회색이 얹힌다. 지금은 `accent.bg` 가 장식용이라(0-22) 문제가 없으나 **유채색 버튼이 생기면 재검토 대상**이다.

---

### 0-15. `gradient` — 절반이 중복이었다

2026-09-01. `interaction` 을 마치고 `gradient` 를 점검했다.

#### `solid` 과 `multiple` 은 같은 그라디언트였다

문자열이 달라 다른 토큰으로 보였지만, **방향과 stop 순서를 동시에 뒤집으면 같은 그림**이다.

```
solid.top       linear-gradient(to top,    transparent, currentColor)   위가 불투명
multiple.bottom linear-gradient(to bottom, currentColor, transparent)   위가 불투명   ← 같다
```

흰 바탕에 합성해 불투명하게 만든 뒤 `mix-blend-mode: difference` 로 겹쳐 확인했다. **네 쌍 모두 완전히 검게** 나왔다 — 픽셀 차이 0.

| | |
|---|---|
| `solid.top` | ≡ `multiple.bottom` |
| `solid.right` | ≡ `multiple.left` |
| `solid.bottom` | ≡ `multiple.top` |
| `solid.left` | ≡ `multiple.right` |

`ratio` 에서 `vertical` 4개가 `horizontal` 안에 그대로 또 있던 것과 같은 유형이다(0-13). 이름이 왜 안 맞았는지도 이걸로 설명된다 — `multiple` 은 여러 개가 아니라 **반대 방향 표기**였다.

**첫 실측은 틀렸다.** 반투명 레이어끼리 `difference` 로 겹쳤더니 검게 나오지 않아 "다르다"고 볼 뻔했다. 알파가 있는 레이어에는 difference 가 등가 검사로 성립하지 않는다 — 불투명하게 만든 뒤 비교해야 한다.

#### 이름 — `solid` → `fade`

`solid` 는 단색을 뜻하는데 단색이 아니다. 하는 일이 가장자리 페이드이므로 `fade` 로 바꿨다.

키 규약은 `solid` 쪽을 그대로 이어받았다 — **키 = 불투명한 가장자리 = 이 페이드를 놓는 자리**이며, CSS `to <방향>` 키워드와 일치한다. 값의 의미는 바뀌지 않았다.

#### `currentColor` 는 유지한다 — 예외지만 의도다

스펙 2025.10 에는 `gradient` 복합 타입(9.7 · `stops` + `angle`)이 있다. 우리 값 세 요소를 대조하면 `transparent` 는 alpha 0 인 color 로, 방향 4종은 `angle` 로 표현된다. **막는 것은 `currentColor` 하나뿐**이다.

그럼에도 유지한다. 쓰는 쪽이 `color` 에 면 색을 넣으면 **토큰 하나가 어느 면에서든 맞기** 때문이다. 특정 색을 박으면 그 면에서만 맞다.

```css
.card        { color: var(--color-bg-elevated); }
.card::after { background-image: var(--gradient-fade-bottom); }
```

`mask-image` 로 쓰면 알파만 읽혀 `currentColor` 가 무시된다. 실측했다 — 같은 마스크를 `currentColor`·`black`·`white` 로 만든 렌더 결과가 동일했다(`mask-mode: match-source` 는 그라디언트를 알파로 읽는다). 즉 **마스크 용도라면 예외를 없앨 수 있지만, 배경 페이드 용도에서는 `currentColor` 가 핵심 장치**다. 후자를 택했다.

#### 용도 정리 — 실무에 필요한 셋 중 하나만 갖고 있다

| | 용도 | 색 | 테마 | 현재 |
|---|---|---|---|---|
| A | **미디어 스크림** — 사진 위 어두운 층으로 글자 대비 확보 | 고정 검정 | 무관 | **없음** |
| B | **면 페이드** — 잘리는 콘텐츠를 면 색으로 덮음 | `currentColor` | 적응 | `gradient.fade` |
| C | **마스크 페이드** — 배경을 모를 때 가장자리를 녹임 | 무의미(알파만) | 무관 | 없음 (B 를 `mask-image` 에 써도 동작) |

A 는 접근성 요구가 붙는다. 사진은 픽셀마다 밝기가 달라 **최악의 경우(완전히 흰 사진)** 를 기준으로 눌러야 한다. 흰 글자 4.5:1 을 보장하는 임계값을 계산했다.

```
검정 스크림 alpha   흰 사진 위 합성색   흰 글자 대비
  0.48              #858585            3.71   ✘
  0.52              #7a7a7a            4.27   ✘   ← elevation.dim(라이트)로도 부족
  0.60              #666666            5.74   ✔   ← opacity 눈금에서 처음 통과
정확한 임계 alpha 0.5347
```

**A 는 지금 만들지 않는다.** 미디어 컴포넌트가 없어 참조 0인 토큰만 늘어난다. 착수할 때 위 계산을 근거로 만든다(미결 16).

#### `maskSize` 는 값을 건드리지 않았다

`24 · 32 · 40 · 56 · 64` 에서 **48만 결번**이고 이유 기록이 없다(미결 13 유지). 티어 이름이 `xs`~`xl` 5개뿐이라 48을 넣으면 6단계가 되어 어휘가 깨지고, 값을 재배치하려면 원 의도를 알아야 한다. `$description` 만 썼다.

#### 결과

| | 전 | 후 |
|---|---|---|
| `gradient` 토큰 | 13 | **9** |
| 토큰별 `$description` | 0/13 | **9/9** |
| DTCG 예외 (소스) | 9 | **5** (gradient 4 + `radius.circle` 1) |
| CSS 변수 | `--gradient-solid-*` · `--gradient-multiple-*` | `--gradient-fade-*` |

참조 0건이라 색·대비에 영향 없다. 그룹 `$description` 이 잘못 가리키던 참조(2-6 → 0-10)도 함께 고쳤다.

---

### 0-16. semantic 계층 비대칭 — 결함이 아니었다

2026-09-01. 미결 2번을 처리했다. **전제가 틀렸다는 결론이 나왔다.**

#### 무엇이라고 적혀 있었나

> `color`(테마별 62) 와 `shadow`(테마별 5) 만 semantic 이 있다. `spacing`(15) · `radius`(9) · `ratio`(21) · `gradient`(13) 등은 primitive 만 있어 컴포넌트가 `--spacing-16` 을 직접 골라야 한다.

숫자부터 낡아 있었다 — 실제는 `color` 73 · `ratio` 12 · `gradient` 9 다. 하지만 문제는 숫자가 아니라 **"비대칭 = 결함"이라는 전제**였다.

#### semantic 은 무엇을 담는 그릇인가

`CLAUDE.md` 의 정의는 *"색 역할, **같은 키·다른 참조**"* 다. 즉 semantic 계층의 존재 이유는 **라이트/다크라는 축을 담는 것**이다. `shadow` 가 primitive 없이 semantic 에만 사는 것도 같은 이유다 — 테마별 색을 쓴다.

**`spacing` 에는 그 축이 없다.** 16px 는 라이트에서도 다크에서도 16px 다. 여기에 semantic 을 만들면 바꿔 끼울 것이 없는 **빈 간접층**이 된다. 대칭을 위한 대칭이다.

#### 다른 시스템 조사 — 두 번째 계층은 있지만 성격이 다르다

| 시스템 | 비색 축의 두 번째 계층 |
|---|---|
| GitHub Primer | **있음** — `--base-size-8`(원시) → `--stack-gap-normal` · `--control-medium-paddingInline` · `--borderRadius-medium` |
| Material 3 | **있음** — `md.ref.*` → `md.sys.shape.corner.{none,extra-small,…,full}` → `md.comp.*` |

둘 다 두 번째 계층이 있지만 **테마를 바꿔 끼우는 층이 아니다.** *의도에 이름을 붙이는 층*이다 — `stack-gap` 인지 `control-padding` 인지, `corner-medium` 인지. 그리고 그 이름은 대부분 **컴포넌트 문맥에서 나온다**(Primer 의 `control-*`, M3 의 `md.comp.*`).

우리 semantic 과 이름만 같고 기능이 다르다.

#### 진짜 결함은 따로 있다

미결 2의 마지막 문장 — *"컴포넌트가 `--spacing-16` 을 직접 골라야 한다"* — 은 사실이다. 다만 해법이 semantic 이 아니다.

```
색     primitive → semantic(테마 축) → 컴포넌트        ← 지금 맞다
비색   primitive ─────────────────→ 컴포넌트        ← 가운데가 비었다
                       ↑
             여기 들어갈 것은 semantic 이 아니라
             "의도 이름" 계층 = 대개 component 토큰
```

`spacing` 의 토큰별 `$description` 이 0/15 인 것도 같은 뿌리다. 미결 10 에 *"값 하나에 용도를 1:1 로 묶기 어려워 컴포넌트 단계로 미루기로 결정했다 — 추측으로 채우지 않는다"* 고 적혀 있다. **그 판단이 이미 답을 말하고 있었다.** 의도 이름은 컴포넌트를 봐야 나온다.

#### 결정

**비색 semantic 을 만들지 않는다.** 미결 2는 닫고, 실재하는 결함은 **미결 17(컴포넌트 토큰 계층 없음)** 로 옮겼다.

지금 만들지 않는 이유는 하나 더 있다 — 의도 이름을 컴포넌트 없이 지으면 추측이 된다. `gradient.maskSize` 와 `ratio` 가 정확히 그렇게 꼬였다(0-10 · 0-13). 같은 실수를 반복하지 않는다.

**남는 비대칭은 문서로만 해소한다.** primitive 그룹의 `$description` 이 "이 눈금을 언제 쓰는가"를 말하게 하면, semantic 없이도 컴포넌트가 고를 근거가 생긴다. `radius` 9/9 · `breakpoint` 5/5 · `layout` 17/17 은 이미 그렇고, 비어 있는 것은 `spacing` 0/15 · `opacity` 0/24 · `fontSize` 0/11 · `fontWeight` 0/4 · `safeArea` 0/4 · `fontFamily` 0/1 이다.

---

### 0-17. `opacity` 24개 — 용도를 지어내지 않고 참조처로 적었다

2026-09-01. 미결 14번을 처리했다. 마지막 남은 "토큰별 `$description` 0건" 그룹이었다.

#### 왜 비어 있었나

`opacity` 는 여러 파운데이션이 공유하는 스케일이다(Colors · Elevation · Interaction). 단계 하나에 용도 하나를 붙일 수 없어 계속 미뤄졌다. `spacing` 이 미뤄진 이유와 같다(미결 10).

#### 지어내는 대신 실측했다

24개 각각을 **누가 참조하는지** 전수 조사했다. `$value` 별칭과 `$extensions."net.infobank.ds.alpha"`(문자열·배열 모두)를 소스 4개에서 훑었다.

**참조 0인 단계는 없었다.** 사다리가 전부 살아 있다.

| 단계 | 참조 | |
|---|---:|---|
| `8` | **7** | 가장 많이 쓰인다 — 오버레이 2 · 면 3 · 그림자 2 |
| `16` | 7 | 비활성 글자 · 선 · 면 · 그림자 |
| `28` | 5 | 보조 글자 · 선 · 면 |
| `12` · `52` · `60` · `88` | 4 | |
| `7` · `9` · `18` · `20` · `30` · `32` · `36` · `42` · `48` · `80` | 1 | 한 곳 전용 |

`$description` 은 **그 참조처를 그대로 적었다.** "언제 쓰는가"를 상상해 쓰지 않았다 — 상상해 쓴 설명이 틀렸을 때가 `maskSize`·`ratio` 였다.

#### 소비자는 네 갈래다

```
상호작용 오버레이   4 ~ 18
그림자             4 ~ 88   (테마별로 쓰는 구간이 다르다)
선 · 면            8 ~ 60
글자               16 ~ 88
```

한 단계를 여러 갈래가 함께 쓰는 것은 정상이다. 값이 겹치는 것이 아니라 **같은 눈금을 참조**하는 것이다(0-5). 그룹 `$description` 에 이 구조와 "새 반투명 토큰은 이 눈금에서 고르고, 없으면 단계를 추가한 뒤 참조한다"를 적었다.

#### 테마가 사다리를 반으로 나눈다

조사에서 드러난 규칙성이다. 그림자는 **라이트가 4~24, 다크가 24~88** 을 쓴다. 다크 배경이 어두워 같은 강도를 내려면 훨씬 큰 알파가 필요하기 때문이다(2-6 · 미결 11). 선·면·글자도 같은 방향으로 갈린다.

`24` 는 그 경계에 걸쳐 있다 — 라이트 `shadow.xl` 과 다크 `shadow.xs` 가 같은 값을 쓴다.

#### 새 검사

`checks/` 에 넣지 않고 일회성으로 돌렸지만, 대조 규칙은 남긴다 — **설명이 "전용"이라고 말한 단계는 실제 참조군이 하나여야 한다.** 24건 전부 통과했다. 참조가 늘어 "전용"이 거짓이 되면 설명을 고쳐야 한다.

#### 결과

`opacity` 24/24 작성(이전 0/24). 값은 하나도 바뀌지 않았다 — 설명만 추가했다.

#### 같은 방법으로 20개를 더 채웠다

미결에 올라와 있지도 않던 네 그룹을 이어서 처리했다. 방법은 같다 — 참조를 전수 조사하고 그 결과를 적었다.

| 그룹 | | 조사에서 드러난 것 |
|---|---|---|
| `fontFamily` | 0/1 → **1/1** | 패밀리가 하나뿐이고 타이포 33개 전부가 참조한다. **대체 글꼴 축이 없다**는 뜻이다 |
| `fontWeight` | 0/4 → **4/4** | **역할군마다 기본 굵기가 다르다** — `title` 은 semibold 기본·bold 강조, `body`·`link` 는 regular 기본·semibold 강조, `label` 은 medium 기본·semibold 강조 |
| `fontSize` | 0/11 → **11/11** | compact 가 같은 눈금을 한 단계씩 내려 써서 **한 크기를 base 와 compact 가 함께 참조**한다. `32` 가 3곳으로 가장 많이 모인다 |
| `safeArea` | 0/4 → **4/4** | **눈금이 아니라 플랫폼 상수**다. 참조 0건이고 웹에서는 `env(safe-area-inset-*)` 를 쓴다 |

`safeArea` 에서 하나 더 나왔다 — **Android 값(36 · 14)의 근거가 없다.** iOS(44 · 34)는 노치 기기 통상값이지만 Android 는 기기·제스처 설정마다 달라 하나로 못 박기 어렵다. 지어내지 않고 "컴포넌트 단계에서 재확인"이라고 토큰에 적었다.

#### 숫자를 적었으면 검증한다

설명에 "7곳" 같은 참조 수를 적었으므로, **설명 속 숫자와 실제 참조 수를 대조하는 검사**를 돌렸다. 13건 전부 일치했다.

첫 실행에서 `opacity.8` 이 "7곳"인데 실제 2곳이라고 나왔는데, **검사기가 `$extensions` 의 알파 참조를 안 세고 있었다.** 설명이 아니라 검사기가 틀렸다. 알파 참조를 포함시키니 일치했다. 참조를 셀 때 `$value` 만 보면 안 된다 — 색·그림자 참조의 대부분이 `$extensions` 에 있다.

#### 남은 공백

| | | |
|---|---|---|
| `spacing` | 0/15 | **의도된 공백**(미결 10) — 값 하나에 용도를 1:1 로 묶을 수 없어 컴포넌트 단계로 미뤘다 |

#### 무채색 23개도 같은 방법으로 채웠다

커버리지를 전수로 세다가 `color` 가 78/101 인 것이 드러났다. `gray` 20개 전원과 `coolGray` 450·500·700, 그리고 `gray` 그룹 설명이 비어 있었다 — 유채색 13계열은 단계별 목표 휘도가 적혀 있는데(1-1) **무채색만 비어 있었다.**

참조를 전수 조사하니 구조가 나왔다.

| | |
|---|---|
| **라이트와 다크가 사다리의 양 끝을 나눠 쓴다** | 라이트는 위쪽(면)·아래쪽(글자)을, 다크는 그 반대를 쓴다. `875` 가 라이트의 반전 배경이자 다크의 기본 면인 것이 그 구조 때문이다 |
| **`1000` 이 최다(19곳)** | 두 테마 `static.black` · 밝은 면 오버레이 base · 라이트 최강 글자 · **다크 그림자 5단계 전원의 base** |
| **`925` 는 라이트 그림자 전용 base(11곳)** | 그림자의 base 색이 테마별로 다르다 — 라이트 `925`, 다크 `1000` |
| **`500` 이 유일하게 양 테마에서 같은 뜻** | 라이트 비활성 전경 · 다크 비활성 글자 |
| **`coolGray.500` 이 선·면 13곳의 단일 base** | `line.*` 4 + `fill.*` 3 이 두 테마 모두 이 색을 쓰고 차이는 알파로만 낸다 |

무채색은 **목표 휘도로 생성한 눈금이 아니라 역할에서 역산한 값**이라 간격이 고르지 않다(0·50·75·100·150·200 … 900·910·925·950·1000). 그룹 설명에 그 사실을 적었다 — 유채색과 같은 규칙을 기대하고 읽으면 어긋난다.

#### 숫자를 적었으면 검증한다 (2)

무채색 설명에는 참조 수와 대비까지 적었으므로 대조 검사를 확장했다 — **"N곳" 9건 · "N.NN:1" 2건 · "전용" 7건 전부 실측과 일치**.

여기서도 검사가 먼저 걸렸다. `gray.300`·`400` 의 **"다크 전용"** 을 검사기가 "사용처 하나"로 읽어 실패시켰다. 실제로는 "다크 테마에서만"이라는 뜻이었다 — **한 단어가 두 뜻으로 쓰이고 있었다.** 사람이 읽어도 헷갈리므로 문구를 `"다크에서만 쓰인다"` 로 바꿨다.

#### 결과

primitive 커버리지 **212/250 → 235/250**. 남은 15건은 `spacing` 하나뿐이고 그건 의도된 공백이다.

---

### 0-18. `ios.*` 와 `maskSize` 를 지웠다

2026-09-01. 미결 4번과 13번을 함께 처리했다. 출발은 *"DTCG 로 관리하니 iOS 토큰은 없어도 되지 않나"* 였는데, **그 근거는 약하다** — DTCG 는 형식이지 플랫폼 추상화가 아니다. 형식이 무엇이든 iOS 가 다른 값을 필요로 하면 토큰은 있어야 한다. 지울 이유는 따로 찾았고, 실제로 있었다.

#### `ios.*` 3개 — 셋의 사정이 달랐다

| 토큰 | 판정 |
|---|---|
| `ios.accentPrimary` | **완전 중복.** 라이트 `{color.blue.600}` · 다크 `{color.blue.400}` 로 `interaction.focus` · `accent.fg.blue` 와 값·알파·테마가 전부 같다 |
| `ios.glassBackground` | **미완성.** 라이트·다크가 base·alpha 가 동일해 테마 적응이 안 돼 있다 |
| `ios.fillSecondary` | 고유하지만 **용도 기록 없음** |

`glassBackground` 는 다크에서 실제로 깨져 있었다.

```
                    면 색      그 위 기본 글자 대비   면-배경 대비
라이트 glassBackground  #f9f9f9    17.18                1.05
다크   glassBackground  #9d9e9e     2.51  ✘             6.42
```

다크에서 **밝은 회색 패널**이 되고 그 위 기본 글자가 2.51:1 로 AA 미달이다. iOS 라이트 모드 값을 그대로 옮기고 다크를 만들지 않은 흔적이다. 대비 검사에서 빠져 있어(텍스트 역할군이 아니라) 지금까지 드러나지 않았다.

**구조적 이유가 더 크다.** 우리 semantic 의 축은 **테마 하나**다. `ios.` 는 **플랫폼이라는 두 번째 축**을 역할 이름 안에 접두사로 밀어 넣은 것이다. 진짜 플랫폼 대응이 필요하면 테마처럼 **modifier** 로 두어야지 이름 앞에 붙일 것이 아니다(0-9 의 sets/modifiers 개념).

참조 0 · 설명 0 · 하나는 중복 · 하나는 깨짐 · 축이 어긋남 — **셋 다 지웠다.** 필요해지면 그때 modifier 로 제대로 만든다.

#### `maskSize` 5개 — 새 눈금이 아니었다

```
spacing   1·2·4·6·8·10·12·14·16·20·24·32·40·48·56
maskSize  xs=24  sm=32  md=40  lg=56  xl=64
          있음   있음   있음   있음   64만 없음
```

**5개 중 4개가 `spacing` 에 이미 있다.** `CLAUDE.md` 는 *"타입이 같아도 눈금이 다르면 다른 그룹"* 이라고 하는데, 여기는 눈금이 다르지 않았다 — `spacing` 의 부분집합 + 64 였다.

**48 결번의 수수께끼(미결 13)도 이걸로 풀린다.** `spacing` 에는 48이 있다. 별도 눈금을 뜬 것이 아니라 필요한 값만 골라 담다가 하나를 빠뜨린 것으로 보인다.

남길 이점은 "이름이 용도를 말한다" 하나였는데, 그건 **미결 17(컴포넌트 토큰 계층)** 이 생기면 거기서 해결될 일이다 — Primer 의 `--control-medium-paddingInline` 처럼. 지금 primitive 에 중복 눈금을 두고 버틸 이유가 없다. **지우고 페이드 길이는 `spacing` 을 쓴다.**

`64` 는 잃는다. 참조 0이라 필요한지 알 수 없고, 실제로 필요해지면 그때 `spacing` 에 추가한다.

#### 부수 수정

- `build.mjs` 의 티어 가드 주석이 `gradient.maskSize` 를 현재형으로 가리키고 있었다 — "(지금은 제거된)"을 붙였다. 가드 자체는 티어 스케일을 동적으로 찾으므로 개수만 6 → 5 로 줄었고 `TIER_EXEMPT` 에도 없어 영향이 없다.
- `checks/surfaces.mjs` 주석의 `ios` 언급을 지웠다.

#### 뒤늦게 드러난 것 — 고아 색 2개와 죽은 설명 4곳

지운 직후 색 토큰에 iOS 흔적이 남았는지 전수 검사했더니 **설명 4곳이 사라진 이름을 가리키고 있었다.** 토큰만 지우고 설명을 안 고친 것이다.

그중 둘이 더 큰 문제였다 — `color.gray.75`(`#F5F5F5`) 와 `color.coolGray.450`(`#78787F`) 은 **지운 iOS 토큰이 유일한 소비자**였다. 참조 0인 고아 색이 됐고, 게다가 `gray.75` 는 `gray.50`(#F7F7F8)·`gray.100`(#F4F4F4) 사이에 끼어 세 색이 육안으로 구분되지 않았다. **둘 다 지웠다.**

`opacity.16`·`opacity.60` 의 설명은 사용처 목록에서 iOS 항목을 빼고 다시 썼다.

**검사가 또 하나 잡았다.** `opacity.16` 을 "5곳"으로 고쳤는데 대조 검사가 "4곳"이라고 실패시켰다. 원인은 **두 검사기의 세는 단위가 달랐던 것**이다 — `graychk` 는 테마 인스턴스(라이트·다크에 같은 이름이면 2), `deschk` 는 고유 경로(1). 설명이 "두 테마"라고 쓰는 방식과 맞는 쪽은 전자라, **`deschk` 를 테마 인스턴스 기준으로 통일**했다. 이후 14건 전부 일치.

#### 결과

| | 전 | 후 |
|---|---|---|
| primitive | 250 | **243** |
| semantic (테마당) | 73 | **70** |
| **전체 토큰** | 429 | **416** |
| `gradient` | 9 | **4** (`fade` 만) |
| primitive 색 | 101 | **99** |
| CSS 변수 | 226 | **214** |

DTCG 예외는 5건 그대로다(`gradient.fade` 4 + `radius.circle` 1). AA 192건 미달 0 — `ios.*` 는 텍스트 역할군이 아니어서 검사 대상이 아니었고, 지워도 결과가 같다.

---

### 0-19. 번들은 스펙으로 표현 못 하는 것만 해소한다

2026-09-01. 미결 15번을 처리했다.

#### 무엇이 문제였나

반투명색은 base 별칭 + `$extensions."net.infobank.ds.alpha"` 로 둔다(0-14). 소스에서는 이게 맞다 — 불투명도를 `opacity` 눈금 한 곳에서 관리하려면 참조가 필요하고, 스펙 `color` 의 `alpha` 는 0~1 숫자만 받아 참조를 담지 못한다.

그런데 **번들까지 그 표현을 그대로 복사**하고 있었다. 스펙은 `$extensions` 안의 참조 해석을 정의하지 않으므로, 제3자 도구는 `fill.normal` 을 8% 회색이 아니라 **`#70737B` 완전 불투명**으로 읽는다. 스펙이 확장을 *"값을 이해하는 데 필수적이지 않은 메타데이터"* 로 제한하라고 한 SHOULD 도 어긴다.

#### 원칙 — 전부 평탄화가 아니다

**번들은 배포용 산출물이라 거기서는 알파가 이미 숫자다.** 소스가 참조를 유지해야 하는 이유가 번들에는 적용되지 않는다.

다만 *"그러니 번들을 전부 값으로 평탄화하자"* 가 아니다.

```
스펙으로 표현 가능한 것(별칭)     →  그대로 둔다. 도구가 네이티브로 따라간다
스펙으로 표현 불가능한 것(알파)   →  리터럴 + native alpha 로 해소
```

별칭은 스펙이 지원하는 정상 표현이고, Figma 플러그인이 변수 참조를 만들 때 쓸 수 있는 정보다. 버릴 이유가 없다. **해소 대상은 "스펙 밖"인 것뿐**이다.

`$extensions` 는 번들에도 **남긴다.** 값이 이미 완전하므로 확장이 비로소 스펙이 말하는 "있으면 좋은 부가 정보"가 된다 — 이 색이 `coolGray.500` 의 `opacity.8` 이라는 출처를 잃지 않는다.

#### 구현에서 걸린 것 — 공유 참조

`mergeInto` 가 잎 토큰을 **복사가 아니라 참조로** 넘긴다. 번들 객체를 그대로 고치면 `SRC` 와 다음 테마 번들까지 오염된다. `structuredClone` 후 처리하도록 했고, 소스 4개가 무변경인지와 두 테마 알파가 서로 다른지(라이트 0.08 / 다크 0.22)를 확인했다.

#### 새 가드

`assertBundleAlphaMaterialized()` — 알파 확장을 들고 있으면서 값이 여전히 별칭인 토큰이 하나라도 남으면 실패시킨다. 색은 `$value.alpha` 가 숫자인지, 그림자는 **모든 레이어**의 `color.alpha` 가 숫자인지 본다. 테마당 25건(색 20 + 그림자 5)을 확인한다.

#### 검증 — 확장을 지우고 읽어봤다

주장이 *"확장을 전부 무시해도 값이 올바르다"* 이므로, 번들에서 `$extensions` 를 통째로 제거한 사본으로 시험했다.

| | |
|---|---|
| 알파 소실 | **0건** — 반투명 토큰 50건 전부 값 안에 alpha 를 갖는다 |
| `components` ↔ `hex` | **일치** — 도구가 어느 쪽을 써도 같은 색이 나온다 |

`audit.mjs` 는 고칠 필요가 없었다. **이미 `alpha` 를 0~1 숫자로, `hex` 를 6자리로 검사하고 있었다** — 스펙이 *"제공된 alpha 와 충돌하지 않도록 hex 는 6자리여야 한다"* 고 요구하기 때문이다.

#### 결과

| | 전 | 후 |
|---|---|---|
| 번들 별칭 | 279 | **249** |
| 번들 native alpha | 0 | **25건/테마** (색 20 + 그림자 5) |
| 제3자 도구가 잘못 읽는 토큰 | 50 | **0** |

소스 4개·`tokens.css`·`tokens.js` 는 무변경이다. DTCG 오류 0 · 예외 5(소스)/10(번들) · AA 192건 미달 0.

---

### 0-20. `DESIGN.md` — 값과 근거를 한 파일에

2026-09-01. 만들었다. 읽는 사람이 둘이기 때문이다 — **사람**은 왜 이 값인지와 금지사항을 찾고, **에이전트**는 front matter 에서 값을, 산문에서 제약을 읽는다.

```
tokens/*.json      값 · $description        (사람이 쓴다)
narrative/*.md     산문 · 근거 · 금지사항    (사람이 쓴다)
        ↓  npm run build:tokens
DESIGN.md          front matter + 본문      (생성물)
```

#### front matter 범위 — CSS 변수가 되는 것

*"semantic 만 넣는다"* 는 지침을 우리 구조에 맞게 옮겼다. **이름 접두사로 거를 수 없다** — 우리 번들에는 `semantic.` 접두사가 없고 최상위가 `color`·`spacing`·`shadow` … 로 평탄하다.

대신 더 정확한 기준이 있었다. **CSS 변수가 되는 것 = 컴포넌트가 실제로 쓰는 것.** primitive 팔레트 99개는 애초에 CSS 로 나가지 않으므로(테마 전환이 깨지니까) 자동으로 빠진다. 여기에 타이포 클래스 33개를 더한다.

| | |
|---|---|
| 색·그림자 | **70** (라이트·다크 두 값을 함께) |
| 그 외 | **144** (spacing · radius · layout …) |
| 타이포 클래스 | **33** (5속성을 풀어서) |
| 제외 | primitive 팔레트 99 |

`$type` 으로 분류하지 않았다 — `dimension` 이 9개 그룹에 흩어져 있어 `spacing` 으로 뭉뚱그리면 틀린다.

#### 미결 20(테마 축) 결정 — 두 값을 함께 낸다

front matter 는 색 하나에 값 하나가 자연스럽지만 **하나만 넣으면 나머지 테마가 문서에서 사라진다.** `{light, dark}` 로 함께 냈다. 파일을 둘로 나누면 산문이 복제되고, 라이트만 넣으면 다크가 없는 문서가 된다.

#### 의존성 없이 YAML 을 만든다

`package.json` 에 dependency 가 없다. 유지하려고 YAML 을 직접 찍는다. 우리 값은 문자열·숫자·평평한 맵뿐이라 최소 구현으로 충분하다.

**대신 왕복 검사를 넣었다** — 만든 YAML 을 되읽어 원본과 다르면 **쓰기 전에** 실패한다. `$description` 에 콜론·백틱·따옴표가 섞여 들어와 조용히 깨지는 것을 막는다.

#### 가드가 헛돌던 것을 시험으로 잡았다

왕복 검사를 넣고 **인용을 무력화한 판으로 시험했더니 통과했다.** 원인은 **내 파서가 실제 YAML 보다 관대했던 것** — `note: 주의: 값` 같은 줄을 첫 콜론에서 갈라 그대로 되읽으니 원본과 같아 보였다. 실제 YAML 파서는 그 줄을 거부한다.

파서가 실제 YAML 이 거부하는 것을 거부하도록 고쳤다 — 인용 없는 값에 `": "` · `" #"` 가 있거나 지시자로 시작하면 실패시킨다. 다시 시험하니 잡았다.

**가드는 넣는 것으로 끝나지 않는다. 깨뜨려 봐야 한다.**

#### 자동 반영

`npm run build:tokens` 가 `build.mjs` 와 `build-design-md.mjs` 를 함께 돌린다. CI 에 drift 검사를 추가했다 — `tokens.css`·`dist/` 에 이미 쓰던 방식이다.

시험했다 — `radius.8` 을 9px 로 바꾸니 `DESIGN.md` 가 따라왔고, 되돌리니 같이 돌아왔다. narrative 만 고쳐도 반영된다. 두 번 빌드해 **바이트 동일**을 확인했다(결정성).

#### 완벽하지 않다

산문 9개는 `DECISIONS.md` 와 토큰 설명에서 뽑아 처음 쓴 것이다. 컴포넌트가 생기면 금지사항이 늘고 구체적으로 바뀐다. **틀을 먼저 세우고 채워 나간다.**

---

### 0-21. 하드코딩 검사 — 문서는 설득하고 검사는 막는다

2026-09-01. `DESIGN.md` 를 만들고 나니 빈 곳이 드러났다. **소비자 코드의 하드코딩을 잡는 장치가 하나도 없었다** — 우리 검사 스크립트는 토큰 자신만 봤다. 에이전트가 `color: #161617` 이라고 써도 아무도 막지 않는다.

#### stylelint 대신 자체 스크립트

| | |
|---|---|
| stylelint | 표준·에디터 연동·제대로 된 CSS 파싱. 다만 **의존성이 생기고**(지금 0개) `.css` 만 본다 |
| 자체 스크립트 | 의존성 0 유지 · `.jsx`·`.vue` 까지 본다 · `checks/` 규약과 맞는다 |

컴포넌트가 JSX 일 가능성이 커서 후자를 골랐다. `.css` 에 대해 stylelint 를 나중에 더할 수는 있다.

#### 설계의 전부는 오탐 줄이기다

조사부터 했다. 소박한 정규식으로 훑으니 우리 뷰어 두 개에서 **대부분이 오탐**이었다 — 토큰 기계장치가 rgba 를 조립하는 JS, 토큰 값을 주입하는 템플릿, 문서에 일부러 넣은 나쁜 예시.

**시끄러운 린터는 꺼진다.** 그래서 좁혔다.

- **CSS 선언(`속성: 값`)만** 본다
- **속성 목록을 좁게** 잡는다 — 색 12종 · 타이포 5종 · 여백 12종. 아무 숫자나 잡지 않는다
- **템플릿 보간·문자열 결합·`var()`·주석·값이 함수인 것**은 검사하지 않는다

#### 시험이 두 번 잡았다

**① 처음엔 아무것도 못 잡았다.** 리포 전체를 돌려 0건이 나왔는데, 예외 파일에서도 0건이라 검출기가 도는지 확인이 안 됐다. **가짜 컴포넌트(픽스처)를 만들어 시험했더니** 9건을 정확히 잡고 정상 케이스 5종(`var()`·`0`·`50%`·`ds-allow`·주석)은 통과했다.

**② JSX 를 못 잡았다.** 값이 따옴표에 싸여 있고(`color: '#161617'`) 프로퍼티가 camelCase(`fontSize`)라 정규식이 걸러냈다. 따옴표 값과 camelCase 를 처리하도록 고쳤다.

**③ 오탐 1건.** `preview.html` 의 `lineHeight: t => ...` — CSS 속성과 이름이 같은 **JS 객체 키**였다. 값이 함수면 스타일 선언이 아니므로 걸러낸다.

이 셋을 고치고 나니 뷰어 두 개에 **예외가 필요 없어졌다.** `FILE_EXEMPT` 는 비워 두고, **쓸모없어진 예외를 실패시키는 검사**를 넣었다(`TIER_EXEMPT`·`RATIO_EXEMPT` 와 같은 규약).

#### 지금은 잡을 것이 없다

정직하게 적는다. **이 도구의 진짜 대상(컴포넌트·소비 앱)이 아직 없다.** 현재 리포에서는 0건이고, 픽스처로만 동작을 확인했다. 컴포넌트를 만들기 전에 두는 것이 순서라서 지금 만들었다.

#### 연결

`npm run lint:hardcode` 단독, `npm run check` 로 전부(빌드 → DTCG 소스·번들 → AA 대비 → 하드코딩). CI 에 단계를 추가했다. `narrative/09-consuming.md` 에 규칙을 적어 `DESIGN.md` 본문으로 나가게 했다 — 에이전트가 읽는 곳에 있어야 한다.

---

### 0-22. `accent.bg` 는 장식용 · status 는 다크만 밝혔다

2026-09-01. 미결 1을 닫고 status 를 조정했다. 둘 다 색 변경이라 대비를 재검증했다.

#### `accent.bg` — 글자가 안 올라간다

용도가 **장식용 면**(카테고리 구분·차트 조각)으로 확정됐다. "흰 글자가 올라간다"는 가정으로 600 을 잡았던 근거가 사라졌다.

단계 선택에 실제 기준이 있었다 — **이 그룹은 라이트·다크가 같은 값**이라 한쪽에 맞추면 반대쪽이 어긋난다.

| 단계 | 라이트 면대비 | 다크 면대비 | 두 테마 차이 |
|---|---|---|---|
| 300 | 1.99~2.02 | 7.43~7.54 | 5.50 |
| 400 | 2.96~3.00 | 5.00~5.06 | 2.04 |
| **500** | **3.89~3.96** | **3.78~3.86** | **0.11** |
| 600 (이전) | 4.73~4.79 | 3.13~3.17 | 1.61 |

**500 이 압도적으로 고르다.** 취향이 아니라 측정으로 골랐다. 면끼리의 대비는 규정 요건이 아니므로(4-1) 4.5:1 을 요구하지 않는다.

#### status — 라이트에서는 밝힐 수 없었다

"조금 밝아졌으면" 이라는 요청이었는데, 재보니 **테마마다 답이 달랐다.**

```
라이트  600 #137F2C 4.78✔  →  500 3.91✘  →  400 3.02✘
다크    400 #1CA53B 4.97✔  →  300 7.44✔  →  200 10.31✔
```

**라이트 600 은 이미 하한에 붙어 있다.** 4.77~4.78 로 4.5 까지 여유가 0.3 도 없고, 한 단계만 밝혀도 미달이다. status 는 텍스트로 쓰이므로 4.5:1 을 피할 수 없다.

다크는 반대로 여유가 넉넉해 **400 → 300** 으로 올렸다. 4.97 → 7.44 가 됐다.

**텍스트용/비텍스트용을 나누는 안은 버렸다.** 비텍스트 3:1 이면 라이트도 500(3.91)까지 갈 수 있지만, 토큰이 3 → 6 으로 늘고 컴포넌트가 매번 둘 중 하나를 고르게 된다. 밝히려는 목적에 비해 대가가 크다.

결과적으로 **라이트 600 · 다크 300 의 비대칭**이 남았다. 이유를 그룹 `$description` 에 적었다 — 근거 없이 어긋나 보이면 다음 사람이 "맞춰야 하나" 하고 건드린다.

#### 부수 — 설명을 새로 썼다

두 그룹 다 `$description` 이 0건이었다. 이제 용도가 정해졌으므로 그룹 2건 + 토큰 10건을 썼다. `accent.bg` 에는 **"글자를 올리지 않는다"** 를 못 박았다 — 그게 이 값의 전제다.

#### 검증

| | |
|---|---|
| AA 대비 | **192건 미달 0** |
| 다크 status | 4.97~5.03 → **7.44~7.46** |
| 고아 색 | 56 → **46** (새로 생긴 것 없음. `green.300`·`red.300`·`orange.300`·`*.500` 7종이 쓰이기 시작) |

---

### 0-23. `coolGray` 를 `gray` 로 합치고 `cool`·`atomic` 을 지웠다

2026-09-01. 미결 5를 닫았다. 출발은 *"gray 들 사이에 coolGray 가 있는 게 이상하다"* 였고, 재보니 **관찰이 맞았다.**

#### `coolGray` 는 이름만큼 다른 색이 아니었다

```
gray.500      L 0.6889  C 0.009412  H 264.5
gray.600      L 0.4715  C 0.009950  H 271.2
coolGray.500  L 0.5556  C 0.012857  H 269.4
```

**`gray` 자체가 순수 무채색이 아니다.** 전 단계가 약한 파란 기운(C 0.001~0.010, H 247~286)을 갖는다. `coolGray.500` 의 chroma 0.0129 는 `gray.600` 의 0.00995 와 큰 차이가 없다 — **같은 색 축 위에 있고**, 다른 것은 밝기뿐이었다.

그리고 그 밝기는 **gray 스케일의 빈 자리**였다.

```
gray.400  휘도 0.4327
gray.500  휘도 0.3269   ┐ 여기가 비어 있었고
coolGray.500     0.1714 │ 그 자리를 별개 계열이 채우고 있었다
gray.600  휘도 0.1047   ┘
```

별개 계열이 아니라 **빠진 단계**였다.

#### `gray.550` 신설 — 값은 스케일 추세에 맞췄다

| | hex | 지금과 ΔE | |
|---|---|---|---|
| ① 값 그대로 옮김 | `#70737B` | 0.0000 | chroma 0.0129 가 추세(0.0097)보다 높아 이 단계만 살짝 파랗다 |
| **② 스케일 추세에 맞춤** | **`#717379`** | **0.0033** | chroma 를 `gray.500`↔`600` 밝기 보간(0.009742)으로 |
| ③ 순수 무채색 | `#737373` | 0.0129 | **gray 전체가 파란 기운인데 혼자 무채색이 된다** |

②를 골랐다. ①이 더 안전하지만 "왜 이 단계만 살짝 파란가"가 영구히 남는다. ③은 계열의 색조 일관성을 깬다.

`line.*` 4종 + `fill.*` 3종, 두 테마 **13건**이 이 색을 참조한다.

#### 합성 결과를 실측했다

이 색은 전부 알파 합성돼 쓰이므로, 원시 색의 ΔE 가 아니라 **합성 결과**가 중요하다.

```
line·fill 합성색 80건 (4개 면 × 두 테마)  최대 ΔE 0.0024
```

예측 상한(0.006)보다도 작았다. **시각적으로 같다.**

#### `cool.neutral30` — "텍스트 계열"이라는 내 설명이 근거 없었다

이 토큰의 제거 이유로 *"두 테마가 같은 색을 쓰는 유일한 텍스트 계열"* 이라고 적었었는데, 검증하니 **순환 논리였다.** 대비 검사에서 제외돼 있기 때문에 "검사되는 텍스트 중 테마 무관이 없다"가 나온 것이다.

**`cool` 이 텍스트라는 근거는 어디에도 없었다** — 이름은 밝기 좌표(`neutral30`)이고, `$description` 이 없고, 참조가 0건이라 코드로도 알 수 없다. `label.neutral` 과 이름이 비슷해 그렇게 적었던 것으로 보인다. 정정한다.

그것을 빼고도 이유가 셋 남았다.

| | |
|---|---|
| 이름이 값과 어긋남 | 키는 `neutral30` 인데 값은 `coolGray.700` — **30 이 아니라 700** |
| 다크에서 1.70:1 | 텍스트든 선이든 **3:1 도 못 넘는다.** 어두운 면 위 어두운 요소다 |
| 역할 불명 | 설명 0 · 참조 0 |

참고로 테마 무관 semantic 색은 16개이고(오버레이 6 · `accent.bg` 7 · `static` 2 · `cool` 1) 나머지 15개는 그게 **의도**다. `cool` 만 의도가 기록되지 않았다.

#### `atomic.*` 3종 — `status` 의 중복

라이트에서 `status.*` · `accent.fg.*` 와 값이 완전히 같았다. 이름이 팔레트식(`green60` = 색 + 단계)이라 **역할을 말하지 않는다** — semantic 계층에 있으면서 primitive 처럼 이름 붙은 것이다.

**0-22 에서 다크 status 를 400 → 300 으로 올렸을 때 `atomic` 이 따라오지 않아 다크에서 갈라졌다.** 내가 만든 불일치인데, 따라와야 하는지 판단할 근거 자체가 없다는 것이 이 토큰의 진짜 문제였다. 함께 지워 해소했다.

#### 결과

| | 전 | 후 |
|---|---|---|
| primitive 색 | 99 | **98** (`coolGray` 2 제거 · `gray.550` 1 추가) |
| semantic (테마당) | 70 | **66** (`cool` 1 + `atomic` 3) |
| **전체 토큰** | 416 | **407** |
| 색 계열 | gray · coolGray + 유채 13 | **gray + 유채 13** |
| AA 검사 건수 | 192 | **168** (`atomic` 3 × 4면 × 2테마 = 24 감소) |

AA 미달 0 · 고아 색 46 유지(증감 없음) · 합성색 최대 ΔE 0.0024 · 재빌드 바이트 동일.

`checks/surfaces.mjs` 의 `TEXT_GROUPS` 에서 `atomic` 을 빼고 `cool` 제외 주석을 갱신했다.

---

### 0-24. 카드 경계는 보더가 만든다 — 미결 11 의 전제가 틀렸다

2026-09-01. 미결 11을 처리하려다 **기록된 사실 셋이 측정과 다른 것**을 발견했다.

#### "다크 그림자는 라이트의 67% 가 최대" — 71~87% 다

그림자가 페이지에 만드는 실제 어둠을 두 층 합성 후 OKLab L 로 쟀다.

| 단계 | 라이트 | 다크 | 비율 |
|---|---|---|---|
| `xs` | 0.0842 | 0.0719 | **85%** |
| `sm` | 0.1147 | 0.0993 | **87%** |
| `md` | 0.1517 | 0.1235 | 81% |
| `lg` | 0.1828 | 0.1368 | 75% |
| `xl` | 0.2171 | 0.1542 | 71% |

**작은 단계일수록 차이가 없다.** 카드에 흔히 쓰는 `sm` 에서 87% 다. 다크 그림자 알파는 이미 0.88 이고 결과 ΔL 0.0993 은 우리가 "보인다"고 인정한 기준(hover 오버레이 0.0388~0.0465)의 **2.5배**다.

#### "다크가 근본 한계" — 총합은 다크가 더 강하다

```
라이트   면 차이 0.0000 + 그림자(sm) 0.1147 = 0.1147
다크     면 차이 0.0275 + 그림자(sm) 0.0993 = 0.1269
```

**라이트는 `bg.elevated` 가 `bg.normal` 과 완전히 같아서**(둘 다 `#FFFFFF`) 그림자 단독이다. 흰색 위로 갈 곳이 없으니 라이트가 더 막혀 있다. 다크는 면 차이라도 있다.

#### 진짜 부족한 곳은 카드 **윗변**이고, 두 테마가 같다

그림자 두 층은 역할이 다르다. 1층은 아래로 떨어지고, 2층은 사방을 감싼다. **카드 윗변에는 2층만 온다.**

| 단계 | 1층(아래) | 2층(사방) |
|---|---|---|
| 라이트 `sm` | 0.0842 | **0.0358** ✘ |
| 다크 `sm` | 0.0870 | **0.0354** ✘ |

**두 테마 모두 보임 기준(0.04) 아래다.** 아랫변은 뚜렷한데 윗변이 흐릿하다. 그리고 이건 **다크만의 문제가 아니다.**

#### 결정 — 카드에 보더를 쓴다. 두 테마 모두

```css
.card {
  background: var(--color-bg-elevated);
  border: var(--divider-1) solid var(--color-line-normal);
  box-shadow: var(--shadow-sm);
}
```

**토큰을 바꾸지 않는다.** 4-1 이 이미 적어둔 방향이다 — *"면은 지각 힌트, 경계는 보더."*

**보강(2026-09-02) — 이 결정에는 규정 근거가 있다.** 당시에는 4-1 을 따라 *"규정 준수가 아니라 실무적 선택"* 이라고 적었으나, **KWCAG 5.4.4(콘텐츠 간의 구분)** 가 정확히 이것을 요구한다 — *"이웃한 콘텐츠는 시각적으로 구분될 수 있도록 제공해야 한다."* 2026-08-24 조사에서 이 항목을 빠뜨렸던 것이다(4-1 정정). **수치 기준이 없고** 준수 방법 첫 번째가 "테두리를 이용하여 구분"이라 **여기서 내린 결정이 그대로 5.4.4 를 만족한다.** 값은 바뀌지 않고 근거만 올라간다.

#### 버린 안 둘

**A. 다크 `bg.elevated` 를 `gray.800` 으로 올린다.** ΔL +0.0275 → +0.0839 로 확실히 떠 보이지만, 연쇄가 붙는다 — `bg.elevatedAlternative` 도 같이 올려야 쌍 간격이 맞고(0.0618 → 0.0564), `interaction.disable`·`line.solidAlternative` 가 면과 같은 색이 된다. 무엇보다 **부족한 곳이 윗변인데 면 밝기로는 윗변이 메워지지 않는다.**

**B(다크에만 보더).** 처음 제안했다가 *"라이트에서는 보더가 안 보이고 다크에서는 보이나"* 라는 반문에 재보니 **보더는 양쪽에서 거의 같다**(카드 위 대비 라이트 1.32 · 다크 1.44). 다크만 다는 근거가 없었고 컴포넌트 구조만 테마별로 갈렸다. 철회했다.

**"Material·Fluent 가 다크에만 보더를 쓴다"고 적었던 것도 근거가 없었다.** 확인해보니 M3 는 카드에 elevated·filled·outlined **변형**을 두고 고르게 하며, 다크 대응은 보더가 아니라 **surface tone** 으로 한다. 검증 없이 쓴 문장이라 철회한다.

#### 남는 것

라이트에서 `bg.elevated` 와 `bg.normal` 이 같은 색인 것은 그대로 둔다. 흰 배경 시스템의 흔한 선택이고, 보더가 경계를 만들면 문제가 되지 않는다. 바꾸려면 **페이지를 회색으로 내리는 큰 결정**(회색 페이지·흰 카드)이 필요한데, 그건 토큰 조정이 아니라 제품 인상이 바뀌는 일이라 실제 화면을 보고 정한다.

---

### 0-25. 10px·11px 을 `label-xs` 로 들인다 — 미결 7 을 닫고 미결 3 의 규칙을 바꾼다

2026-09-02. *"10px·11px 을 추가하고 compact 에도 배치하고 싶다"* 는 요청으로 시작했다. 조사해 보니 **빌드 제약이 설계를 거의 결정했고**, 그 과정에서 미결 3(`compact` 범위)의 답도 함께 바뀌었다.

#### 미결 7 에 적혀 있던 근거는 무효였다

미결 7 은 *"Bottom Navigation 아이콘 라벨·Data Visual 축 레이블에 10px 이 필요하다"* 고 적고 있었다. 둘 다 검증되지 않는다.

- **Bottom Navigation 라벨은 이미 `label-sm`(12px)에 배정돼 있다** — `typography.json` 의 `label-sm` `$description` 이 그렇게 적고 있다.
- **Data Visual 은 계획된 컴포넌트 29종에 없다.** 그리고 이 매핑 자체가 추정이라고 미결 8 에 적혀 있다.

거기에 `narrative/04-typography.md` 는 정반대로 *"10px 이하를 만들지 마라"* 를 금지 항목에 올려두고 있었다. **미결과 산문이 정면으로 충돌한 상태로 방치돼 있었다.**

그래서 이 결정의 근거는 미결 7 의 원문이 아니라 **밀집 UI 크롬이라는 용도**로 다시 세웠다 — 차트 축 레이블, 타임스탬프, 배지, 조밀 테이블, 아이콘과 짝지어진 탭바 라벨.

#### 빌드가 설계를 좁혔다

프리미티브만 추가하는 선택지는 없다.

```
build.mjs:359 — letterSpacing.${k} 을 쓰는 typography 토큰이 없다 — 크기와 1:1 이어야 한다
```

**모든 자간 토큰은 실제 타이포 토큰이 소비해야 한다.** "스케일만 먼저 깔고 나중에 쓴다"가 불가능하다. 여기에 `TIER_ORDER = ['xs','sm','md','lg','xl']` 이라 base 에 새로 둘 이름은 `xs` 하나뿐이다. 두 제약을 곱하면 배치가 하나로 떨어진다 — **11px = base `label-xs`, 10px = 그 compact.**

#### 행간을 양쪽 14px 로 맞췄다

| | 글자 | 배수 | 라인박스 |
|---|---|---|---|
| `base.label-xs` | 11px | `11-14` = 1.272727 | **14px** |
| `compact.label-xs` | 10px | `10-14` = 1.4 | **14px** |

배수는 다르지만 결과 높이가 같다. **브레이크포인트를 넘을 때 탭바 높이가 흔들리지 않는다.** 대안이던 `11-16`(1.4545)은 전환에서 16→14 로 2px 레이아웃 시프트를 만든다.

2px 그리드와 배수 복원 검사는 둘 다 통과한다 — `1.272727 × 11 = 13.999997`(허용 오차 1e-4).

#### 10px 의 근거는 하나뿐이고, 그래서 규칙으로 못 박았다

| 기준 | 최소 | 10px |
|---|---|---|
| WCAG 2.2 · KWCAG 2.2 | **조항 없음** | 위반 아님 |
| Apple HIG | 11pt | 밑돈다 |
| Material 3 nav bar | 12sp | 밑돈다 |

두 플랫폼 권장선 아래다. 그럼에도 성립하는 이유는 **Apple 자신의 탭바 라벨이 10pt** 라는 사실이고, 그것이 가능한 조건은 **라벨이 아이콘과 짝지어져 텍스트가 유일한 전달 수단이 아니라는 것** 하나다.

그래서 조건을 규칙으로 고정했다 — **`label-xs` 는 아이콘·수치와 짝지어진 보조 정보에만 쓴다. 단독 전달 금지.** `body`·`link` 계열에는 만들지 않았다. 본문 최소는 `body-sm`(12px)으로 그대로 둔다.

rem 기반이라 사용자 확대(WCAG 1.4.4)에는 정상 반응한다. 색을 바꾸지 않았으므로 대비 재검증 대상이 아니다(168건 미달 0 유지).

#### 미결 3 의 답이 바뀐다 — 경계가 크기가 아니다

`label-xs` 가 들어가기 전 compact 는 **"24px 이상만"** 이라는 깨끗한 경계를 갖고 있었다. 이제 깨진다. 새 규칙은 크기가 아니라 조건이다.

> **compact 에 담는 기준 = 뷰포트에 따라 값이 달라져야 하는가.**
> `display`·`heading` 6종 → 넘침 방지. `label-xs` → 밀도. 나머지 21종(20px 이하) → 좁은 화면에서도 그대로.

**모바일에서 본문을 더 작게 만들지 않는다**는 방향은 유지한다. compact 는 축소 장치가 아니다. 이 규칙을 `typography` 그룹 `$description` 과 산문에 함께 적었다 — 미결 3 이 "의도인가 미완인가"로 남았던 이유가 **기록이 없어서**였으므로, 기록을 남기는 것이 처리의 본체다.

#### `label-xs-strong` 은 만들지 않았다

`label-sm` 에는 `-strong` 이 있어 계열이 비대칭이 된다. 나중에 미결 3 과 똑같은 질문("의도인가 미완인가")을 부를 수 있으므로 **`$description` 에 "수요가 확인되기 전까지 두지 않는다"를 명시**했다. 참조 0 토큰을 늘리지 않는 쪽을 택했다.

#### 남긴 한계 — 자간이 0 이다

작은 글자는 자간을 **넓히는** 것이 정석인데, 밴드(`display −0.02 · heading −0.015 · title −0.01 · none 0`)에 양수가 없다. 양수 밴드를 새로 만드는 것은 이번 범위 밖이라 `none` 파생 0 을 썼고, `letterSpacing.10`·`11` 의 `$description` 에 한계로 적었다. 실제 화면에서 뭉쳐 보이면 그때 밴드를 늘린다.

#### 결과

```
프리미티브 +6   fontSize.10 · 11 / lineHeight.10-14 · 11-14 / letterSpacing.10 · 11
타이포     +2   base.label-xs(11px) · compact.label-xs(10px)
토큰 합계  407 → 415
검증        DTCG 오류 0 · AA 168건 미달 0 · 하드코딩 0건
```

---

### 0-26. 타이포 속성은 5개가 전부다 — `fontFeature` 는 미결로 올리고 `fontVariation` 은 버린다

2026-09-02. *"보통 타이포 토큰은 7개(＋fontFeature ＋fontVariation)로 관리한다는데 우리는 빠진 것 아닌가"* 라는 질문에서 시작했다. **스펙 기준으로는 빠진 것이 없다.**

#### 스펙 전문을 받아 확인했다

DTCG 2025.10 §9.8 원문 —

> "The `$type` property MUST be set to the string `typography`. The value MUST be an object with the following properties: `fontFamily` · `fontSize` · `fontWeight` · `letterSpacing` · `lineHeight`"

**정확히 5개이고 전부 MUST 다.** 문서 356KB 전문을 내려받아 검색한 결과다.

```
fontFeature 0건 · fontVariation 0건 · fontStyle 0건 · textDecoration 0건 · textCase 0건
```

7개짜리 목록은 스펙이 아니라 도구·업계에서 흔히 쓰는 분류다. 다음에 같은 질문이 나오면 이 문단을 본다 — 스펙을 다시 받을 필요가 없다.

#### 그래도 5개로는 부족하다는 관찰 자체는 맞다

우리는 이미 6번째를 쓰고 있다. `link` 3종의 밑줄이 `$extensions."net.infobank.ds.decoration"` 에 있다. 스펙 5개에 없어서 확장으로 뺀 것이다.

다만 이것은 스펙 권고를 조금 늘린 것이다 — 스펙은 확장을 *"토큰의 값을 이해하는 데 필수적이지 않은 선택적 메타데이터"* 로 제한하는데, 링크의 밑줄은 필수에 가깝다. **전례로 삼되 무한정 늘리지 않는다.**

#### `fontVariation` 은 만들지 않는다

우리 글꼴이 `Pretendard JP Variable` 이라 관련 있어 보이지만 **`font-weight: 500` 이 이미 `wght` 축을 구동한다** — 브라우저가 자동으로 매핑한다. `font-variation-settings` 가 필요한 것은 `wght` 가 아닌 축(`opsz`·`wdth`·`GRAD`)을 쓸 때뿐이다.

Pretendard JP Variable 이 `wght` 외 축을 갖는지는 **확인하지 못했다** — 공식 문서에 축 목록이 없고 폰트 파일도 저장소에 없다. 다만 축이 있더라도 `font-variation-settings` 는 `font-weight` 를 덮어쓰는 함정이 있어 도입 이유가 없다. **추정으로 토큰을 만들지 않는다.**

#### `fontFeature` 는 실재하는 결함이라 미결 21 로 올린다

핵심은 `tnum`(tabular figures) 하나다. 숫자 폭을 고정해 값이 갱신될 때 흔들리지 않게 한다. 필요한 곳이 **0-25 에서 `label-xs` 의 용도로 적어둔 것과 정확히 겹친다** — 차트 축 레이블, 타임스탬프, 조밀 테이블, 배지.

**다만 `decoration` 전례를 그대로 쓰면 안 된다.**

| | 성격 |
|---|---|
| 밑줄 | **`link` 스타일에 내재**한다. 밑줄 없는 link 는 link 가 아니다 |
| `tnum` | **스타일과 직교**한다. 같은 `label-xs` 가 차트 축(필요)에도 텍스트 라벨(불필요)에도 쓰인다 |

`label-xs` 에 박으면 숫자가 아닌 라벨까지 강제된다. 타이포 토큰의 하위 속성이 아니라 **별도 축**이며, 조합 가능한 유틸리티(`class="label-xs tnum"`)가 맞는 형태다.

**지금 만들지 않는다.** `cssRecipe` 예외가 1건 늘고(DTCG 에 타입 없음) 참조 0 토큰이 된다. `tnum` 이 필요한 컴포넌트(Chart·Table)가 아직 없어 어느 스타일에 붙일지도 추측이 된다 — 미결 16(미디어 스크림)을 미룬 것과 같은 판단이다.

---

### 0-27. 모션·쌓임 토큰을 신설한다 — 컴포넌트 착수 전 마지막 공백

2026-09-02. 컴포넌트 P0 목록을 검토하다 **모션 토큰과 쌓임 순서 토큰이 0개**임을 확인했다. 저장소 전체 검색에서 `duration` · `cubicBezier` · `transition` · `zIndex` 어느 것도 없었다.

그런데 P0 가 전부 요구한다 — Loading(스피너 회전) · Switch(thumb 전환) · Tooltip(페이드 + 쌓임 순서 전부) · `prefers-reduced-motion`(끌 대상이 있어야 한다).

#### 예외를 만들지 않았다 — 셋 다 스펙 안이다

처음에는 *"z-index 는 `cssRecipe` 예외가 된다"* 고 적었다. **틀렸다.** z-index 는 단위 없는 수라 DTCG `number` 가 그대로 덮는다(`lineHeight` · `interaction.opacity` 와 같다).

| 그룹 | 타입 | 스펙 |
|---|---|---|
| `duration` | `duration` | 8.5 — `{value, unit}`, 단위는 `ms`·`s` |
| `cubicBezier` | `cubicBezier` | 8.6 — `[P1x, P1y, P2x, P2y]`, x 는 [0,1] |
| `zIndex` | `number` | 8.7 |

**새 `cssRecipe` 예외 0건.** 예외는 5건 그대로다.

#### 곡선 값을 지어내지 않았다

`cubicBezier` 4개는 **CSS `<easing-function>` 키워드의 정의값을 그대로** 썼다 — `linear` `[0,0,1,1]` · `standard`=`ease-in-out` `[0.42,0,0.58,1]` · `enter`=`ease-out` `[0,0,0.58,1]` · `exit`=`ease-in` `[0.42,0,1,1]`. 출처가 명확하고 브라우저 기본 동작과 어긋나지 않는다. 이름은 곡선 모양이 아니라 **하는 일**로 지었다(`overlay.darken/lighten` 과 같은 규칙).

#### 파운데이션 — Motion 을 신설했다 (12번째)

두 번 뒤집었으므로 경위를 남긴다. **Motion 신설**을 먼저 제안했고 → 비용을 아끼려 **Interaction 편입**으로 정했다가 → **다시 Motion 신설**로 돌아왔다.

Interaction 편입의 이점은 파운데이션 수가 11 로 유지되고 `CLAUDE.md` 수정이 사라진다는 것뿐이었다. 대가가 더 컸다 — **스피너 루프·토스트 자동 닫힘은 조작과 무관한데 Interaction 아래 있게 된다.** `duration` 그룹 `$description` 에 "조작 피드백 전용은 아니다" 라는 **변명을 달아야** 했고, 그 변명이 필요하다는 것 자체가 배정이 틀렸다는 신호였다.

Motion 은 어느 디자인 시스템에나 있는 독립 축이고, 실제 비용은 `FOUNDATIONS` 배열 한 줄 + `CLAUDE.md` 한 줄이었다. **이름이 내용을 속이지 않는 쪽을 택한다.** 편입안에 달았던 변명은 통째로 사라졌다.

`zIndex` 는 **Elevation** 이다 — `shadow` · `color.elevation.dim` 과 같은 z축 개념이라 자연스럽게 맞는다.

산문은 `narrative/07-interaction.md` 안에 그대로 두었다. 산문 파일은 파운데이션과 1:1 이 아니다(파일 9개 · 파운데이션 12개). 파일을 쪼개려면 `08`·`09` 를 밀어야 하는데 얻는 것이 없다.

#### `transition` 복합은 만들지 않았다

스펙 9.5 는 `duration` + `delay` + `timingFunction` 을 **셋 다 필수**로 요구한다(그래서 `duration.0` 이 필요하다 — 지연이 없어도 값을 넣어야 하고 원시값 인라인은 금지다).

만들려면 `transition.enter` 같은 **의도 이름**을 지어야 하는데 그게 미결 17 이 경고하는 지점이다 — *"이름이 컴포넌트 문맥에서 나오므로 먼저 지으면 추측이 된다."* `duration`·`cubicBezier` 는 **눈금**이라 지금 만들어도 추측이 아니다. 조합은 Button 을 만들면서 짓는다.

#### `prefers-reduced-motion` 을 토큰으로 끄지 않는다

빌드가 `@media (prefers-reduced-motion: reduce) { :root { --duration-*: 0ms } }` 를 낼 수도 있었다. **하지 않았다 — 스피너까지 멈춘다.** 로딩 표시는 진행 중이라는 정보를 전달하므로 reduced-motion 에서도 살려두거나 회전이 아닌 형태로 바꾸는 것이 접근성 관점의 정답이다. 일괄 0ms 는 틀린 처리다. 컴포넌트가 각자 판단하고, 규칙만 산문에 적었다.

#### 검사기도 함께 고쳤다

감사 스크립트가 두 새 타입을 검증하지 않고 *"검사기가 다루지 않는 타입"* 정보 11건으로 흘려보내고 있었다. 8.5·8.6 검증을 추가했다(단위 집합 · 배열 길이 · x 좌표 범위 · 별칭 타입 일치).

**검사기가 실제로 잡는지 일부러 깨뜨려 확인했다** — `cubicBezier` x 를 1.5 로, `duration` 단위를 `px` 로 바꾸니 감사 오류 2건, 빌드도 실패했다. 복원 후 오류 0. 검출기를 검증 없이 두지 않는다(0-21 에서 하드코딩 린터에 같은 절차를 밟았다).

#### 파운데이션 목록이 네 곳에 복제돼 있다 — 빌드가 못 잡는다

Motion 을 넣으며 드러났다. 파운데이션 정본 목록이 **네 곳**에 있다.

```
tokens/build.mjs      FOUNDATIONS            ← 빌드가 배정 누락·오타·미사용을 검사한다
tokens/guide.html     FOUNDATIONS
tokens/preview.html   FOUNDATIONS
tokens/preview.html   SECTIONS               ← 내비 항목 목록. 별개로 또 있다
```

뒤 셋은 **빌드가 검사하지 않는다.** `build.mjs` 에만 추가하면 감사·빌드는 전부 통과하는데 뷰어 내비에는 새 파운데이션이 안 보인다. 실제로 이번에 그 상태를 먼저 만들었고 브라우저로 확인해서야 발견했다.

`guide.html` 의 목록은 주석에 *"실제 배정은 데이터에서 읽고, 목록과 어긋나면 화면에 드러낸다"* 고 적힌 **의도된 교차 검증**이라 지우면 안 된다. 문제는 **어긋남이 화면에만 드러나고 CI 에는 안 드러난다**는 것이다.

**이번에는 네 곳을 손으로 맞췄다.** 자동 검사를 붙이는 것은 이번 범위 밖이라 **미결 22** 로 올린다.

#### 뷰어 둘이 새 타입을 몰랐다 — 세 번째 같은 사고

`CLAUDE.md` 가 경고하는 그대로였다. *"토큰 값의 형식을 바꾸면 JSON 을 직접 읽는 소비자 전부를 같이 고친다."*

- `guide.html` — 배열을 **fontFamily 아니면 shadow** 둘 중 하나로 가정해 `cubicBezier` 를 **"4개 레이어"** 로 표시했다. 값 모양만 보면 구분이 안 된다.
- `preview.html` — `$type` 으로 분기하는데 새 타입이 없어 `duration` 이 `[object Object]`, `cubicBezier` 가 원본 배열로 새어 나왔다.

둘 다 **`$type` 으로 먼저 가르도록** 고쳤다. 빌드가 잡아주지 못하는 부류이고 0-6 · 0-7 에 이어 세 번째다.

#### 곁들여 — 산문의 자기모순을 고쳤다

`narrative/07-interaction.md` 가 본문에서 *"오버레이 방향도 테마별로 바꿔야 한다"* 라 하고 "하지 말 것"에서 *"오버레이를 테마로 고르지 마라"* 라 하고 있었다. 사용자 문서의 오해가 여기서 나온 것으로 보인다. 규칙은 면 밝기이고, 뒤집히는 면에서는 **결과적으로** 테마별 선택자가 필요해진다는 것으로 다시 썼다(0-14 보강과 같은 내용).

#### 결과

```
프리미티브 +18   duration 7 · cubicBezier 4 · zIndex 7
그룹        22 → 25 · 파운데이션 11 → 12 (Motion 신설)
토큰 합계   415 → 433
CSS 변수    150 → 168
검증        DTCG 오류 0 · 예외 5 · 정보 0 / AA 168건 미달 0 / 하드코딩 0건
```

---

### 0-28. headless 를 쓰지 않는다 · 웹 구현체는 React 로 확정한다

2026-09-02. 컴포넌트 착수 직전의 선결 결정이다. 토큰 결정이 아니지만 **같은 질문이 반복될 것이 확실해서** 여기 남긴다.

#### 먼저 틀린 근거 하나를 걷어냈다

출발점은 *"멀티플랫폼을 고려하니 프레임워크 종속 headless 는 피하자"* 였다. **그 논리 자체는 성립하지 않는다.**

```
토큰 층      DTCG · 플랫폼 중립 · dist/*.json 으로 이미 나간다   ← iOS 가 쓰는 곳
웹 구현체 층  headless 를 쓰든 말든 여기서 끝난다                 ← iOS 와 만나지 않는다
```

Radix 를 썼어도 iOS 는 Swift 로 따로 구현한다 — **웹과 네이티브를 함께 덮는 headless 는 없다.** DTCG 를 강조해온 것과 headless 선택은 다른 층의 문제다.

#### 그런데 결론은 같다 — 진짜 근거는 "동작 명세"다

iOS 에서 같은 Switch 를 만들려면 역할·키보드/포커스 맵·상태 전이를 **어차피 문서로 써야 한다.** 그걸 Radix 에 맡기면 그 명세가 **서드파티 React 코드 안에 암묵적으로 갇히고** iOS 개발자는 읽을 수 없다.

> **토큰이 값의 단일 원본이라면, 동작 명세는 행동의 단일 원본이다.**

어차피 쓸 명세라면 한 번 써서 세 곳에 구현하는 쪽이 일관된다. 이것이 DTCG 정신의 확장이고, `COMPONENTS.md` **3-0(동작 명세)** 이 그 형식이다. **headless 를 안 쓰기로 한 대신 3-0 을 반드시 쓴다** — 둘은 짝이다.

**보조 근거 셋** — ① KR delta 5건 중 4건은 headless 가 안 해준다(`id` 중복만 해준다). ② 의존성 0개가 지금 자산이다. ③ P0 다수가 네이티브 HTML 로 거의 끝난다.

#### 대가를 눈 뜨고 받는다 — 비싼 5종

Tooltip(1.4.13 의 dismissible·hoverable·persistent 동시 충족) · Dropdown(roving tabindex + 위치 계산) · **Date Picker(가장 비쌈 — 키보드 그리드 + 로케일 + 범위)** · Slider(2.5.7 끌기 대안) · Bottom Sheet(포커스 트랩 + 스크롤 잠금). `COMPONENTS.md` 2절에 표로 남겼다.

플랫폼 API 가 덜어주지만 **조건부다** — Popover API 는 Baseline **newly available 2025-01**, CSS anchor positioning 은 **2026-01** 이다. 둘 다 *widely* available 이 아니라 최신 브라우저 전용이다. **그래서 지원 범위가 선결로 올라갔다**(⓪′) — 못 쓰면 5종의 비용이 다시 오른다.

#### 중간 지대는 열어둔다

headless **프레임워크 라이브러리**는 안 쓰되 **프레임워크 중립 단일 목적 유틸리티**는 후보로 남긴다. `@floating-ui/dom` 은 바닐라 JS 라 React 에 묶지 않는다. anchor positioning 이 지원 범위 밖이면 Dropdown·Tooltip 의 대안이다. **"의존성 0개"를 목적 자체로 삼지는 않는다.**

#### 웹 구현체는 React 다

배포 형태 셋(React 컴포넌트 · Web Components · CSS-only)을 놓고 **React 로 정했다.** 부수 효과로 KWCAG 8.1.1(id 중복 금지)이 `useId` 로 기계적으로 닫힌다 — 손으로 id 문자열을 짓지 않는다는 규약만 세우면 된다.

**토큰은 이 결정에 묶이지 않는다.** DTCG 산출물(`dist/*.json` · `tokens.css` · `tokens.js`)은 그대로이고, 미결 19(플랫폼 축)도 영향받지 않는다. 묶이는 것은 웹 구현체 층뿐이다.

#### 남은 선결

**지원 범위(브라우저 × 입력 방식)** 가 아직 미정이다. 위 플랫폼 API 사용 가부가 여기서 갈리므로, 이것이 정해져야 비싼 5종의 설계를 시작할 수 있다.

---

### 0-29. 모노레포로 옮긴다 — 토큰 패키지의 의존성 0 을 지키기 위해

2026-09-03. 컴포넌트 착수 직전에 구조를 정리했다. **배포 전이라 지금이 가장 싸다** — `private: true` 이고 소비자가 0 이다.

#### 왜 나누나 — 소비자가 다르다

DTCG 는 **패키지 구조를 정하지 않는다.** 형식 스펙일 뿐이다. 다만 실무 관행이 있고, 이유는 하나다 — **토큰은 웹·iOS·Android·Figma 가 다 쓰고 React 구현은 웹만 쓴다.** 한 패키지에 넣으면 iOS 팀이 React 의존성을 끌어온다.

확인한 사례 둘.

| | 구조 |
|---|---|
| **Primer**(GitHub) | `@primer/primitives`(`src/tokens/` + `dist/`)를 React·CSS 패키지가 의존한다. 다만 **순수 DTCG 는 아니다** — Style Dictionary 기반이고 `$extensions` 만 W3C 스펙에서 차용했다 |
| **Adobe Spectrum** | `packages/tokens` · **`component-schemas`**(구현 무관 컴포넌트 API 스키마) · `design-data` · `design-data-spec`. 웹·iOS·Android 를 덮기 때문에 **구현 독립 명세를 따로 패키지로 뺐다** |

**Adobe 의 `component-schemas` 가 우리 `COMPONENTS.md` 3-0(동작 명세)과 같은 자리다.** 0-28 에서 headless 를 버린 근거 — *"어차피 iOS 용으로 쓸 명세"* — 를 그쪽은 이미 패키지로 만들어 두고 있다.

#### 결과 구조

```
DS/                        @infobank/ds          npm workspaces (추가 의존성 없음)
├─ packages/tokens/        @infobank/ds-tokens   의존성 0 · 플랫폼 중립
│    *.json · build.mjs · checks/ · dist/ · tokens.css · tokens.js
│    narrative/ · DESIGN.md · package.json
├─ packages/react/         @infobank/ds-react    런타임 의존성은 여기에만
├─ CLAUDE.md · COMPONENTS.md · DECISIONS.md      루트(교차 관심사)
```

**핵심은 `ds-tokens` 가 의존성 0 을 유지하는 것이다.** `@floating-ui/dom` 을 도입하더라도 `ds-react` 에만 들어간다 — 0-28 에서 *"의존성 0개가 자산"* 이라 해놓고 의존성을 추가하자던 모순이 여기서 풀린다. **그 전제가 빠져 있었다.**

#### `spec` 패키지는 만들지 않았다

Adobe 처럼 `packages/spec` 를 둘 수도 있으나 **iOS 착수 전에는 소비자가 없다.** 3-0 명세를 `COMPONENTS.md` 안에 문서로 쓰다가 iOS 가 실제로 시작될 때 승격한다. 미결 16·17 을 미룬 것과 같은 판단이다.

#### `DECISIONS.md` 를 루트로 올렸다

`tokens/DECISIONS.md` 에 있었는데 **0-28(headless·React)은 토큰 결정이 아니다.** 이미 범위를 벗어나 있었다. 지금 옮기면 자연스럽고 나중에 옮기면 링크가 많아진다.

#### 가드가 한 번 걸렸다 — 약화시키지 않고 좁혔다

`build.mjs` 의 COMPOSITION 양방향 검사가 **`package.json` 을 미등록 토큰 소스로 보고 빌드를 세웠다.** 패키지 매니페스트가 토큰 소스와 같은 폴더에 오면서 생긴 일이다.

`package.json` **한 건만** 제외했다. 다른 `.json` 은 계속 빌드를 세운다 — 새 파일이 생기면 등록 여부를 의식적으로 정하게 하는 것이 이 가드의 목적이기 때문이다. 소스를 `src/` 로 내려 근본적으로 분리하는 안도 있었으나 경로 변경이 커져 이번 범위 밖으로 뒀다.

#### 검사기 두 개의 경로를 갈랐다

`hardcode.mjs` 는 **검사 범위(저장소 전체)와 눈금 출처(토큰 패키지)가 다르다.** `ROOT` 와 `PKG` 로 나눴다. `packages/react` 에 위반을 심어 **실제로 잡는지 확인했다** — 색 리터럴·스케일 밖 `13px` 2건을 잡았고 제거 후 0건으로 돌아왔다.

`build-design-md.mjs` 는 `narrative/`·`DESIGN.md`·`package.json` 을 전부 `DIR` 기준으로 바꿨다. `ROOT` 변수가 사라졌다.

#### 검증

브라우저에서 두 뷰어를 확인했다. `guide.html` 이 `../package.json` 을 읽고 있어 404 였다 — 같은 폴더로 내려왔으므로 `./package.json` 으로 고쳤고, 버전 태그 `v0.1.0` 이 정상 표시된다. `preview.html` 은 상대 경로뿐이라 그대로 동작한다.

```
산출물 drift   0 (tokens.css · tokens.js · dist 무변경)
DESIGN.md      package 이름 한 줄만 변경 (@infobank/next-ds-tokens → @infobank/ds-tokens)
DTCG           오류 0 · 예외 5 · 정보 0
AA             168건 미달 0
하드코딩        0건
```

---

### 0-30. 의도 이름 계층을 만든다 — 미결 17 을 닫고 Button 명세를 쓴다

2026-09-03. Button 착수의 두 갈래(동작 명세 · 컴포넌트 토큰 계층)를 함께 처리했다. 미결 17 이 *"이름이 컴포넌트 문맥에서 나오므로 먼저 지으면 추측이 된다"* 고 경고했으므로, **Button 이라는 실제 문맥을 놓고** 이름을 지었다.

#### 계산 하나가 미결 하나를 닫았다 — 24×24 가 6.0mm 를 포함한다

`COMPONENTS.md` §5 가 *"어떤 사이즈가 2.5.8(24 CSS px)과 KWCAG 6.0mm 를 모두 만족하는지"* 를 미정으로 두고 있었다. CSS 기준 픽셀(1px = 1/96 in = 0.2646mm)로 계산하면 답이 나온다.

| 정사각 | 대각 | KWCAG 6.0mm | WCAG 24px |
|---|---|---|---|
| 16×16 | 5.99mm | ✘ | ✘ |
| **24×24** | **8.98mm** | **✔** | **✔** |

6.0mm 대각을 만족하는 최소 정사각은 **16.04 CSS px** 다. **WCAG 2.5.8 이 KWCAG 6.1.3 을 완전히 포함한다** — 24 만 지키면 두 기준이 함께 닫히고 따로 잴 필요가 없다. 전제(CSS 기준 픽셀)는 명세에 조건으로 적었다.

#### 계층의 자리 — `typography.json` 이 선례다

`primitive.json` 에 넣으면 *"원시 값이 사는 유일한 곳 = primitive 리프"* 를 깬다. `semantic` 도 아니다 — **테마 축이 없다**(0-16). `typography.json` 이 이미 "복합·primitive 직접 참조·테마 없음" 자리에 있으므로 **`component.json` 을 같은 자리에** 두고 `COMPOSITION.sets.base` 에 등록했다.

#### 이름은 컴포넌트가 아니라 역할 단위로

```
✘  button.height.md      Text Field 가 같은 값을 또 정의하게 된다
✔  control.minHeight.md  Button · Text Field · Search · Select Button 이 공유
```

폼 한 줄에서 버튼과 입력의 높이가 맞아야 한다. Primer 가 `--control-*` 로 가는 이유다.

#### `height` 가 아니라 `minHeight` 다

WCAG **1.4.12(글자 간격)** 는 사용자가 행간·자간을 덮어써도 깨지지 않을 것을 요구한다. `height: 40px` 로 박으면 행간을 키운 사용자에게 글자가 잘린다 — `COMPONENTS.md` 3-4 가 *"고정 높이 버튼·칩에서 실패한다"* 고 적어둔 바로 그 지점이다.

**이름이 사용법을 강제하도록** `control.minHeight` 로 지었다. `control.height` 였다면 구현자가 `height` 로 쓴다.

#### 만든 것 — 14개, 새 원시 값 0개

```
control.minHeight      sm 32 · md 40 · lg 48   → {spacing.*}
control.paddingInline  sm 12 · md 16 · lg 20   → {spacing.*}
control.gap            sm  4 · md  6 · lg  8   → {spacing.*}
control.radius         sm  6 · md  8 · lg 12   → {radius.*}
control.minTarget      24                      → {spacing.24}
transition.control     100ms · standard · 0ms  → {duration.*} {cubicBezier.*}
```

**전부 기존 눈금의 별칭이다.** `sm=32` 도 24 를 넘어 가장 작은 단계가 자체로 조작영역 요건을 만족한다. 기본 단계는 `md`.

**색은 이 계층에 넣지 않았다.** variant 별 색은 이미 semantic 에 다 있고, 복제하면 테마 축이 두 곳으로 갈라진다. variant→색 매핑은 컴포넌트가 갖는다.

**`transition` 은 `control` 하나만 만들었다.** `enter`·`exit` 를 지금 넣으면 참조 0 이 된다 — 0-27 에서 미룬 이유 그대로다.

#### 파운데이션 — `Size` 를 신설했다 (13번째)

조사해보니 **"Control" 파운데이션을 가진 곳은 없다.** Primer 는 `functional/size/`(size · size-coarse · size-fine · radius · z-index)에 넣고 `--control-*` 은 **토큰 이름**으로만 쓴다. M3 는 `md.comp.<컴포넌트>.*` 로 컴포넌트별 네임스페이스를 쓴다.

우리 12개에 **`Size` 축이 없다는 것**이 진짜 공백이었다. `Spacing`(간격)·`Radius`(모서리)로 1:N 배정하는 안도 있었으나 **height 를 "Spacing" 이라 부르는 것은 정확하지 않다.** Motion 때와 같은 판단으로 신설했고, 앞으로 아이콘·아바타 치수가 갈 자리가 생겼다.

#### `disabled` 정책 — 두 상태를 둔다

처음에는 *"`aria-disabled` + 클릭 차단"* 을 권했다. **그건 두 방식의 나쁜 조합이다** — 포커스는 되는데 눌러도 아무 일이 없으면 사용자는 고장으로 읽는다. `aria-disabled` 의 존재 이유가 **활성화를 허용해 사유를 알리는 것**이다.

Primer 의 실제 지침이 명확했다.

| | 마크업 | 포커스 | 활성화 |
|---|---|---|---|
| **inactive**(기본) | `aria-disabled="true"` | ✔ | ✔ 눌리면 사유 안내 |
| **disabled**(예외) | 네이티브 `disabled` | ✘ | ✘ *"rare cases … should generally be avoided"* |

**기본을 `inactive` 로 한다.** 키보드·스크린리더 사용자가 버튼의 존재를 알 수 있다.

**대가를 미결로 남겼다** — 네이티브 `disabled` 는 WCAG 1.4.3 의 *inactive component* 예외에 들지만 `aria-disabled` 는 여전히 조작 가능하므로 예외로 보기 어렵다. 우리 `label.disable` 이 대비 검사에서 빠져 있는데 **그 면제를 다시 봐야 한다.** Button 구현 때 실측한다(8-1 미결 ①).

#### 가드 셋이 걸렸다 — 전부 실제 결함이었다

**① 별칭이 CSS 로 그대로 새어 나갔다.** `--control-min-height-sm: {spacing.32};` 가 출력됐다. 원인은 내가 `$type` 을 빠뜨린 것이다 — `valueOf` 가 타입으로 분기하는데 타입이 없어 전 분기를 통과했다. **`$type` 은 필수라고 `CLAUDE.md` 에 적어놓고 어겼다.** 13개에 `dimension` 을 달아 해결했다.

**② 티어 순서 검사가 별칭을 통째로 건너뛰고 있었다.** `numOf` 가 `$value` 를 그대로 읽어 문자열이면 `null` 을 반환했다. **의도 이름 계층은 전부 별칭이라 이 검사가 무용지물이었다.** 별칭을 먼저 풀도록 고쳤고 검사 대상이 5개 → **9개**로 늘었다. `lg` 를 `sm` 보다 작게 만들어 실제로 잡는지 확인했다.

**③ 감사가 `component.json` 을 아예 읽지 않았다.** `audit.mjs` 의 `FILES` 가 하드코딩 목록이었다. **미결 22 와 같은 부류다.** 하드코딩을 지우고 **디스크에서 읽도록** 바꿨다 — 앞으로 새 소스가 감사에서 조용히 빠지지 않는다.

또한 `transition` 복합 검사(스펙 9.5)가 없어 추가했다. `delay` 를 지우고 `timingFunction` 타입을 어긋나게 해 **감사 2건·빌드 1건이 실제로 잡는 것**을 확인했다.

#### 뷰어 — 네 곳을 또 손으로 맞췄다

`guide.html`·`preview.html` 의 `FOUNDATIONS`·`SECTIONS`·소스 파일 목록. **미결 22 가 예고한 그대로다.** `preview.html` 은 파운데이션 섹션을 `prim` 트리에서만 뽑고 있어 `component.json` 그룹이 렌더되지 않았다 — 별칭 해석은 진짜 primitive 로 두고 **섹션 렌더만 두 트리를 합치도록** 고쳤다. 브라우저에서 `Size 13` · `Motion 12` 를 확인했다.

#### 남긴 관찰

뷰어가 의도 이름 계층의 값을 `{spacing.32}` 로 보여준다. 참조 관계를 드러내는 것이라 틀리지 않지만 해석값(32px)이 함께 보이면 낫다. 지금은 `$description` 에 적어 가려지는데 **설명에 의존하는 것은 취약하다.** 전 별칭 토큰의 표시 방식을 바꾸는 일이라 이번 범위 밖으로 뒀다.

#### 결과

```
토큰       433 → 447   component.json 14 (control 13 · transition 1)
파운데이션  12 → 13     Size 신설
소스        4 → 5       COMPOSITION.sets.base 에 등록
CSS 변수   168 → 182
검사        티어 스케일 5 → 9 · 감사가 소스 목록을 디스크에서 읽음 · transition 9.5 검사 추가
검증        DTCG 오류 0 · 예외 5 · 정보 0 / AA 168건 미달 0 / 하드코딩 0건
```

**미결 17 을 닫는다.** `COMPONENTS.md` 8-1 에 Button 동작 명세를 썼다 — 27종의 첫 사례이고 형식의 본이 된다.

---

### 0-31. Button 을 만든다 — 그리고 inactive 대비 미달을 찾는다

2026-09-03. `COMPONENTS.md` 8-1 명세대로 `packages/react` 에 Button 을 구현했다. **명세가 예고한 미결 ① 이 실제 결함으로 확인됐다 — 아직 해결되지 않았다.**

#### 상태를 CSS 로 처리한다 — 취향이 아니라 요구다

참고한 기존 구현은 `useState` 로 hover·focus·press 를 관리했다. 두 가지가 깨진다.

1. **`onFocus` 는 마우스 클릭에도 발생한다.** 그래서 클릭만 해도 포커스 표시가 뜬다. `:focus-visible` 이 존재하는 이유가 이것이다.
2. **인라인 스타일로는 미디어쿼리를 쓸 수 없다.** `forced-colors` · `prefers-reduced-motion` · `hover: hover` 는 `COMPONENTS.md` 3-5 의 **요구사항**이다.

CSS 로 옮기니 `useState` 3개와 마우스 핸들러 6개가 통째로 사라졌다. **실측으로 확인했다** — 마우스 클릭 시 `:focus-visible` false · `outline-style: none`, Tab 시 true · `2px solid`, offset 2px.

#### 참고 구현에서 가져온 것

**로딩 시 본문을 `visibility: hidden` 으로 두고 스피너를 절대 중앙에 놓는다.** `display: none` 이면 버튼 폭이 흔들린다. 그 외 오버레이 별도 레이어, lookup 테이블, `iconOnly` 의 `label`→`aria-label` 재사용, 아이콘 `aria-hidden` 래퍼, `flex: none` 을 가져왔다.

**가져오지 않은 것** — `outline: none`(포커스 링 없음), `opacity: 0.32` 매직넘버, `color-mix` 런타임 합성(우리는 합성된 rgba 토큰이 있다), 하드코딩 `0.15s`, `spacing` 을 radius 로 쓰기.

#### 오버레이 방향을 variant 별로 실측했다 — 세 가지 거동이 나왔다

| variant | 라이트 | 다크 | |
|---|---|---|---|
| `primary` | lighten (L 0.028) | darken (L 0.931) | 뒤집힌다 |
| `secondary`·`outline`·`text` | darken (L 1.000) | lighten (L 0.011) | **반대로** 뒤집힌다 |
| `negative` | lighten (L 0.156) | **lighten** (L 0.437) | **안 뒤집힌다** |

**테마로 골랐다면 `negative` 가 다크에서 반대 방향이 된다.** 0-14 의 결론을 실물이 다시 확인했다. 구현은 variant 마다 `--ds-btn-ov-*` 변수만 바꾸고, 뒤집히는 면만 테마 선택자로 되돌린다. 브라우저에서 8조합 전부 목표와 일치함을 확인했다.

#### 통과한 검증

```
조작영역     sm 32 · md 40 · lg 48 / 아이콘전용 32·40·48 — 24 미만 0건
min-height   행간 4배 강제 시 40 → 66px 로 자라고 잘리지 않는다 (height 였다면 잘린다)
1.4.12       사용자 스타일시트 !important 로 검사 — 잘림 없음
포커스        마우스 클릭 링 없음 · Tab 링 2px + offset 2px
포커스 대비   6면 전부 4.79~6.03:1 (필요 3:1)
라벨 대비     variant 5 × 테마 2 = 10건 전부 통과 (최저 negative 라이트 4.77:1)
타이포        sm label-md(14/20) · md·lg label-lg(16/24)
```

#### ★ 미해결 — inactive 라벨 대비 미달

명세 미결 ① 이 예고한 그대로다. `aria-disabled` 는 **포커스를 받고 활성화되므로** WCAG 1.4.3 의 *inactive user interface component* 예외로 보기 어렵다. 그런데 색은 `label.disable` 을 쓰고 있다.

| 후보 | 라이트 fill | 라이트 bg | 다크 fill | 다크 bg | 최저 |
|---|---|---|---|---|---|
| `label.disable`(현재) | 1.32 | 1.33 | 1.30 | 1.29 | **1.29 ✘** |
| `label.assistive` | 1.67 | 1.69 | 1.74 | 1.79 | 1.67 ✘ |
| `label.alternative` | 4.51 | 4.73 | 4.10 | 4.93 | 4.10 ✘ |
| `label.neutral` | 8.49 | 9.15 | 6.25 | 7.84 | **6.25 ✔** |

**기존 토큰 중 4면 전부를 통과하는 것은 `label.neutral` 하나뿐이다.** 다만 6.25:1 이면 거의 죽어 보이지 않아 비활성 신호가 약해진다 — **"보이는 비활성"과 "읽히는 대비"가 서로 당긴다.**

**결정 전까지 Button 은 완료가 아니다.** 구현은 현재 상태로 커밋하되 8-1 미결 ① 에 ★ 로 표시했다. 값을 임의로 바꾸지 않는다.

#### 곁들여 — `.gitignore` 가 거짓이 됐다

`package-lock.json` 을 *"이 저장소는 의존성이 0이다"* 라는 이유로 무시하고 있었다. `packages/react` 가 devDependency 4개(`vite` · `@vitejs/plugin-react` · `react` · `react-dom`)를 갖게 되면서 그 전제가 깨졌다. **잠금 파일 없이는 CI 와 로컬이 다른 버전을 받는다.** 무시를 풀고 커밋했다.

`packages/tokens` 의 의존성 0 은 그대로다(0-29).

#### 검증 페이지도 한 번 틀렸다

라이트 테마 스크린샷에서 `outline`·`text` 가 안 보였다. 원인은 **검증 페이지가 자기 배경을 칠하지 않아** 라이트 토큰이 브라우저의 어두운 바탕 위에 얹힌 것이었다. 컴포넌트 결함이 아니라 **실측이 거짓말을 한 경우**다. `body` 에 `--color-bg-normal` 을 칠해 고쳤다.

---

### 0-32. inactive 는 `label.neutral` 을 쓴다 — 0-31 의 미해결을 닫는다

2026-09-03. 0-31 이 남긴 ★ 를 처리했다. 세 안(A 읽히는 색 · B 네이티브로 회귀 · C 전용 토큰 신설) 중 **A** 를 택했다.

#### 결정

```
inactive  aria-disabled  →  label.neutral    조작 가능하므로 4.5:1 을 지킨다
disabled  네이티브        →  label.disable   진짜 inactive 라 1.4.3 예외가 적용된다
```

**두 상태를 색으로도 갈랐다.** 이전에는 한 규칙이 둘 다 덮고 있었다.

#### 걱정했던 것 — 읽히면 비활성으로 안 보이지 않나

**기우였다.** 정상 라벨과 `label.neutral` 사이 대비를 재보니 **1.98:1(라이트) · 2.06:1(다크)** 이다. 배경에 대해서는 6.25:1 이상으로 읽히면서, 정상 라벨과 비교하면 눈에 띄게 흐리다. 두 요구가 동시에 만족된다.

게다가 비활성 신호가 색 하나에 의존하지 않게 됐다 — 배경(`interaction.disable`)·보더(`line.alternative`)·`cursor: not-allowed`·`aria-disabled` 가 함께 담당한다. **KWCAG 5.4.1(색에 무관한 콘텐츠 인식) 관점에서 오히려 나아졌다.**

화면으로도 확인했다. **활성 → inactive → disabled 세 단계의 시각 위계가 분명하다.** `negative` 는 inactive 가 되면 빨강이 빠져 신호가 특히 강하다.

#### 브라우저 실측 — 계산이 아니라 렌더된 값

| | 라이트 | 다크 |
|---|---|---|
| inactive / primary · secondary · negative | 8.49 | 6.25 |
| inactive / outline · text | 9.15 | 7.84 |
| disabled / (네이티브) | 1.32 | 1.30 |

**inactive 10건 전부 통과, 최저 6.25:1.** `disabled` 가 1.3 으로 남은 것은 의도다 — 포커스도 활성화도 안 되는 진짜 inactive 라 1.4.3 예외 대상이다.

**토큰은 하나도 바꾸지 않았다.** 기존 semantic 색의 배정만 고쳤다.

#### 측정 스크립트가 한 번 거짓말했다

첫 측정에서 라이트 테마가 `1.00:1` 로 나왔다. 테마를 바꾸고 **리플로우를 기다리지 않아** 스타일 재계산 전의 값을 읽은 것이었다. 원색을 직접 찍어보니 `rgb(244,244,244)` 위 `rgba(46,47,50,0.88)` 로 정상이었다.

`requestAnimationFrame` 2회 + 지연을 넣어 다시 쟀다. **측정 도구를 먼저 의심하지 않았다면 있지도 않은 결함을 고칠 뻔했다.**

---

### 0-33. 컨트롤 치수는 우리 값을 유지한다 — 기존 구현과 다르다

2026-09-03. 참고한 기존 구현(0-31)과 치수가 얼마나 같은지 대조했다. **세 축에서 다르다.**

| | 우리 | 기존 구현 | |
|---|---|---|---|
| `paddingInline` | 12 / 16 / 20 | 16 / 20 / 24 | 전 단계 4px 작다 |
| `gap` | 4 / 6 / 8 | 4 / 6 / 6 | `lg` 만 2px 크다 |
| `radius` | 6 / 8 / 12 | 12 고정 | 철학이 다르다 |
| `minHeight` | 32 / 40 / 48 | **없음**(패딩+행간으로 결정) | |
| 기본 단계 | `md` | `large` | |

**우리 값을 유지하기로 했다(안 C).**

#### 정직하게 — 우리 값에는 측정 근거가 없다

`control.*` 을 지을 때 *"기존 `spacing` 눈금에서 그럴듯한 세 단계"* 를 골랐다. **디자인에서 역산한 것이 아니다.** 기존 구현의 값도 출처를 확인하지 못했다. 즉 **둘 다 문서화된 유도 과정이 없고**, 우리 것을 유지하는 쪽을 택한 것이다.

**실제 화면이 쌓이면 조정될 수 있다.** 그때 이 표를 보고 판단한다. 미결로 올리지는 않는다 — 어느 디자인 시스템이든 치수는 실물을 보고 다듬는 것이 정상이고, 지금 답을 낼 근거가 없다.

#### radius 는 취향이 아니라 두 방식이다

높이 대비 곡률로 보면 차이가 뚜렷하다.

| | sm(32) | md(40) | lg(48) |
|---|---|---|---|
| 12 고정 | **37%** | 30% | 25% |
| 6 / 8 / 12 | 19% | 20% | 25% |

고정 방식은 **작은 버튼이 알약에 가까워진다.** 우리 방식은 곡률 비율을 비슷하게 유지한다. 어느 쪽이 옳다기보다 **작은 버튼을 얼마나 둥글게 볼 것인가**의 문제이고, 실제 화면에서 정할 일이다.

#### `minHeight` 는 우리 쪽이 낫다 — 이건 근거가 있다

기존 구현은 높이를 **패딩 + 행간으로 암묵적으로** 결정한다. 그러면

- **WCAG 2.5.8(24×24)이 보장되지 않는다** — 폰트 설정에 따라 달라진다
- 대신 **1.4.12 에는 자연히 안전하다** — 고정 높이가 없으므로

`min-height` 는 하한만 정하므로 **두 이점을 동시에 갖는다.** 실측으로 확인했다 — 행간 4배를 강제해도 40 → 66px 로 자라고 잘리지 않으며(0-31), 아이콘 전용도 32/40/48 로 24 를 넘는다.

---

### 0-34. Text Field 를 만든다 — `control.*` 이 검증되고, `line.*` 의 구멍이 드러난다

2026-09-03. P0-2 를 구현했다. **미결 17 의 결정이 실물로 검증됐고**, 동시에 **토큰 계열 하나의 설계 공백**이 드러났다.

#### `control.*` 은 옳았다 — 두 번째 소비자가 증명했다

미결 17 을 닫으며 *"`button.height` 가 아니라 `control.minHeight` — 폼 한 줄에서 버튼과 입력의 높이가 맞아야 한다"* 를 근거로 삼았다. **Button 하나로는 검증되지 않는 주장이었다.**

실측했다 — 폼 한 줄에서 **입력 40px · 버튼 40px · 바닥 정렬 차 0**. 두 컴포넌트가 같은 토큰을 그대로 공유한다. 역할 단위 이름이 맞았다.

#### ★ `line.*` 계열 전체가 1.4.11(3:1)에 미달한다

입력 테두리는 **컴포넌트를 식별하는 시각 정보**라 WCAG 1.4.11 의 3:1 대상이다. 후보를 4면 × 2테마로 전수 측정했다.

| 후보 | 최저 | |
|---|---|---|
| `line.normal` | 1.31 | ✘ |
| `line.neutral` | 1.21 | ✘ |
| `line.strong` | **1.97** | ✘ (계열 최고) |
| `line.solidNormal` | 1.22 | ✘ |
| `line.solidNeutral` | 1.13 | ✘ |
| `label.alternative` | **4.57** | ✔ |
| `label.neutral` | 7.36 | ✔ |

**`line.*` 다섯 개가 전부 미달이고, 통과하는 것은 `label.*` 뿐이다.**

원인은 설계 의도다. 우리 `line.*` 는 **구분선**용으로 만들어졌다 — 0-24 에서 카드 경계를 다룰 때도 *"보더는 KWCAG 5.4.4(콘텐츠 간의 구분)를 만족한다"* 가 근거였고, **5.4.4 에는 수치 기준이 없다.** 그래서 은은한 값으로 정해졌다.

그런데 **입력 테두리는 성격이 다르다.** 이웃 콘텐츠를 나누는 선이 아니라 **컨트롤을 식별하는 경계**이고, 그건 1.4.11 의 3:1 대상이다. **우리 토큰 집합에 "컨트롤 경계선" 자리가 없다.**

**이번에는 `label.alternative` 를 썼다**(라이트 4.73 · 다크 4.93 실측 통과). 동작은 맞지만 **의미가 어긋난다 — 글자 색을 선 자리에 쓰고 있다.** 전용 토큰이 필요하다. **미결 23** 으로 올린다. 값을 임의로 만들지 않는다.

> **뒤에 붙임(0-38)** — 결론은 전용 토큰 신설이 아니었다. `line.strong` 의 라이트 알파를 `{opacity.80}` 으로 올려 라이트 3.09 · 다크 3.49 로 맞췄고, 새 토큰은 만들지 않았다. 위 문단의 *"전용 토큰이 필요하다"* 는 그 시점의 판단이다.

#### 설계 결정 여섯 — 원본이 없어 근거를 적어 둔다

디자인 원본이 없다(0-33 과 같은 조건). 각각 근거를 남겼다.

| | 결정 | 근거 |
|---|---|---|
| 라벨 위치 | **위(stacked)** | floating label 은 1.4.4 200% 확대에서 잘리고 placeholder 를 라벨로 오인하게 한다 |
| 테두리 | **outline 사방** | Search 만 Underline 변형을 갖는다(1절) |
| 크기 | **md · lg 둘만** | 32px 입력은 글자와 여백이 답답하다 |
| helper/error | **한 줄 자리를 미리 잡는다** | 오류가 뜰 때 아래가 밀리면 레이아웃이 튄다 |
| 검증 시점 | **컴포넌트가 갖지 않는다** | 폼 상태는 폼의 것이다. `error` 를 prop 으로 받는다. 권장 규칙(submit 우선 · 이후 blur)은 명세에 적었다 |
| 필수 표시 | **보이는 "필수" 텍스트** | 별표만 쓰면 5.4.1 · 3.3.2 에 걸린다 |

**오류가 떠도 helper 를 유지한다.** `aria-describedby` 에 둘 다 넣는다 — 형식 안내가 사라지면 3.3.2 를 잃는다. 실측으로 두 id 가 모두 실제 텍스트를 가리키는 것을 확인했다.

#### 아이콘 크기 토큰 — Button 의 실제 결함을 고쳤다

`control.iconSize` sm 16 · md 20 · lg 24 를 만들었다(전부 `spacing` 별칭, 새 원시 값 0).

**Button 의 `.ds-btn__icon` 에는 크기가 없었다.** 호출자가 큰 SVG 를 넘기면 버튼이 밀려났다. 미결 ③ 으로만 적어뒀는데 실제 결함이었다. 래퍼가 크기를 정하고 `svg { inline-size: 100% }` 로 채우게 했다 — **아이콘 세트와 무관하게 동작한다.**

색 토큰은 두지 않았다. 아이콘은 `currentColor` 를 따르므로 컨테이너의 `color` 가 제어한다.

#### 측정 도구가 또 거짓말했다 — 두 번째다

라이트 테두리가 **1.74:1** 로 나왔다. 확인해보니 렌더된 색이 `rgba(173,176,181,0.74)` — **다크 테마 값**이었다. `document.documentElement.dataset.theme` 을 스크립트로 바꾸고 잰 것이 원인이다. `offsetHeight` 로 리플로우를 강제해도 요소마다 커스텀 속성 재계산 시점이 어긋난다.

0-32 에서 같은 종류의 사고가 있었다. **이번에는 규칙으로 만든다.**

> **대비를 잴 때 테마를 스크립트로 바꾸지 않는다.** 브라우저의 실제 `colorScheme` 을 바꾸고 **페이지를 새로 로드해서** 잰다.

바꿔서 재니 라이트 4.73 · 다크 4.93 으로 정상이었다. **도구를 먼저 의심하지 않았다면 있지도 않은 결함에 토큰을 고칠 뻔했다** — 0-32 와 똑같은 문장을 두 번 쓰게 됐다.

#### 실측 결과

| | 라이트 | 다크 | 기준 |
|---|---|---|---|
| 테두리 | 4.73 | 4.93 | 3:1 |
| 오류 테두리 | 5.10 | 8.01 | 3:1 |
| 입력값 · 라벨 | 18.08 | 16.11 | 4.5:1 |
| placeholder · 도움말 | 4.73 | 4.93 | 4.5:1 |
| 오류 문구 · 필수 표시 | 5.10 | 8.01 | 4.5:1 |

> **뒤에 붙임(0-38)** — 위 표의 **테두리** 값은 임시로 쓰던 `label.alternative` 기준이다. 2026-09-03 에 `line.strong`(라이트 3.09 · 다크 3.49)으로 옮겼다. 다른 행은 그대로다.

`id` 중복 0건(필드 8개) · 라벨 미연결 0건 · 오류 필드의 `aria-describedby` 가 도움말과 오류 둘 다 가리킴.

```
토큰       447 → 450   control.iconSize 3개
검증        DTCG 오류 0 · 예외 5 / AA 168건 미달 0 / 하드코딩 0건
```

---

### 0-35. Text Area 를 만들고, 공유 껍데기를 추출한다

2026-09-03. P0-3 을 구현했다. Text Field 와 90% 겹치는 컴포넌트라 **새로 배운 것보다 정리한 것이 많다.**

#### 소비자가 둘이 되자 추출할 것이 분명해졌다

`Field` 를 뽑았다 — 라벨 · 필수 표시 · 도움말 · 오류 · 메타 줄의 **껍데기**다. 입력 요소 자체의 모양은 각 컴포넌트가 갖는다.

```
Field/Field.css        .ds-field · __label · __required · __help · __error · __meta · __count
Field/useFieldIds.js   id · helpId · errId · countId · describedBy 를 한 곳에서 만든다
```

**`useFieldIds` 가 더 중요하다.** 접근성 배선(`useId` · `aria-describedby` 조합)은 **틀리기 쉽고 틀려도 조용한** 코드다. 다섯 컴포넌트가 각자 복제하면 그중 하나는 반드시 어긋난다. *"오류가 떠도 도움말을 describedby 에 남긴다"* 는 규칙도 여기 한 줄로 산다.

미결 17 과 같은 논리다 — **소비자가 둘 모이기 전에는 뽑지 않았고, 둘이 되자 뽑았다.**

#### `control.minHeight` 를 쓰지 않았다 — 그게 맞다

Text Field 는 `control.*` 을 그대로 썼지만(0-34에서 검증) **Text Area 는 `minHeight` 를 쓰지 않는다.** 여러 줄 컨트롤이라 높이를 `rows` 가 정하고, `min-height` 는 하한만 잡는다.

**`control.*` 이 만능이 아니라는 뜻이고, 그게 정상이다.** 역할 단위 이름은 *"한 줄 컨트롤의 높이"* 를 담는 것이지 모든 컨트롤의 모든 치수를 담는 게 아니다. `paddingInline` · `radius` 는 그대로 공유한다.

#### `resize` — 2.5.7 을 따져봤다

네이티브 `<textarea>` 의 크기 조절 손잡이는 **끌기 동작**이다. WCAG 2.5.7 은 끌기로만 되는 기능에 단일 포인터 대안을 요구한다.

그런데 조항에 예외가 있다 — *"사용자 에이전트가 정하고 저작자가 수정하지 않은 기능"*. 크기 조절은 UA 가 제공하는 것이다.

**`resize: vertical` 로 축만 제한했다.**
- **가로를 막는 이유는 접근성이다** — 가로로 늘리면 320px 리플로우(1.4.10)가 깨진다. 제한이 오히려 개선이다.
- 세로는 UA 기능 그대로 둔다.
- `inactive` · `disabled` 에서는 `resize: none` — 조작할 수 없는 컨트롤에 손잡이를 남기지 않는다.

#### 자동 높이는 넣지 않았다

`field-sizing: content` 가 정답이지만 **Baseline newly available 2026-06** 으로 3개월밖에 안 됐다. widely available 은 2028년 말이다.

JS 로 흉내낼 수도 있지만 **사용자의 수동 크기 조절과 싸운다** — 사용자가 늘려놓으면 다음 입력에서 도로 줄어든다. 둘 중 하나를 포기해야 하는데 지금 고를 근거가 없다. **`field-sizing` 이 성숙하면 그걸로 바꾼다**(8-3 미결 ①).

#### 글자 수 카운터 — 두 가지를 의도적으로 안 했다

**① `aria-live` 를 쓰지 않는다.** 글자마다 발화하면 입력을 방해한다. `aria-describedby` 로 연결해 포커스 시 읽히게 했다.

**② 비제어일 때는 표시하지 않는다.** 값을 모르면 숫자가 틀린다. **틀린 숫자를 보여주느니 감춘다.**

숫자 폭은 `font-variant-numeric: tabular-nums` 로 고정했다 — 미결 21 이 닫히면 토큰으로 옮긴다.

#### 실측

| | 라이트 | 다크 | 기준 |
|---|---|---|---|
| 테두리 | 4.73 | 4.93 | 3:1 |
| 오류 테두리 | 5.10 | 8.01 | 3:1 |
| 입력값 | 18.08 | 16.11 | 4.5:1 |
| placeholder · 카운터 | 4.73 | 4.93 | 4.5:1 |
| inactive 라벨 | — | 6.25 | 4.5:1 |

> **뒤에 붙임(0-38)** — 위 표의 **테두리** 값은 임시로 쓰던 `label.alternative` 기준이다. 2026-09-03 에 `line.strong`(라이트 3.09 · 다크 3.49)으로 옮겼다. 다른 행은 그대로다.

`rows` 반영 88px(3행) · 132px(5행) · `id` 중복 0(6개) · 라벨 미연결 0 · 카운터 `describedby` 연결 · 입력 시 `0 / 200` → `11 / 200` 갱신 · **320px 리플로우 통과**(넘치는 요소 0).

0-34 에서 만든 규칙대로 **테마는 브라우저 `colorScheme` 을 바꾸고 새로 로드해서** 쟀다. 이번에는 어긋난 값이 나오지 않았다.

```
컴포넌트   2 → 3 (Button · Text Field · Text Area)
토큰       450 변동 없음 — 새 토큰이 필요하지 않았다
검증        DTCG 오류 0 · AA 168건 미달 0 · 하드코딩 0건
```

---

### 0-36. Search 를 만들고 lucide 를 들인다

2026-09-03. P0-4 를 구현했다. **아이콘이 처음으로 실제 필요해진 컴포넌트**다.

#### lucide 기본값을 소스에서 확인했다 — 추정하지 않았다

`COMPONENTS.md` 에 *"설치 시점에 확인할 것"* 으로 미뤄둔 것을 채웠다. `lucide-react@1.39.0` 의 `defaultAttributes.mjs` 를 직접 읽었다.

```
width 24 · height 24 · viewBox "0 0 24 24"
stroke "currentColor" · strokeWidth 2 · fill "none"
```

**두 가지가 확인됐다.**

1. `stroke` 가 `currentColor` 다 → **아이콘용 색 토큰을 두지 않은 판단이 맞았다.** 컨테이너의 `color` 가 제어한다.
2. **작은 크기에서 획이 두꺼워질 거라던 내 걱정은 반대였다.** CSS 로 크기를 주면 viewBox 가 함께 줄어 획도 비례해 얇아진다 — 16px 에서 ≈1.33px 다. `strokeWidth` 를 조정할 필요가 없고, `size` prop 대신 CSS 를 쓰는 선택이 유지된다.

#### 네이티브 지우기 버튼을 숨겼다 — 키보드로 도달할 수 없다

`<input type="search">` 는 WebKit·Chromium 에서 X 버튼을 그려준다. **그 버튼은 포커스를 받지 않는다** — 마우스로만 누를 수 있어 WCAG 2.1.1(키보드 사용 보장)을 만족하지 못한다.

`::-webkit-search-cancel-button { display: none }` 으로 숨기고 **진짜 `<button>` 을 뒀다.**

- 값이 있을 때만 나타난다 — 제어·비제어 모두에서 동작하도록 "비었는지"만 내부 상태로 따로 추적한다
- 조작 영역 **24×24**(`--control-min-target`) — 2.5.8 과 KWCAG 6.1.3 을 함께 만족한다(0-30)
- 이름은 **`"<라벨> 지우기"`** — 한 화면에 검색이 여럿일 때 `"지우기"` 만으로는 구분되지 않는다
- **지운 뒤 포커스를 입력으로 되돌린다.** 버튼이 사라지면 포커스가 `body` 로 떨어져 키보드 사용자가 위치를 잃는다. 실측으로 확인했다

#### 라벨을 생략할 수 없게 했다

검색은 실무에서 라벨 없이 돋보기와 placeholder 만 두는 일이 흔하다. **그건 이름이 없는 컨트롤이다** — placeholder 는 접근 가능한 이름이 아니고 입력을 시작하면 사라진다.

`label` 을 필수로 하고, 화면에서 감추려면 **`hideLabel`** 을 쓰게 했다. 구현은 `.ds-visually-hidden` 이다 — `display: none` 이나 `visibility: hidden` 은 보조기술에서도 사라지므로 쓸 수 없다. `Field.css` 에 두어 앞으로 다른 컴포넌트도 쓴다.

#### 앞 아이콘에 `pointer-events: none`

돋보기를 눌렀을 때 아무 일도 안 일어나면 고장으로 읽힌다. 포인터 이벤트를 꺼서 **클릭이 입력으로 통과**하게 했다.

#### 실측

| | 라이트 | 다크 | 기준 |
|---|---|---|---|
| outline 테두리 · underline 밑줄 | 4.73 | 4.93 | 3:1 |
| 오류 테두리 | 5.10 | 8.01 | 3:1 |
| 입력값 | 18.08 | 16.11 | 4.5:1 |
| placeholder · 앞 아이콘 · 지우기 아이콘 | 4.73 | 4.93 | 4.5:1 |

> **뒤에 붙임(0-38)** — 위 표의 **테두리** 값은 임시로 쓰던 `label.alternative` 기준이다. 2026-09-03 에 `line.strong`(라이트 3.09 · 다크 3.49)으로 옮겼다. 다른 행은 그대로다.

지우기 버튼 4개 전부 24×24 · 값 비움 → 버튼 사라짐 → 포커스 복귀 → 재입력 시 복귀 확인 · `hideLabel` 이 화면 1×1 이면서 `htmlFor` 로 연결됨.

#### 이번에 못 한 것 — 시각 확인

브라우저 패널이 숨겨진 상태라 **스크린샷이 계속 빈 화면으로 왔다.** DOM 측정으로 대체했고 수치는 전부 얻었지만, **눈으로 본 확인은 이번 컴포넌트에 없다.** 다음 작업에서 패널을 띄우고 한 번 훑어야 한다.

`::-webkit-search-cancel-button` 숨김도 같은 이유로 시각 확인을 못 했다. CSSOM 에 규칙이 살아 있는 것(브라우저가 선택자를 버리지 않음)까지만 확인했다.

```
컴포넌트   3 → 4 (Button · Text Field · Text Area · Search)
의존성     ds-react 에 lucide-react 추가 (ISC) — ds-tokens 는 여전히 0
토큰       450 변동 없음
검증        DTCG 오류 0 · AA 168건 미달 0 · 하드코딩 0건
```

---

### 0-37. 하드코딩 감사 — 린터가 못 보던 곳을 찾는다

2026-09-03. 토큰과 컴포넌트 전체에 하드코딩이 없는지 훑었다.

#### `✔ 0건` 은 답이 아니었다

`npm run lint:hardcode` 는 **0건**을 냈다. 그런데 그 0건은 *"위반이 없다"* 가 아니라
*"검사기가 보는 범위에 없다"* 였다. 그래서 감사의 대상을 위반이 아니라 **검사기 자체**로
바꿨다. 정규식에 속성 이름을 하나씩 넣어 시험했고, 다음이 통째로 빠져 있었다.

```
✘ 논리 속성    padding-inline-start · padding-block-end · margin-inline-start
                inset-inline-start · inset-block-end · border-start-start-radius
✘ 크기          width · height · min-height · max-width
                inline-size · block-size · min-block-size · min-inline-size
✘ 축 자체       z-index · transition · animation
✘ 단위          단위 없는 숫자(JSX 인라인 스타일) · ch · s
```

**`padding-inline` 은 잡히는데 `padding-inline-start` 는 안 잡혔다** — 정규식이
`padding-[a-z]+` 라 하이픈이 하나 더 붙는 순간 빠져나간다. 우리 컴포넌트가 실제로 쓰는
속성이었다. `min-block-size: 37px` 을 써도 통과한다는 뜻이다.

**컴포넌트 넷이 깨끗한 것은 내가 신경 써서 그런 것이고, 검사기가 막아줘서가 아니었다.**
이것이 이 감사에서 가장 중요한 발견이다.

#### 구멍을 메우자 38건이 드러났다

목록과 단위를 넓히고 다시 돌렸다. `dim` 34 · `z` 3 · `time` 1 = 38건. **전부 뷰어 둘과
검증 페이지에서 나왔고, 배포되는 컴포넌트 CSS 넷은 0건이었다.**

| 파일 | 건수 | 성격 |
|---|---|---|
| `preview.html` | 19 dim · 2 z · 1 time | 표본 상자 치수 · 읽기 폭 · 스티키·토스트 층 · 페이드 시간 |
| `guide.html` | 6 dim · 1 z | 읽기 폭 · `1em` 아이콘 · 스티키 층 |
| `dev/main.jsx` | 9 dim | JSX 인라인 스타일의 단위 없는 숫자 |

**실제 위반은 4건이었다** — `z-index: 30`·`20`·`30` 과 `transition: opacity .18s`.
0-27 에서 `zIndex`·`duration`·`cubicBezier` 눈금을 만들어 두고 **정작 뷰어가 안 쓰고
있었다.** 눈금의 첫 소비자가 될 자리를 놓친 것이다. 넷 다 토큰으로 바꿨다.

검증 페이지의 9건은 인라인 스타일을 `dev.css` 의 클래스로 옮겨 해결했다. 남은 열 폭
(420·520px)은 *"좁은 열에서 어떻게 줄바꿈하는지"* 를 보려는 값이라 눈금에 매지 않고
이유를 적어 예외로 뒀다 — 눈금에 맞추면 그 폭에서만 괜찮은지를 검증하게 된다.

#### `ds-allow-block` 을 추가했다

뷰어의 표본 상자는 20줄 남짓이 **같은 이유**다(토큰 값이 바뀌어도 표본 틀은 고정이어야
한다 — 눈금에 매면 토큰을 바꿀 때 표본이 함께 움직여 무엇이 바뀐 건지 알 수 없다).
줄마다 같은 주석을 스무 번 다는 것은 소음이라 블록 형태를 뒀다.

```css
/* ds-allow-block: 이유 */  …  /* ds-allow-end */
```

**닫지 않으면 파일 끝까지 조용히 삼키므로 빌드를 세운다.** 이유가 없으면 열리지 않는다.
`FILE_EXEMPT`(파일 통째 예외)로 뷰어를 덮는 쪽도 검토했지만 버렸다 — 그러면 위 4건
같은 진짜 위반도 함께 사라진다. 예외는 **자리마다 이유가 붙어야** 한다.

#### 별칭 강제 검사가 없었다 — 규칙만 산문으로 있었다

`CLAUDE.md` 는 *"semantic·typography 에 원시 값 인라인 금지 — 반드시 별칭 `{group.key}`
로 참조"* 라고 못박아 두었다. 그런데 **그것을 강제하는 검사가 없었다.** 막고 있던 것은
`transition` 한 곳뿐이고(0-30 에서 넣은 것), 그마저 우연히 그 자리에만 있었다.

`assertLeavesAreAliases()` 를 뒀다. `semantic.light`·`semantic.dark`·`typography`·
`component` 의 **모든 리프**를 훑어 별칭이 아닌 값이 앉은 자리를 찾는다. 복합 값
(typography 5속성, shadow 레이어)은 하위 키까지 내려간다. 예외는 `ALIAS_EXEMPT` 에
자리 이름과 이유를 함께 적고, **쓸모없어지면 빌드를 세운다**(`TIER_EXEMPT` 규약).

실측으로 확인했다 — `component.json` 의 `{spacing.40}` 을 `{value:40,unit:"px"}` 로
바꿔 넣자 정확히 그 자리를 짚고 실패했다.

#### 예외는 `shadow` 기하 4자리다 — 근거를 실측으로 바로잡았다

걸린 것은 `shadow.offsetX`·`offsetY`·`blur`·`spread` 뿐이다. 색만 별칭
(`{color.gray.925}`)이고 기하는 `{value:3,unit:"px"}` 처럼 원시값이다.

**감사 보고 때 나는 "값들이 spacing 눈금에 없다"고 말했는데, 절반만 맞았다.** 실제로
세어 보니 서로 다른 기하 값 14개 중 **6개는 눈금에 있다**(1·6·12·14·20·24). 없는 것은
8개다(-7·-4·-2·-1·0·3·7·36).

그래도 예외로 둔 이유는 다르다.

1. **축이 다르다.** 그림자 기하는 Elevation 축이고 spacing 은 Spacing 축이다. blur
   반경을 여백 눈금으로 표현하는 것은 범주 오류다 — *"타입이 같아도 눈금이 다르면 다른
   그룹"* 이라는 우리 원칙에 정면으로 어긋난다.
2. **Elevation 축에는 눈금이 없다.** 만들려면 14개 값을 토큰으로 세워야 하는데 각 값이
   한두 그림자에서만 쓰이고 순서에 뜻이 없다. 눈금이라 부를 것이 없다.
3. **음수를 담을 길이 눈금이 없다.** 넷은 음수다.
4. **한 묶음이다.** offsetX·offsetY·blur·spread 는 한 레이어를 이루고, 따로 떼면 뜻이
   없다. 별칭 넷으로 흩으면 읽기가 더 나빠진다.

`shadowOffset`·`shadowBlur` 눈금을 신설하는 안(감사 때의 C)은 그래서 버렸다.
**1회용 값 14개를 눈금이라 부르는 셈이다.**

#### 남는 한계 — 알고 남긴다

- **여러 줄에 걸친 스타일**을 못 본다. JSX 인라인 스타일은 `style=` 이 같은 줄에 보일
  때만 단위 없는 숫자를 검사한다. 여러 줄로 흩어진 스타일 객체와, 값이 여러 줄인 CSS
  선언(`background-image: linear-gradient(…)` 안의 `rgba()`)은 빠진다.
- 정식 CSS 파서를 두지 않는 대가다. **놓치는 쪽이 잘못 잡는 쪽보다 낫다** — 오탐이
  이 도구를 죽인다. 다만 이제 그것이 *모르는 구멍*이 아니라 *아는 한계*다.
- 이 감사의 교훈은 그것이다. **검사기가 0건을 내면 검사기를 의심한다.**

```
검사기      속성 21종 · 단위 3종 추가 · ds-allow-block 신설
가드        assertLeavesAreAliases() — 별칭 강제(예외 4자리)
고친 것     뷰어 z-index 3건 · 페이드 시간 1건 → 토큰 · 검증 페이지 인라인 → 클래스
토큰        450 변동 없음 (tokens.css·dist·DESIGN.md 재생성해도 무변화)
검증        DTCG 오류 0 · AA 168건 미달 0 · 하드코딩 0건
```

---

### 0-38. 컨트롤 경계선 — 새 토큰이 아니라 `line.strong` 의 비대칭을 고친다 (미결 23 해소)

2026-09-03. Selection Controls(P0-5) 착수 전에 미결 23 을 닫았다. Checkbox·Radio 는
**테두리 말고는 아무것도 없는** 컴포넌트라 WCAG 1.4.11 의 3:1 이 그대로 걸린다.

#### 후보를 먼저 지웠다 — 추가할 것이 없었다

*"토큰을 추가한다"* 의 후보가 여럿 있어 보였지만, 실제로 열어 보니 남는 건 하나였다.

| 후보 | 판정 | 근거 |
|---|---|---|
| primitive 색 추가 | **필요 없음** | 색은 이미 있다 — 라이트 `gray.550` · 다크 `gray.300` |
| `opacity` 눈금 추가 | **필요 없음** | 필요한 `.88`·`.52` 가 이미 눈금에 있다 |
| `component.json` 에 `control.borderColor` | **불가능** | `sets.base` 라 테마 축이 없다. 색은 라이트/다크 두 값이 필요하다 |
| 테두리 없이 면(`fill`)으로 경계 | **불가능** | 실측 `fill.*` 4면 최소 **1.06~1.35:1**. 3:1 근처도 못 간다 |
| semantic 에 자리 | **여기뿐** | |

모자란 것은 색도 알파도 아니고 **그 색을 어떤 역할로 부를 이름**이었다.

#### 두 안을 실측으로 비교했다

gray 계열 × opacity 눈금 전체를 4개 면 × 2테마로 훑었다.

```
라이트 (4면 최소)      opacity  .52   .60   .70   .74   .80   .88
  gray.550 (line 계열 base)     1.97  2.23  2.61  2.79  3.08✔ 3.55✔
  gray.600                      2.28  2.65  3.23✔ 3.51✔ 4.00✔ 4.78✔
  gray.700 (label.alternative)  2.86  3.50✔ 4.57✔ 5.12✔ 6.09✔ 7.74✔
다크 (4면 최소)
  gray.300 (현 line.strong)     3.50✔ 4.19✔ 5.19✔ 5.63✔ 6.33✔ 7.36✔
```

이 표는 부동소수로 계산한 값이다. `checks/surfaces.mjs` 는 합성색을 **8비트 hex 로
양자화한 뒤** 재므로 소수 둘째 자리가 0.01 다를 수 있다(`.80` → 표 3.08 · 검사 3.09).
브라우저가 실제로 칠하는 것이 8비트라 **검사기 쪽이 맞다.** 아래 수치는 검사기 값을 쓴다.

**① `line.control` 신설** — 새 자리를 만든다 / **② `line.strong` 의 라이트를 올린다** — 기존 자리를 고친다

**②를 택했고, 알파는 `.80` 으로 정했다** — 통과하는 가장 옅은 단계다. 사연은 아래 "값을 두 번 정했다" 절에 있다.

**②를 택했다.**

#### ①을 버린 이유 — 내가 든 근거가 틀렸다

처음에 ①을 권하면서 *"3.5 를 사다리 꼭대기에 얹으면 눈금이 깨진다"* 고 했다.
**라이트만 보고 한 말이었다.**

```
라이트   alternative 1.10 · neutral 1.21 · normal 1.31 · strong 1.97
다크     alternative 1.25 · neutral 1.35 · normal 1.43 · strong 3.50   ← 이미 벌어져 있다
```

②가 만드는 간격이 아니라 **다크에 이미 있는 간격**이고, 지금까지 아무 문제도 일으키지
않았다. 사다리 논리는 성립하지 않는다.

그리고 더 중요한 것 — **다크의 `line.strong` 은 이미 컨트롤 경계로 쓸 수 있는 값이다.**
그러니 ②는 토큰의 용도를 바꾸는 일이 아니라 **라이트가 다크와 다르게 동작하는 것을
고치는 일**이다. ①을 택하면 그 비대칭(라이트 1.97 / 다크 3.50)이 그대로 남는다.

**결정적 신호는 값이 겹치는 것이었다.** ①은 다크에서 `line.control` 과 `line.strong` 이
**완전히 같은 값**이 된다. 같은 값의 토큰 두 개는 역할이 갈리지 않았다는 뜻이다.

#### `line.*` 는 설명이 하나도 없었다

*"`line.*` 는 구분선 의도로 설계됐다"* 고 말했는데, 확인해 보니 **7개 전부 `$description`
이 없고 narrative 에도 설명이 없다.** 0-24 는 카드 경계 얘기였고 내가 그것을 넓혀 말했다.
문서화된 의도가 없는 상태에서 "의도에 어긋난다"를 근거로 쓸 수는 없다.

7개를 채웠다. `strong` 에는 **컨트롤 경계 전용**임과 실측값을, 나머지 여섯에는 각자의
4면 최소 대비와 **컨트롤 경계로 쓸 수 없다**는 사실을 적었다. 다음 사람이 `normal`(1.31)
을 집는 것을 이름이 막지 못하므로 설명이 막는다.

**남은 34건** — semantic 색 리프 61개 중 41개에 `$description` 이 없다. `line` 7개를
채워 34개가 남았다. 이번 범위 밖이라 손대지 않았다. 보고만 한다.

#### 값을 두 번 정했다 — `.88` 은 과했다

처음에 `{opacity.88}`(4면 최소 3.55)로 올렸다. 통과선을 넘기는 데만 신경 쓰고 **얼마나
진해지는지는 보지 않았다.** 실물을 보고 "라이트 선이 강해진 것 같다"는 지적이 나왔고,
숫자로도 맞는 말이었다 — 흰 면 위에서 `#B5B6B9` → `#828489`, 2.02 → 3.75 로 거의 두 배다.

통과하는 가장 옅은 단계는 `{opacity.80}` 이다.

| alpha | 합성색 | 4면 최소 | |
|---|---|---|---|
| `.52` | `#B5B6B9` | 1.97 | ✘ 이전 값 |
| `.74` | `#96979C` | 2.79 | ✘ |
| **`.80`** | **`#8D8F94`** | **3.09** | ✔ **채택** |
| `.88` | `#828489` | 3.55 | ✔ 과했다 |

**`.80` 으로 내렸다.** 기준은 지키면서 세 단계 옅다.

#### 여유가 0.09 뿐이라 검사를 붙였다

`.80` 의 대가는 여유가 얇다는 것이다. 누가 `bg.normalAlternative` 를 조금만 밝게 바꿔도
3.09 가 3 아래로 떨어지는데, **그것을 잡는 검사가 없었다** — `surfaces.mjs` 는 텍스트
4.5:1 만 168건 봤다. 값을 아슬아슬하게 두려면 구조로 받쳐야 한다.

`surfaces.mjs` 에 **비텍스트 3:1 검사**를 더했다. 4개 면 × 2테마 = 8건이고, 여유가 0.2
미만이면 경고를 함께 낸다. CI 에도 같은 스크립트가 이미 걸려 있어 배선은 필요 없었다.

```
  라이트 line.strong    3.24  3.09  3.24  3.09   최소 3.09 ✔
         ⚠ 여유 0.09 — 면 색을 조금만 바꿔도 미달로 떨어진다
  다크 line.strong    3.59  3.67  3.49  3.68   최소 3.49 ✔
```

알파를 `.74` 로 되돌려 **실제로 실패하는지**(미달 4건, 종료코드 1) 확인하고 복구했다.

**목록을 넓히지 않았다.** 1.4.11 은 *"컴포넌트와 상태를 **식별하는 데 필요한**"* 시각
정보만 대상이고, W3C Understanding 은 히트 영역 경계선을 요구하지 않는다고 명시한다 —
컨트롤 안에 보이는 콘텐츠(글자·충분히 대비되는 아이콘)가 있으면 테두리는 면제다.

| | 3:1 대상 | |
|---|---|---|
| outline Button | **아니다** | 안에 글자가 있다. `line.normal`(1.31)로 두는 것이 맞다 |
| Text Field · Text Area · Search | **맞다** | 빈 상태에 보이는 콘텐츠가 없다. 라벨은 컨트롤 **바깥**이다 |
| Checkbox · Radio(미체크) | **맞다** | 윤곽 말고 아무것도 없다 |

그래서 검사 목록은 `line.strong` 하나다. 늘릴 때는 *"그 색이 없으면 컨트롤이 거기 있는지
알 수 없는가"* 를 먼저 묻는다. 그 물음이 검사기 주석에 적혀 있다.

**KWCAG 2.2 에는 1.4.11 에 해당하는 항목이 없다.** 3:1 은 WCAG 쪽 요구다(4-1).

#### 브라우저 실측

0-34 규칙대로 `colorScheme` 을 바꾸고 새로고침했다 — 스크립트로 테마를 바꾸지 않았다.
Text Field · Text Area · Search(outline·underline) 네 자리 모두 `1px solid` 로 그려지고,
오류 상태는 `status.negative`, `forced-colors` 는 시스템 색으로 정상 분기했다.

**측정 스크립트가 처음에 9.77:1 을 냈다.** 알파를 배경에 합성하지 않고 원색으로 계산한
탓이다. `CLAUDE.md` 에 *"반투명색은 배경에 합성한 뒤 계산한다"* 고 적혀 있는 규칙이다.
0-34 에서 두 번 겪은 "실측이 거짓말하는" 부류가 **또 한 번** 나왔다 — 이번엔 테마가 아니라
합성이었다. 그래서 이 검사를 스크립트가 아니라 **빌드 파이프라인**에 넣었다.

#### 함께 고친 것

- `gray.550` 의 `$description` 이 *"`line.*` 4종이 **두 테마 모두** 이것을 쓴다"* 였는데
  다크 `line.strong` 은 `gray.300` 이다. 사실과 달라 정정했다.
- **`opacity.*` 설명이 알파를 옮길 때마다 썩는다.** 0-17 에서 **실제 참조처**를 전수 조사해
  적어둔 것이라, `.52` → `.80` 이동으로 세 토큰(`52`·`80`·`88`)의 목록이 어긋났다. 전수
  대조해 고쳤고 나머지 22개는 정확했다(`opacity.8` 의 "7곳", `opacity.16` 의 "5곳" 같은
  개수 표기까지 다시 셌다). **이것을 잡는 검사는 아직 없다 — 미결 22 부류다.**

```
토큰        450 → 450 (변동 없음). 새 토큰·새 primitive·새 opacity 0개
값          semantic.light line.strong 알파 1건 (.52 → .80)
설명        line.* 7건 신규 · gray.550 1건 · opacity 3건 정정
검사        surfaces.mjs 에 비텍스트 3:1 8건 추가 (음성 시험 통과)
소비자      label.alternative → line.strong 4자리
검증        DTCG 오류 0 · 텍스트 AA 168건 미달 0 · 비텍스트 8건 미달 0 · 하드코딩 0건
```

---

### 0-39. 사용처 색인 — 손으로 적은 것은 이미 썩어 있었다

2026-09-03. *"어느 색·어느 폰트가 어느 컴포넌트에 쓰이는지 토큰에 남기고 있느냐"* 는
물음에서 시작했다. 세어 보니 **절반만이었고, 남긴 것마저 사실과 달랐다.**

#### 실태 — 색은 0건이었다

```
토큰 리프 450개 · $description 있음 367개 (81%)
그중 컴포넌트 이름이 나오는 것 ······ 9개  (타이포 7 · duration.150 · control.minHeight.sm)
색 토큰 ································· 0개
```

색·`opacity` 설명이 적고 있던 것은 *"어느 **토큰**이 나를 참조하는가"* 였다 —
`gray.550` → `line.* 4종과 fill.* 3종`, `opacity.16` → `label.disable · line.neutral …`.
**토큰→토큰이지 토큰→컴포넌트가 아니다.** 반대 방향은 `COMPONENTS.md` §8-x 에 손으로
적혀 있고, `DESIGN.md` 에는 색인이 아예 없었다.

#### 손으로 적은 것을 코드와 대조했더니 4건이 어긋나 있었다

`packages/react/src` 를 훑어 실제 사용처를 뽑아 비교했다.

| 토큰 | 설명이 주장했던 것 | 실제 코드 |
|---|---|---|
| `body-lg` | *"… Text Area"* | **TextArea 는 `body-md`** 를 쓴다 |
| `body-sm` | *"Text Field 헬퍼·에러 문구"* | **헬퍼·에러는 `label-sm`** 이다 |
| `label-sm` | Bottom Navigation·Tag·Breadcrumb·Chip | 헬퍼·에러·글자수가 **빠져 있었다** |
| `label-lg` | *"Button(대)"* | Button 의 **md·lg 둘 다** 쓴다 |

`body-lg` 는 **설명 쪽이 틀렸다.** Text Area 는 폼 컨트롤이라 같은 폼의 Text Field
(`body-md`, 14px)와 글자 크기가 맞아야 한다. `body-lg`(16px)를 쓰면 한 줄 아래 입력창과
글자가 어긋난다. 타이포 토큰이 컴포넌트보다 먼저 만들어져서 생긴 초기 추측이었다.
넷 다 설명을 고쳤다.

#### 그래서 의도와 현실을 나눠 적는다

```
$description  →  의도    "무엇에 쓰라고 만들었나"   미래의 컴포넌트도 들어간다
usedBy        →  현실    "지금 어느 컴포넌트가 쓰나"  코드에서 긁어 온다
```

**한 곳에 섞으면 방금 같은 일이 또 난다.** `label-md` 설명의 "Tab, Dropdown" 은 아직 없는
컴포넌트라 틀린 게 아니지만, 읽는 사람은 사실과 구분할 수 없다. 나란히 두면 어긋난 것이
보인다 — `body-lg` 의 의도에 Text Area 가 있는데 `usedBy` 에 없으면 둘 중 하나가 틀렸다.

`build-design-md.mjs` 가 `packages/react/src` 를 스캔해 `DESIGN.md` front matter 의 각
토큰에 `usedBy` 를 붙인다.

```yaml
  line-strong:
    light: rgba(113, 115, 121, 0.8)
    dark: rgba(193, 195, 199, 0.52)
    note: "**컨트롤 경계선.** …"
    usedBy: [Search, TextArea, TextField]
```

**286개 중 53개가 쓰이고 있다.** 컴포넌트별로는 Button 41 · Search 27 · TextField 23 ·
TextArea 21 · Field 8 이다. **나머지 233개가 죽은 토큰이라는 뜻이 아니다** — `spacing`·
`radius`·`breakpoint` 눈금은 우리 컴포넌트가 아니라 **앱을 만드는 쪽이 쓰라고 내보내는
것**이다. 이 숫자는 "우리 컴포넌트가 얼마나 덮고 있나"로 읽는다.

#### 방향이 거꾸로인 의존을 어떻게 다뤘나

토큰 패키지가 React 패키지를 읽는 것은 층 순서에 어긋난다. `ds-tokens` 는 **의존성 0** 이고
iOS·Android·Figma 가 이것만 가져간다.

**없으면 그냥 건너뛴다.** `packages/react` 가 없으면 `usedBy` 를 하나도 붙이지 않고
`usage` 에 그렇게 적는다. 토큰 패키지만 떼어내 복사한 뒤 빌드해 **실제로 확인했다** —
`usedBy` 0건, 나머지 286개 항목은 그대로다. 모노레포·CI 에는 항상 있으므로 drift 검사는
평소대로 동작한다. npm 의존성은 늘지 않았다.

#### 결정적이어야 한다

`DESIGN.md` 는 CI 가 다시 만들어 바이트 비교한다. 스캐너가 그것을 깨뜨리지 않도록
디렉터리 읽기를 이름순으로 정렬하고 컴포넌트 목록도 정렬해 낸다. 두 번 돌려 같은 파일이
나오는 것을 확인했다.

**세부 둘**
- 타이포는 CSS 변수가 아니라 **클래스**로 쓰인다. `body-md` 가 `body-md-strong` 안에
  걸리지 않도록 하이픈까지 경계로 본다(`(?<![\w-])…(?![\w-])`).
  `base` 와 `compact` 는 같은 클래스 이름을 쓰므로(compact 는 미디어쿼리로 덮는다) 둘 다 같은 `usedBy` 를 갖는다.
- 검증 페이지(`src/dev`)는 **사용처로 세지 않는다.** 배포물이 아닌데 세면 "이 토큰은
  쓰이고 있다"가 거짓이 된다. 처음 센 "55개" 에는 컴포넌트 지역 커스텀 프로퍼티
  (`--ds-btn-ov-*`)까지 들어가 있었다 — 우리 토큰만 세도록 고쳐 53개가 됐다.

```
생성기      build-design-md.mjs 에 사용처 스캐너 추가 (react 없으면 건너뜀)
문서        DESIGN.md front matter 53개 항목에 usedBy · 최상위 usage 설명
설명        타이포 4건 정정 (body-lg · body-sm · label-sm · label-lg)
토큰        450 변동 없음. 값 변경 0건
검증        결정적(2회 동일) · 단독 빌드 확인 · DTCG 오류 0 · AA 미달 0 · 하드코딩 0건
```

---

### 0-40. outline Button 의 테두리 — 규정이 아니라 변형의 존재 이유가 근거다

2026-09-03. *"outline 버튼의 테두리가 연한데 괜찮냐"* 는 물음에서 시작했다.

#### 규정상으로는 괜찮았다

1.4.11 면제에 정확히 해당한다. 버튼 안에 글자가 있고 **18.08:1** 이라, 테두리 없이도
컨트롤이 있다는 것을 알 수 있다(0-38 에서 확인한 조항). KWCAG 에는 대응 항목도 없다.

**그래서 "괜찮다"로 끝낼 수도 있었다. 그런데 실측을 늘어놓으니 다른 문제가 보였다.**

```
outline     면 #FFFFFF  1.00(페이지와 같음)   테두리 #E0E0E2  1.32:1
text        면 #FFFFFF  1.00                  테두리 없음
secondary   면 #F4F4F4  1.10:1                테두리 없음
```

**`outline` 과 `text` 는 면도 같고 글자도 같다.** 둘을 가르는 것은 1.32:1 선 하나뿐이다.
그 선이 안 보이면 두 변형은 같은 버튼이다. 규정 위반이 아니라 **변형이 존재할 이유**의
문제다.

**대비 숫자만으로는 안 잡히는 것도 하나 봤다.** `secondary` 는 면이 1.10:1 로 outline 의
테두리(1.32)보다 낮은데 화면에서는 더 잘 보인다. **얇은 선 하나보다 넓은 면이 눈에 띈다.**
같은 비율이라도 칠해진 넓이가 다르면 지각 결과가 다르다.

#### 근거가 기록돼 있지 않았다

`line.normal` 을 고른 이유가 `DECISIONS.md` 어디에도 없었다. `COMPONENTS.md` §8-1 에
*"border `{color.line.normal}`"* 이라고만 있었다. Button 을 만들 때(0-31) 정하고 근거를
남기지 않은 것이다. **근거가 없으니 "괜찮은가"에 답할 수가 없었다.**

#### `line.strong` 으로 올렸다

| | 라이트 | 다크 |
|---|---|---|
| 활성 | `#8D8F94` **3.24** | `#717275` **3.59** |
| 비활성 | `#F4F4F4` 1.10 | `#2D2E30` 1.27 |

**비활성은 `line.alternative` 그대로 뒀다.** 1.4.11 은 inactive 컴포넌트를 명시적으로
제외하고, 활성과 벌어질수록 *"지금 쓸 수 없음"* 이 더 잘 읽힌다. 활성 3.24 ↔ 비활성 1.10
이면 상태 차이가 분명하다.

**"입력 테두리와 같은 색이 된다"는 우려는 받아들이지 않았다.** 버튼은 높이 40 · 라운드 8 ·
가운데 정렬 라벨이고 입력은 좌측 placeholder · 넓은 폭이다. 모양이 충분히 다르고, 오히려
폼 한 줄에서 테두리 처리가 같은 쪽이 일관된다. Material 도 `outline` 하나를 둘 다에 쓴다.

**새 토큰은 만들지 않았다.** `line.*` 라이트 사다리는 1.10 · 1.21 · 1.31 · 3.09 이고
1.31 과 3.09 사이가 비어 있지만, 그 사이 값을 만들자고 토큰을 늘리는 것은 0-38 에서
내린 판단과 어긋난다.

#### 사용처 색인이 바로 잡아냈다

0-39 에서 붙인 `usedBy` 가 다시 만들자마자 반영됐다.

```
line-strong        usedBy: [Button, Search, TextArea, TextField]
line-normal        usedBy: (없음)          ← 소비자가 0 이 됐다
line-alternative   usedBy: [Button]
```

**`line.normal` 의 소비자가 0 이 된 것이 문서에 그대로 드러난다.** 지우자는 뜻은 아니다 —
`line.*` 는 앱을 만드는 쪽도 쓰는 눈금이다. 다만 **우리 컴포넌트가 아무도 안 쓴다는 사실이
보이는 것**이 색인의 값어치다. 손으로 적었으면 아무도 몰랐다.

```
값          토큰 변경 0건. Button.css 의 참조 1건 (line.normal → line.strong)
문서        COMPONENTS.md §8-1 · DESIGN.md 사용처 색인 자동 반영 (53 → 52)
검증        라이트 3.24 · 다크 3.59 실측 · DTCG 오류 0 · AA 미달 0 · 하드코딩 0건
```

---

### 0-41. secondary 면을 한 단계 올린다 — 그리고 활성·비활성이 같은 색이었다

2026-09-03. *"다른 시스템은 secondary 가 조금 더 진해서 primary 와 나란히 뒀을 때 무게가
한쪽으로 안 기우는 느낌"* 이라는 관찰에서 시작했다.

#### 새 토큰 없이 한 단계다

`fill.*` 는 이미 3단 사다리다. secondary 가 쓰던 `fill.normal` 바로 위가 `fill.strong` 이고,
**우리 컴포넌트 중 아무도 쓰지 않고 있었다**(0-39 의 사용처 색인으로 확인). 참조 한 줄만
바꿨다.

```
라이트  fill.normal .08  #F4F4F4 (1.10)  →  fill.strong .16  #E8E9EA (1.22)
다크    fill.normal .22  #2D2E30 (1.27)  →  fill.strong .28  #323436 (1.38)
```

두 단계는 못 올린다 — `fill.strong` 위가 없다. 새 토큰을 만들어야 하는데 0-38·0-40 에서
내린 판단과 어긋난다.

#### 확인하다 결함을 찾았다 — 활성과 비활성의 면이 같은 색이었다

```
라이트   fill.normal(활성)  #F4F4F4   ==   interaction.disable(비활성)  #F4F4F4
다크     fill.normal(활성)  #2D2E30   ≈    interaction.disable(비활성)  #2E2F32   (1.01:1)
```

**secondary 버튼은 활성이든 비활성이든 배경이 같았다.** 다른 것은 라벨 색뿐이었다.

0-32 에 *"비활성 신호는 배경·보더·커서가 함께 담당한다(KWCAG 5.4.1)"* 고 적었는데,
**secondary 에 한해서는 배경이 아무 일도 하고 있지 않았다.** 그 문장이 절반은 사실이
아니었다. WCAG 위반은 아니다(비활성은 1.4.3·1.4.11 대상이 아니다) — 그러나 *"색만으로
구분하지 마라"* 의 취지에는 어긋난다.

**이번 변경이 이것도 함께 닫는다.** 활성 `#E8E9EA` ↔ 비활성 `#F4F4F4` 로 면이 갈린다.
비활성 쪽은 손대지 않았다.

#### 부작용을 재고 넣었다

| | 지금 | 변경 후 |
|---|---|---|
| 라벨 대비 (라이트) | 16.44 | **14.88** |
| 라벨 대비 (다크) | 12.70 | **11.67** |
| 기본 ↔ press (라이트) | 1.31 | **1.31** |
| 기본 ↔ press (다크) | 1.46 | **1.45** |
| primary 와의 대비 | 12.17 | **11.01** |

상태 오버레이는 검정·흰색 절대값(5%·12%)이라 바탕이 바뀌어도 계단이 그대로다.
라벨은 4.5:1 기준에서 한참 위다.

**브라우저 실측** — 라이트 `#E8E9EA`(페이지 1.22 · 라벨 14.88) · 다크 `#323436`
(페이지 1.38 · 라벨 11.67).

#### 옆 시스템의 Filled 는 우리에게 그대로 오지 않는다

참고한 화면은 **페이지 바탕이 회색**이라 흰 면을 칠하면 그 자체로 경계가 생긴다
(`Tertiary (Filled)`). 우리 라이트 페이지는 흰색이고, 우리 면 4종은 서로 **1.00~1.12:1**
안에 있다(4-2). **면을 바꿔 경계를 만들 수 없다.**

그래서 우리는 방향을 뒤집어 쓰고 있다 — 회색 면을 흰 페이지에 얹는다. 그 자리가
`secondary` 다. 이번에 한 단계 올린 것은 그 역할을 더 분명히 한 것이다.

`subtle` 변형은 만들지 않았다. 필요해지는 시점은 **보조 버튼 여럿이 한 줄에 서는 화면**
(Table 행 액션 · Toolbar · Chip 줄)이고, 지금 만들면 쓸 곳 없이 변형만 는다.

#### 측정 스크립트가 또 알파를 안 합성했다

브라우저에서 `rgba(113,115,121,0.16)` 을 그대로 색으로 읽어 `#71737900` 이라는 값을 냈다.
0-38 에서 같은 실수를 하고 그 절에 적어두기까지 했는데 **또 했다.** 값이 이상해서 잡았다.

토큰 검사(`checks/surfaces.mjs`)에는 합성이 들어 있다. **브라우저에서 즉석으로 재는
스크립트가 문제다** — 반투명 토큰을 잴 때는 반드시 배경에 합성한다.

```
값          토큰 변경 0건. Button.css 의 참조 1건 (fill.normal → fill.strong)
부수효과    secondary 활성·비활성의 면이 갈렸다 (전에는 라이트에서 완전히 같은 색)
문서        COMPONENTS.md §8-1 · DESIGN.md 사용처 색인 자동 반영
검증        라이트 1.22/14.88 · 다크 1.38/11.67 실측 · AA 미달 0 · 하드코딩 0건
```

---

### 0-42. inactive 는 면을 벗고 윤곽만 남긴다 — 활성 secondary 와 헷갈렸다

2026-09-03. *"inactive 가 secondary 랑 너무 비슷해서 사용자가 헷갈리겠다.
보통 inactive 는 텍스트 색을 좀 빼지 않나"* 라는 지적에서 시작했다. 맞는 말이었고,
원인은 바로 앞 0-41 이 만든 것이었다.

#### 원인 — inactive 가 secondary 가 떠난 자리를 물려받았다

`interaction.disable` 은 `#F4F4F4` 이고, 0-41 이전의 `fill.normal`(= 옛 secondary)과
**같은 값**이다. secondary 를 `fill.strong`(`#E8E9EA`)으로 올리자 inactive 가 예전
secondary 의 모습을 그대로 입게 됐다.

실제로 헷갈리는 장면이 있다 — 폼 하단의 `취소`(활성 secondary)와 `저장`(inactive).

```
활성 secondary   면 #E8E9EA (1.22)   라벨 #161617 (14.88)
inactive         면 #F4F4F4 (1.10)   라벨 #464749 ( 8.46)
```

면 대비 **1.105**, 라벨은 둘 다 진하다. 어느 쪽이 눌리는지 모양으로 알 수 없었다.

#### "텍스트를 뺀다"가 산수에서 막혔다

4개 면 × 2테마로 전부 쟀다.

```
                                              라이트 최소   다크 최소
면 interaction.disable + label.neutral            8.46 ✔      6.23 ✔   ← 그때까지
면 interaction.disable + label.alternative        4.49 ✘      4.08 ✘
면 fill.alternative    + label.alternative        4.41 ✘      4.25 ✘
면 없음                + label.alternative        4.55 ✔      4.71 ✔
```

**어떤 회색 면을 깔아도 `label.alternative` 가 4.5:1 을 못 넘는다.** 0-32 가 정한 바닥
(inactive 는 눌리므로 WCAG 1.4.3 의 예외가 아니다)이 걸린다.

면으로 가르는 것도 안 된다. 우리 면 전체가 페이지 대비 **1.00~1.22** 안에 있어서(4-2)
회색 두 개를 눈에 띄게 벌릴 수 없다 — `fill.alternative` 로 옮겨도 secondary 와의 대비가
1.105 → 1.145 로 거의 그대로다. **값 조정이 아니라 팔레트의 구조다.**

**그래서 둘 중 하나만 고를 수 없었다. 라벨을 빼려면 면을 없애야 한다.**

#### 면을 없애면 다른 구멍이 도로 열린다 — 윤곽으로 막았다

같은 점검에서 **`inactive text` 에 비색 신호가 하나도 없다**는 것도 찾았다. 면도 보더도
없어 **글자 색 하나**로만 비활성을 알렸고, 남는 것은 `cursor: not-allowed` 인데
**커서는 터치 기기에 없다.**

`Button.css` 주석에는 *"배경·보더·커서가 함께 담당한다(5.4.1)"* 고 적어 두었는데,
`text` variant 에 대해서는 그 문장이 사실이 아니었다. 내가 쓴 문장이다.

모든 inactive 가 면을 벗으면 이 구멍이 전 variant 로 번진다. 그래서 **윤곽을 남겼다.**

```
.ds-btn[aria-disabled='true'], .ds-btn:disabled {
  background-color: transparent;
  box-shadow: inset 0 0 0 var(--divider-1) var(--color-line-alternative);
}
```

신호가 색 하나가 아니다 — **면이 사라지고 윤곽이 남는다.** 모양이 바뀐다.

> **뒤에 붙임(0-43)** — 이 절의 근거가 틀렸다. 1.4.1/5.4.1 은 비활성 상태의 표현을 다루지 않는다(원문 확인). 윤곽은 규정이 아니라 내 오해 위에 세운 장치였고, 그 대가로 primary 의 실루엣이 뒤집혔다. 0-43 에서 걷어내고 variant 별 처리로 바꿨다. **아래의 "disabled 도 함께 벗겼다" 도 더는 유효하지 않다.**

#### disabled 도 함께 벗겼다

disabled 만 면을 유지하면 **disabled 가 inactive 보다 더 있어 보여** 순서가 거꾸로 된다.
윤곽은 같게 두고 **라벨로 갈린다.**

| | 면 | 윤곽 | 라벨 (라이트/다크) |
|---|---|---|---|
| 활성 secondary | `#E8E9EA` / `#323436` | — | **14.88 / 11.67** |
| inactive | 없음 | `line.alternative` 1.10 / 1.25 | **4.73 / 4.92** |
| disabled | 없음 | 같음 | **1.33 / 1.29** |

라벨 4.73 ↔ 14.88 은 3배 차이다. 이제 모양으로 갈린다.

윤곽에 `line.alternative`(1.10)를 쓴 것은 1.4.11 이 inactive 컴포넌트를 명시적으로
제외하기 때문이다. 활성 outline(3.24)과 벌어질수록 *"지금 쓸 수 없음"* 이 잘 읽힌다.

#### 뜻밖의 소득 — 라벨이 이미 검사받는 자리로 왔다

전에는 inactive 라벨이 `interaction.disable` **면 위**에 앉아 있었다. `surfaces.mjs` 의
168건 검사는 `bg.*` 4면 위의 텍스트만 보므로 **그 조합은 아무도 검사하지 않았다.**

면을 없애자 라벨이 페이지 면 위로 내려왔고, `label.alternative` 는 이미 168건에 들어
있다. **검사를 새로 만들지 않고 기존 검사 범위 안으로 들어왔다.**

#### 남긴 것

`inactive` 는 여전히 variant 를 구분하지 않는다 — primary·secondary·negative·outline·
text 가 모두 같은 모습이 된다. `negative` 의 빨강도 사라진다. **의도다.** 비활성인
파괴적 버튼이 빨갛게 남으면 *"지금 위험한 액션이 활성"* 으로 잘못 읽힌다. 의미는 라벨
글자가 지킨다. 전에는 코드에만 있고 근거가 없던 것을 여기 적어 둔다.

```
값          토큰 변경 0건. Button.css 의 비활성 블록 하나
동작        inactive·disabled 모두 면 제거 + line.alternative 윤곽
            inactive 라벨 label.neutral → label.alternative
검증        라이트 4.73 / 다크 4.92 실측 (4개 면 최소 4.55 / 4.71)
            AA 168건 미달 0 · 비텍스트 8건 미달 0 · 하드코딩 0건
```

---

### 0-43. 근거를 잘못 인용했다 — 그리고 그 위에 세운 장치를 걷어낸다

2026-09-03. *"1.4.1 / 5.4.1 이거 우리 기준선 내용 맞지?"* 라는 확인에서 시작했다.
맞았지만, **그 조항을 근거로 쓴 내 문장들이 틀렸다.**

#### 원문을 확인했다

| | |
|---|---|
| WCAG 1.4.1 Use of Color | **Level A**. *"Color is not used as the only visual means of conveying information…"* |
| 등급 | AA 준수는 A 를 포함하므로 우리 범위 안이다 ✔ |
| KWCAG 5.4.1 색에 무관한 인식 | 1.4.1 에 대응 → **KR delta 가 아니다**(KR delta 는 5.4.4 뿐) ✔ |

**대응관계와 등급은 맞았다.** 문제는 두 가지였다.

**① 1.4.1 은 비활성 상태의 표현을 다루지 않는다.** Understanding 은 정보 전달·링크
구분·폼 필드를 다루고, **컨트롤이 비활성일 때 어떻게 보여야 하는지에 대해 아무 말도
하지 않는다.** 그런데 나는 *"비활성을 색만으로 표시하면 5.4.1 위반"* 이라고 규정처럼
써왔다. 그건 **우리가 채택한 설계 원칙**이지 규정이 아니다.

**② G183 의 범위를 잘못 짚었다.** G183 은 *"본문 속 인라인 링크를 주변 텍스트와 3:1 로
구분한다"* 는 기법이고, 버튼의 활성↔비활성에 적용되는 기법이 아니다. 내가 0-42 직후
"명도 차 3:1 이면 색 구분이 아니다"의 근거로 댔는데 잘못 댔다.

살아남는 것은 Understanding 본문의 *"색이 hue 와 명도 **양쪽**에서 3:1 이상 다르면
색만으로 구분한 것이 아니다"* 뿐이고, 우리 비활성은 hue 없이 명도만 다르므로 **그대로
적용되지도 않는다.**

`COMPONENTS.md` §3-3 에 1.4.1 을 등급과 함께 넣고, **범위를 넓게 잡지 말라**는 주석을
달았다. 표에 없어서 등급도 대응관계도 확인할 수 없던 것이 이번 혼란의 원인이었다.

#### 근거가 사라지자 장치도 근거를 잃었다

0-42 는 **모든 비활성 버튼의 면을 벗기고 윤곽을 남겼다.** 그 윤곽의 이유가 *"text
variant 에 비색 신호가 없다(5.4.1)"* 였다. **그 이유가 규정 근거가 아니었다.**

그리고 그 장치에는 대가가 있었다 — **primary 가 비활성이 되면 검은 채움이 흰 빈 버튼으로
뒤집혔다.** 사용자 지적: *"프라이머리 버튼이 inactive 가 되면 갑자기 색이 빠지고
아웃라인이 생기나? 이렇게 보인다."* 같은 버튼이 꺼진 것으로 읽히지 않는다.

**근거가 사라진 장치를 남겨두면 다음 사람이 "접근성 때문에 있는 것"으로 알고 못
건드린다.** 실제로 하루 만에 내가 그렇게 만들었다. 걷어냈다.

#### variant 의 실루엣을 지킨다

```
             기본                inactive              disabled
primary      primary.normal      interaction.inactive  interaction.disable
negative     status.negative     interaction.inactive  interaction.disable
secondary    fill.strong         면 없음               면 없음
outline      line.strong 선      line.alternative 선   line.alternative 선
text         면·선 없음          면·선 없음            면·선 없음
```

**채워진 버튼은 채워진 채로, 빈 버튼은 빈 채로 약해진다.**

실측(라이트, 4열 = 기본·inactive·disabled·loading)

```
primary     #2E2F32 면13.39 글12.50  → #989BA1 면2.79 글6.49 → #F4F4F4 면1.10 글1.32
negative    #D5242A 면 5.10 글 4.77  → #989BA1 면2.79 글6.49 → #F4F4F4 면1.10 글1.32
secondary   #E8E9EA 면 1.22 글14.88  → 면 없음      글4.73  → 면 없음      글1.33
outline     선3.24        글18.08    → 선1.10       글4.73  → 선1.10       글1.33
text        —             글18.08    → —            글4.73  → —            글1.33
```

**`interaction.inactive` 를 면으로 쓴다.** 채워진 채로 확실히 약해지는 면이 이것뿐이다 —
활성 primary 와 **4.81(라이트) · 6.34(다크)** 로 벌어져 상태가 명도로 읽히고, 활성
secondary 와도 2.29 / 1.84 로 떨어져 헷갈리지 않는다. **이 면 위에서 4.5:1 을 넘는 라벨은
`label.normal`(6.49 / 6.34) 하나뿐**이라 그것을 쓴다. 토큰 값은 그대로 두고
`$description` 만 넓혔다 — 전에는 "전경색"으로만 적혀 있었다.

**`secondary` 만 면을 잃는다.** 이미 가장 옅은 채워진 면이라 더 내려갈 자리가 없고
(회색을 한 단계 낮춰봐야 활성과 1.11 차이 — 4-2: 우리 면은 전부 1.00~1.22 안에 있다),
면이 있으면 `label.alternative` 가 4.5:1 을 못 넘는다(라이트 4.49 · 다크 4.08).
변화는 `#E8E9EA → 흰색` 이라 미미하다.

#### 검증 페이지에 variant × 상태 전면표를 넣었다

한 variant 가 상태를 넘어갈 때 모양이 어떻게 변하는지 **가로로** 읽고, 같은 상태끼리
구별되는지 **세로로** 본다. 0-42 의 문제는 이 표를 만들자마자 한눈에 보였다 —
inactive·disabled 열이 5행 모두 같은 값이었다.

**상태를 눈으로 판단하지 않으려면 눈으로 볼 수 있게 늘어놓아야 한다.**

#### 고친 자리

```
Button.css        비활성 블록 — 5.4.1 인용 제거, variant 별 처리로 재작성
COMPONENTS.md     §3-3 에 1.4.1 추가 + 범위 주석 · §8-1 상태 표 · 금지 항목 1건
semantic.*.json   interaction.inactive 의 $description (값 변경 없음)
dev/main.jsx      variant × 상태 전면표
```

**정당한 5.4.1 인용은 그대로 뒀다** — 필수 표시(`Field.css`)와 오류 표시
(`TextField.css`)는 정보 전달이라 조항 범위 안이다.

```
값          토큰 변경 0건
검증        DTCG 오류 0 · 텍스트 AA 168건 미달 0 · 비텍스트 8건 미달 0 · 하드코딩 0건
```

---

## 1. 색

### 1-1. 단계를 WCAG 상대휘도로 고정한다

유채색 13계열 × 6단계는 OKLCH 로 생성한다. 단계 번호는 **목표 상대휘도**로 정의하며, 계열이 달라도 같은 번호면 대비가 같다.

| 단계 | 목표 휘도 | 흰 배경 대비 | `#1A1B1C` 대비 | 역할 |
|---|---|---|---|---|
| 200 | 0.620 | 1.56~1.57 | 11.00~11.08 | 밝은 틴트 |
| 300 | 0.440 | 2.13~2.16 | 7.98~8.10 | 다크 텍스트(여유) |
| 400 | 0.278 | 3.17~3.23 | **5.33~5.44** | **다크 텍스트** · 라이트 비텍스트 |
| 500 | 0.200 | 4.16~4.24 | 4.06~4.14 | 균형점 |
| 600 | 0.156 | **5.07~5.13** | 3.36~3.40 | **라이트 텍스트** · 다크 비텍스트 |
| 700 | 0.098 | 7.02~7.14 | 2.42~2.46 | 라이트 텍스트(여유) |

**400 과 600 은 서로의 거울이다.** 라이트 테마는 600, 다크 테마는 400 을 쓰면 양쪽 모두 5.1~5.4 로 맞는다. `CLAUDE.md` 의 "라이트·다크 동등" 요구가 이 구조로 충족된다.

**왜 휘도 앵커인가.** 대비는 휘도의 함수다. OKLCH 의 L 을 고정하면 톤은 맞지만 대비는 계열마다 어긋난다. 반대로 휘도를 고정하면 대비가 정확히 맞고 L 편차는 5 이내로 따라온다(실측 400단계 63.1~68.0, 폭 4.9). 대비가 목적이므로 휘도를 앵커로 삼았다.

**검토했다 버린 것 — 동일 OKLCH L 고정.** 톤 균일도는 조금 낫지만 계열별 대비가 갈려서, 결국 토큰마다 대비를 따로 확인해야 한다. 자동 보장이라는 이점이 사라진다.

### 1-2. 채도 상한은 400~700 에만 건다

채도는 그 휘도·hue 에서 sRGB 가 허용하는 최대의 95% 를 쓰되, 400~700 에만 상한을 둔다(400 = 0.21, 500 = 0.22, 600 = 0.21, 700 = 0.18). **200·300 에는 상한이 없다.**

**왜 어두운 쪽만 막는가.** blue·violet·purple·pink 는 색역 특성상 어두워질수록 채도 여력이 커진다. 상한이 없으면 700 이 600 보다 더 채도가 높아져, "어두울수록 깊어진다"는 기대와 반대로 네온처럼 튄다. 상한이 실제로 구속하는 토큰은 15 개다 — `red.500~700`, `blue.600~700`, `violet.600~700`, `purple.400~700`, `pink.400~700`.

**200·300 의 상한은 한 번 걸었다가 풀었다.** 처음엔 밝은 단계를 균일한 파스텔로 만들려고 0.11 / 0.17 을 걸었는데, `yellow.200` 이 카키(`#DED078`), `green.200` 이 탁한 연두(`#9CDEA0`)가 되어 계열 정체성을 잃었다. 대비에는 아무 영향이 없는 제약이었으므로 해제했다. 해제로 8 개 토큰이 바뀌었고 대비는 그대로다.

> **주의** — 다음에 "일관성 있게 상한을 전부 풀자" 는 판단을 하지 말 것. 400~700 의 상한은 위 이유로 의도적으로 남긴 것이다.

### 1-3. 어두운 노랑·주황이 칙칙한 것은 색역 한계다

`yellow.400`(`#A29119`), `yellow.600`(`#7C6E11`), `orange` 전 단계는 **이미 그 휘도에서 가능한 최대 채도**다. 노랑은 본래 휘도가 높은 색이라, 라이트 테마 텍스트에 필요한 어두운 영역에서는 올리브가 될 수밖에 없다. sRGB 의 제약이며 조정으로 해결되지 않는다.

### 1-4. Hue 는 계열당 하나로 고정한다

| 계열 | hue | 계열 | hue | 계열 | hue |
|---|---|---|---|---|---|
| red | 26° | yellow | 100° | lightBlue | 240° |
| redOrange | 41° | lime | 122° | blue | 260° |
| orange | 61° | green | 146° | violet | 284° |
| | | teal | 180° | purple | 313° |
| | | cyan | 214° | pink | 335° |

기존에는 계열 안에서 hue 가 흔들렸다(blue 254~261°, violet 282~291°). 계열당 하나로 고정했다.

**lime 을 138° → 122° 로 옮겼다.** green(146°)과 8° 차이라 사실상 구분되지 않았다. 122° 로 옮겨 황록으로 분리했다. 이번 개편에서 인상이 가장 크게 바뀐 부분이다.

**yellow(100°)·teal(180°) 을 신설했다.** 61°→146°, 146°→214° 구간이 각각 85°·68° 로 비어 있었다. 추가 후 최대 간격이 39° 로 좁혀졌다.

### 1-5. semantic 매핑 규칙

| 그룹 | 라이트 | 다크 |
|---|---|---|
| `status.*` | 600 | 400 |
| `atomic.*` | 600 | 400 |
| `accent.fg.*` | 600 | 400 |
| `accent.bg.*` | 600 | 600 |

`atomic.*` 은 이전에 라이트·다크가 같은 값(`*.400`)이어서 라이트에서 비텍스트 3:1 조차 통과하지 못했다. 테마별로 분리했다.

### 1-6. `platform.iosAccent` 를 제거했다

`blue.350` = `#0088FF` 라는 출처 불명의 상수가 `ios.accentPrimary` 하나만을 위해 존재했다. `$description` 이 없어 근거를 확인할 수 없었고, 널리 알려진 iOS 시스템 블루(`#007AFF` / `#0A84FF`)와도 일치하지 않았다. 라이트에서 3.52:1 로 텍스트 기준 미달이기도 했다.

스케일로 이관했다 — 라이트 `{color.blue.600}`, 다크 `{color.blue.400}`. 대비는 3.52 → **5.13** 으로 개선됐다.

### 1-7. 개선 전/후 (기준값)

| 항목 | 이전 | 이후 |
|---|---|---|
| AA 대비 실패 | **7 건** | 0 |
| 라이트/다크 대비 격차 | 최대 Δ7.1 | 최대 Δ0.37 |
| 같은 단계 OKLCH L 편차 | 최대 23 | 최대 4.9 |
| 유채색 계열 / 단계 | 11계열 · 3~7단계 | 13계열 · 6단계 |
| 유채색 토큰 | 65 | 78 |

이전 실패 7 건은 전부 라이트 테마였다 — `atomic.green60` / `atomic.orange60` / `atomic.red60`(3:1 미달), `accent.fg.redOrange` / `accent.fg.lime` / `accent.fg.lightBlue` / `accent.fg.pink`(4.5:1 미달).

---

## 2. 타이포

### 2-1. 행간은 2px 그리드를 원본으로 둔다

> **갱신(2026-08-25):** 그리드는 여전히 설계 원본이지만 **저장 형태가 바뀌었다** — DTCG 9.8 요구에 따라 rem 절대값에서 fontSize 배수로 전환했다. 근거와 대가는 0-8 에 있다.

`primitive.lineHeight` 는 **키 = px, 값 = rem** 이다(`22` → `1.375rem`). 그리드는 13 종 — 16 · 18 · 20 · 22 · 24 · 26 · 28 · 32 · 36 · 40 · 44 · 52 · 72px. 전부 짝수다(4의 배수는 아니다).

**왜 배수(ratio)가 아닌가.** 이전에는 복합 토큰에 `1.5714` 같은 배수가 직접 적혀 있었다. 확인해 보니 27 개 전부 `목표 행간 px ÷ 글자 px` 로 정수가 떨어졌다 — 즉 진짜 원본은 px 그리드였고 파일에는 계산 결과만 남아 있었다. 배수는 글자 크기에 종속돼 재사용이 불가능하다(같은 24px 행간이 16px 글자에선 1.5, 20px 글자에선 1.2). 그래서 배수를 primitive 로 올리면 `lineHeight.1-5714` 같은 무의미한 이름이 된다. 그리드를 원본으로 두는 쪽을 택했다.

**rem 을 쓴 이유.** 절대 px 로 두면 사용자 글자 확대에 반응하지 않는다. rem 이면 글자와 행간이 같은 비율로 커진다. 전환 후 33 개 유틸 클래스가 픽셀 단위로 이전과 동일함을 확인했다.

**검토했다 버린 것 — 빌드에서 `행간px ÷ 글자px` 계산.** rem 으로 두면 나눗셈이 필요 없고 `build.mjs` 는 `fontSize` 와 동일한 별칭 변환만 추가하면 된다. 더 간단해서 이쪽을 택했다.

### 2-2. 자간은 크기 밴드 4개다

> **갱신(2026-08-25):** 밴드라는 **설계 의도는 그대로**이고 **저장 형태만 바뀌었다** — 밴드 4개(`em`)에서 크기별 11개(`rem`)로. 밴드 원본은 `$extensions."net.infobank.ds.trackingBands"` 에 남아 있고, "자간은 크기가 결정한다"를 이제 빌드가 강제한다. 근거는 0-11 에 있다.

| 토큰 | 값 | 적용 글자 크기 |
|---|---|---|
| `letterSpacing.display` | `-0.02em` | 40px 이상 |
| `letterSpacing.heading` | `-0.015em` | 24~36px |
| `letterSpacing.title` | `-0.01em` | 16~20px |
| `letterSpacing.none` | `0em` | 14px 이하 |

자간은 독립 축이 아니라 **글자 크기의 종속 함수**다. 실측 결과 같은 크기인데 자간이 다른 경우가 한 건도 없었다. 원래 `typography.json` 의 설명에도 "자간 = 크기밴드 파생값" 이라고 적혀 있었고, 그 의도대로 밴드를 토큰화했다.

### 2-4. `spacing` 에서 홀수·반픽셀 4개를 제거했다

`0.5` · `3` · `5` · `7` 을 지웠다(19 → 15개). 1~8 구간이 1px 단위로 8단계나 있었는데 실무에서 `2·4·6·8` 만 쓰이고 홀수는 거의 쓰이지 않는다. 남은 스케일은 전부 짝수이며, 4px 그리드(`4·8·12·16·20·24·32·40·48·56`)에 미세 조정용 `1·2·6·10·14` 가 붙는 구조다.

`0.5px` 는 헤어라인용으로 추정됐으나 배율 1x 화면에서 0 또는 1px 로 반올림돼 의도대로 나오지 않고, 같은 용도로 `divider.1`(1px) 이 이미 있다. 삭제 직전 참조 0 건을 확인했다.

### 2-5. `radius` 는 사용처를 토큰에 적었다

`spacing` 과 달리 `radius` 는 각 단계의 사용처가 정해져 있어 `$description` 에 기입했다(0 · 4 · 6 · 8 · 12 · 16 · 20 · circle · full). `radius.0` 을 새로 추가했다 — `CLAUDE.md` 상 `0` 은 구조적 상수로 하드코딩 예외이지만, 스케일을 0 부터 완결시키고 "모서리 없음"을 명시적으로 표현하기 위해 토큰으로 뒀다.

`12`(카드·팝업)와 `20`(더 큰 컨테이너)의 경계는 원 설명에서 둘 다 "카드"를 가리켜 모호했다. **12 를 개별 카드·팝업, 20 을 그보다 큰 컨테이너로 해석해 적었다** — 실제 의도와 다르면 조정이 필요하다.

### 2-3. 레거시 51개를 제거했다

이전 세대의 px 기반 토큰 — `fontSize` 16 개(`display-1` 등), `lineHeight` 19 개, `letterSpacing` 16 개. 참조가 0 건이었고, px 라 글자 확대에 반응하지 않았으며, `tokens.css` 에는 그대로 출력되고 있어 신·구를 혼동할 위험이 있었다. 삭제 직전 참조 0 건을 재확인하고 제거했다.

---

## 2-6. Elevation — 5단계로 축소, 약어 통일, semantic 이동

`shadow` 11개 중 **6개를 삭제**했다 — `spread.small` · `spread.medium` · `segmentKnob` · `switchIosKnob` · `textOverlay` · `pageIndicatorText`. 넷은 컴포넌트 전용 이름인데 해당 컴포넌트가 없고, `$description` 도 참조도 없어 용도를 확인할 수 없었다. `spread` 2개는 blur 60·75px 로 그림자보다 광원에 가까웠으나 역시 근거가 없었다. 필요해지면 컴포넌트 구현 시 값을 새로 정한다.

남은 5개는 `normal.` 중간 단계를 없애고 이름을 바꿨다. **값은 그대로다.**

| 이전 | 이후 | CSS 변수 |
|---|---|---|
| `shadow.normal.xsmall` | `shadow.xs` | `--shadow-xs` |
| `shadow.normal.small` | `shadow.sm` | `--shadow-sm` |
| `shadow.normal.medium` | `shadow.md` | `--shadow-md` |
| `shadow.normal.large` | `shadow.lg` | `--shadow-lg` |
| `shadow.normal.xlarge` | `shadow.xl` | `--shadow-xl` |

**이름 규칙의 근거.** 이 DS 의 숫자 키는 전부 "숫자 = 실제 값"이다(`spacing.16`=16px, `radius.8`=8px, `lineHeight.22`=22px, `opacity.52`=52%). 그림자는 `x·y·blur·spread·color` 가 묶인 복합 토큰이라 단일 숫자로 표현할 수 없으므로, `shadow.1/2/3` 같은 서수를 쓰면 숫자 키의 의미가 깨진다. 같은 복합 토큰인 `typography` 가 이미 t-shirt 약어(`-xl`/`-lg`/`-md`/`-sm`)를 쓰고 있어 그쪽에 맞췄다.

> **원칙** — 판별 기준은 "값이 바뀌어도 이름이 여전히 맞는가" 다.
> - **이름이 값 자체인 것 → 숫자.** `spacing.16`(16px) · `radius.8` · `lineHeight.22` · `divider.1` · `opacity.52`. 값을 바꾸려면 다른 토큰을 쓰는 것이므로 이름과 값이 붙어 있어도 된다.
> - **이름이 역할·단계인 것 → 이름.** `shadow.md` · `typography.body-md` · `letterSpacing.heading` · `breakpoint.md`. 임계값·구성이 조정돼도 단계는 그대로 유효해야 하므로 값을 이름에 넣지 않는다.
>
> **티어 이름을 쓰면 값이 이름 순서를 따라야 한다.** `xl < lg` 같은 상태는 어느 명명 철학에서도 깨진 것이다. 빌드의 `assertTierScalesOrdered()` 가 강제하고, 의도적 역순은 `TIER_EXEMPT` 에 이유와 함께 등록한다.

**`gradient.maskSize`(5개)는 2026-08-25 에 `xs`~`xl` 로 바꿨다.** 그 과정에서 값이 이름 순서를 거스르고 있던 것이 드러났다 — 0-10 참조.

### Elevation 은 파일에서 두 곳, 문서에서 한 묶음이다

파운데이션 분류상 Elevation = **그림자 + dim** 이지만, 파일에는 `elevation` 이라는 단일 컨테이너가 없다.

```
semantic.light/dark.json
├── color
│   └── elevation
│       └── dim        $type: color   → --color-elevation-dim
└── shadow             $type: shadow  → --shadow-xs … --shadow-xl
```

**`$type` 이 다르기 때문이다.** dim 은 `background` 에 쓰는 색이고 그림자는 `box-shadow` 다. 한 그룹에 넣으면 빌드가 타입별로 분기해야 하고 CSS 변수 접두사도 성격이 다른 것이 섞인다.

**검토했다 버린 것 — `shadow` 그룹을 `elevation` 으로 개명.** 그러면 `--elevation-xs` 와 `--color-elevation-dim` 으로 접두사가 갈라져 오히려 헷갈린다.

**대신 문서와 미리보기에서 묶는다.** `preview.html` 에 **Elevation 섹션**을 두고 그림자 5개와 dim 을 함께 보여준다(dim 은 색 Semantic 섹션에서 제외해 중복을 막았다). 파운데이션 분류는 원래 문서상의 묶음이지 파일 구조와 1:1 일 필요가 없다 — Typography 도 `typography.json` + primitive 의 font 계열 5개 그룹에 걸쳐 있다.

### Divider — 숫자 키로 바꾸고 8px 로 줄였다

| 이전 | 이후 |
|---|---|
| `divider.thickness.normal` = 1px → `--divider-thickness-normal` | `divider.1` = 1px → `--divider-1` |
| `divider.thickness.thick` = **12px** → `--divider-thickness-thick` | `divider.8` = **8px** → `--divider-8` |

**t-shirt(`sm`/`lg`)가 아니라 숫자다.** divider 는 단순 치수라 `spacing.16`·`radius.8`·`lineHeight.22` 와 같은 계열이고, 위의 원칙(단일 수치 = 숫자, 복합 = t-shirt)을 그대로 적용했다. t-shirt 는 `shadow`·`typography` 처럼 여러 속성이 묶인 복합 토큰에만 쓴다.

`thickness` 중간 단계도 없앴다 — divider 가 두께만 정의하므로 구분할 대상이 없다(`shadow.normal.*` 을 없앤 것과 같은 이유).

12px 은 선이라기보다 띠로 보여 **8px** 로 줄였다. 참조가 `preview.html` 10곳뿐이라 함께 갱신했다.

**색은 이 토큰에 없다.** `$description` 에 명시했다 — 색은 `color.line.*` 에서 가져온다. 이전에는 이 사실이 어디에도 적혀 있지 않아, 미리보기가 `spacing` 과 같은 파란 막대로 그리는 바람에 "구분선이 파랗다"는 오해를 만들었다. 지금은 실제 `line.solidNormal` 색으로 그린다.

> `divider.8` 은 굵은 띠라서 실제로는 `line` 색보다 면 색(`bg.normalAlternative` 등)이 자연스럽다. 토큰 설명에 적어 뒀다.

### Dim 은 semantic 색이다

`color.material.dimmer` → **`color.elevation.dim`** 으로 개명했다(`--color-elevation-dim`). `material` 그룹은 사라졌다. 값·알파는 그대로다(라이트 `gray.910 @52`, 다크 `gray.900 @74`).

### 그림자 값 — 참조 스펙과 기존 값을 섞었다

기존 5개도, 참고로 받은 3개(새도우1·2·3)도 **그대로 쓰지 않고 양쪽에서 취해 새로 정의했다.**

| 출처 | 가져온 것 |
|---|---|
| 참조 스펙 | 방향 + 앰비언트 **2레이어 구조**, 앰비언트는 `0 0 y 0`(오프셋 없음), `blur/y` 비율이 깊어질수록 감소 |
| 기존 토큰 | 방향 레이어의 **음수 spread** — 그림자가 요소에 밀착해 붕 뜬 halo 를 막는다. 위쪽으로 더 넓은 범위 |
| 새로 정한 것 | 알파를 **단조 증가 램프**로(양쪽 다 아님). 앰비언트 알파도 단계별로 올린다 |

| 단계 | 방향 레이어 | 앰비언트 | 라이트 α | 다크 α | blur/y |
|---|---|---|---|---|---|
| `xs` | `0 1 3 0` | `0 0 1 0` | 0.08 / 0.04 | 0.48 / 0.24 | 3.00 |
| `sm` | `0 3 7 -1` | `0 0 3 0` | 0.12 / 0.05 | 0.70 / 0.30 | 2.33 |
| `md` | `0 6 14 -2` | `0 0 6 0` | 0.16 / 0.06 | 0.80 / 0.36 | 2.33 |
| `lg` | `0 12 24 -4` | `0 0 12 0` | 0.20 / 0.07 | 0.88 / 0.42 | 2.00 |
| `xl` | `0 20 36 -7` | `0 0 20 0` | 0.24 / 0.08 | 0.88 / 0.52 | 1.80 |

**y 램프를 `1·3·6·12·20` 으로 잡았다.** 참조 스펙의 `2·4·8` 과도, 기존의 `1·4·10·16·24` 와도 겹치지 않는다 — 어느 한쪽을 베낀 값이 아님을 구조적으로 보장한다.

**base 색은 `{color.gray.925}`(`#131314`).** 참조 스펙의 `rgb(20,20,20)` 에 가장 가까운 팔레트 값이며 채널 최대 차이가 1, 흰 배경 α0.2 에서 차이 0.2 로 보이지 않는다.

**다크는 깊은 단계일수록 라이트를 못 따라간다.** 알파를 88% 까지 올려도 `lg`·`xl` 은 48%·40% 수준이다(라이트 Δ47·57 을 내려면 알파 >1 필요). 얕은 단계는 64~66% 를 달성한다. `lg` 와 `xl` 의 방향 알파가 같은 88% 인 것도 이 상한 때문이며, 둘은 기하(y 12 vs 20)와 앰비언트 알파(42% vs 52%)로 구분된다.

### 그림자를 primitive 에서 semantic 으로 옮겼다

처음에는 `primitive.shadow` 에 두고 "테마 무관"으로 관리했으나 **두 가지 문제가 있었다.**

**① 다크에서 그림자가 보이지 않았다.** 그림자 색이 `rgba(23,23,23,…)` 인데 다크 배경이 `#1A1B1C`(= `rgb(26,27,28)`) 라 거의 같은 색이었다. 배경 위에 합성했을 때 채널당 변화가 라이트는 14~28, **다크는 0.2~0.4** 였다 — 8비트에서 반올림되면 사라지는 수준. 표면 밝기로 대체되지도 않았다(`bg.elevated` vs `bg.normal` 이 다크에서 1.07:1, 라이트에서는 아예 같은 값).

**② 색이 별칭이 아니었다.** `rgba(23,23,23,0.10)` 을 직접 기입해 "반투명색은 base 색 참조 + alpha(opacity 참조)" 규칙과 어긋났다.

그래서 `primitive.shadow` 를 제거하고 **`semantic.light/dark` 의 `shadow` 그룹**으로 옮겼다.

| | 라이트 | 다크 |
|---|---|---|
| base 색 | `{color.gray.900}` | `{color.gray.1000}` (순수 검정) |
| 알파 | 위 표 참조 | 위 표 참조 (상한 88%) |
| 기하 | 동일 (offset·blur·spread) | 동일 |

**base 색 치환은 무해하다.** 기존 `rgb(23,23,23)` 은 팔레트에 없는 값이었고, 가장 가까운 `gray.900`(`#161617`) 으로 바꿨을 때 흰 배경에서의 차이가 최대 0.12 — 보이지 않는다.

**다크는 라이트와 같은 강도를 낼 수 없다.** 배경이 이미 어두워 어둡게 만들 여지가 없다. 물리적 한계이며, 더 나은 다크 elevation 은 `bg.elevated` 를 밝게 하는 쪽으로 해결해야 한다(미결 11번).

> **갱신(2026-09-01):** 이 문단의 "같은 강도를 낼 수 없다"는 **과장이었다.** 실측하니 다크 그림자는 라이트의 **71~87%**(카드에 쓰는 `sm` 은 87%)이고, 면 차이까지 더한 총합은 오히려 다크가 앞선다. 그리고 해법도 `bg.elevated` 밝히기가 아니었다 — 실제로 부족한 곳은 두 테마 공통으로 **카드 윗변**이라 보더로 푼다. 근거는 0-24.

**알파는 레이어별로 다를 수 있다.** `$extensions."net.infobank.ds.alpha"` 가 문자열이면 전 레이어 공통, 배열이면 레이어별로 적용된다(`xl` 이 `[12%, 10%]` 로 다르다).

**기하는 두 파일에 중복 기입되므로 빌드가 검사한다.** `build.mjs` 의 `assertShadowGeometryMatches()` 가 라이트/다크 기하 불일치와 알파 개수 불일치를 발견하면 빌드를 실패시킨다.

`opacity` 는 24단계가 됐다 — 원본 14단계는 값이 그대로이고 그림자용으로 `4 · 6 · 7 · 20 · 24 · 30 · 36 · 42 · 48 · 80` 을 추가했다. 중간에 추가했다가 값 변경으로 쓰이지 않게 된 `10 · 72` 는 제거했다. 현재 미참조 단계는 없다.

## 2-7. Breakpoint 를 `web` 에서 분리했다

파운데이션에서 Breakpoint 를 독립 항목으로 관리하기로 해 `web.breakpoint.*` → **`breakpoint.*`**(최상위)로 옮겼다. `build.mjs` 가 `web` 래퍼를 평탄화하고 있었으므로 **CSS 변수는 `--breakpoint-*` 그대로이고 산출물의 값·집합이 바뀌지 않았다**(선언 순서만 이동). `tokens.js` 에는 `breakpoint` export 가 새로 생기고 `web` 에서 빠졌다.

**5단계·현재 값을 유지했다.**

| 단계 | 값 | 뜻 |
|---|---|---|
| `xs` | 0px | 기준점 없음. 모바일 우선 설계의 기본 구간 |
| `sm` | 768px | 태블릿 세로 / 작은 화면 진입점 |
| `md` | 992px | 태블릿 가로 / 작은 데스크톱 |
| `lg` | 1200px | 데스크톱 |
| `xl` | 1600px | 와이드 데스크톱 |

**타이포 `compact` 오버라이드가 `breakpoint.sm`(768px)과 맞물린다.** `@media (max-width: 767.98px)` 로 동작하므로 `sm` 미만 구간이다. 두 값은 함께 움직여야 한다 — 한쪽만 바꾸면 어긋난다.

**t-shirt 이름을 쓴 이유.** breakpoint 는 **단계(tier)** 를 가리키고 px 는 그 임계값이다. `breakpoint.768` 로 두면 임계값을 792 로 조정하는 순간 이름이 거짓이 되고 모든 소비자를 고쳐야 한다. 반면 `spacing.16` 은 값을 바꾸는 게 아니라 다른 토큰(`spacing.20`)을 쓰는 것이므로 숫자가 맞다. 위의 다듬은 원칙이 이 차이를 담는다.

**`$value` 는 객체 형식을 유지했다.** 참고로 받은 샘플은 `"$value": "640px"`(문자열, DTCG 초기 드래프트)였으나, 이 프로젝트는 `{"value": 768, "unit": "px"}`(객체, 최신 스펙)를 dimension 토큰 전체에서 쓴다. breakpoint 만 문자열로 바꾸면 `build.mjs` 의 `valueOf` 가 깨지고 100개 넘는 다른 dimension 토큰과 형식이 갈린다.

**`web` 에 남은 것** — `screen`(4) · `container`(2) · `layout`(1). 이 7개가 Artboard·Layout 파운데이션으로 갈 후보다.

## 2-8. Layout — 그리드를 breakpoint 단계별로 정의했다

`web.container`(2) · `web.layout`(1) 3개를 걷어내고 최상위 **`layout`** 을 신설했다. **4속성 × 5단계 = 20개**이며 키가 `breakpoint` 와 1:1 대응한다.

| 단계 | maxWidth | columns | margin | gutter |
|---|---|---|---|---|
| `xs` · `sm` | `none` | 4 | 20px | 12px |
| `md` | `none` | 8 | 20px | 16px |
| `lg` · `xl` | **1080px** | 12 | 0 | 24px |

**`"auto"` 를 `none` 으로 표현했다.** 최대폭 제약이 없다는 뜻이고, CSS `max-width` 의 초기값이 `none` 이라 그대로 쓸 수 있다(`auto` 는 `max-width` 에 유효하지 않다). `$type` 은 두지 않았다 — `ratio`·`gradient` 처럼 값 자체가 문자열인 경우와 같은 방식이다.

**`margin: 0` 은 여백이 없다는 뜻이 아니다.** `lg`·`xl` 에서는 `maxWidth`(1080px)가 여백을 만든다 — `lg`(1200)에서 좌우 각 60px, `xl`(1600)에서 각 260px. 이 관계를 `$description` 에 적었다.

**산수를 검증했다** — 전 단계에서 컬럼 폭이 양수다.

| 단계 | 컨텐츠 폭 | 거터 합 | 컬럼 폭 |
|---|---|---|---|
| `xs`(375 가정) | 335 | 36 | 74.8 |
| `sm`(768) | 728 | 36 | 173.0 |
| `md`(992) | 952 | 112 | 105.0 |
| `lg`·`xl` | 1080 | 264 | 68.0 |

### 교체된 옛 값

| 제거 | 대체 |
|---|---|
| `web.container.maxWidth` = **1100px** | `layout.maxWidth.lg`·`.xl` = **1080px** |
| `web.container.maxWidthXl` = **1440px** | 없음 — `xl` 도 1080px |
| `web.layout.paddingHorizontal` = 20px | `layout.margin.*` (단계별) |

**1100 → 1080, 1440 → 1080 으로 값이 바뀌었다.** 참조가 0건이어서 안전했다(`preview.html` 도 이 변수를 쓰지 않았다).

### margin·gutter 가 spacing 과 값이 같은 것은 의도된 독립이다

`layout.margin.xs`(20px) ↔ `spacing.20`, `layout.gutter.md`(16px) ↔ `spacing.16` 등 8건이 같은 숫자를 갖는다. **묶지 않는다.**

**① 두 축은 다른 이유로 바뀐다.** `spacing` 은 컴포넌트 내부 여백(버튼 패딩, 리스트 간격), `layout` 은 페이지 그리드(뷰포트 여백, 컬럼 사이)다. "카드 안쪽 여백을 16→20" 이라는 변경이 그리드 거터를 끌고 가면 안 된다.

**② 숫자 키 스케일에서 별칭은 유지보수 이득이 없다.** 색은 `gray.900` 의 hex 를 조정하면 참조가 전부 따라오므로 별칭이 실질 이득이다. 반면 `spacing.16` 은 정의상 항상 16px 이라(키가 곧 값) 거터를 20px 로 바꾸는 일은 별칭을 `{spacing.20}` 으로 고치든 raw 값을 고치든 작업량이 같다.

**③ 드리프트 위험이 없다.** `spacing.16` 이 미래에 다른 값이 될 방법이 없으므로 두 값이 어긋날 수 없다.

**업계 경향도 그리드가 자기 값을 갖는 쪽이 흔하다** — Material 3 는 window size class 별 margin·gutter 를 그리드 스펙에서 직접 정의하고, Carbon 도 거터를 그리드가 소유한다. Tailwind 는 반대로 `gap` 이 spacing 스케일을 공유하지만 유틸리티 기반이라 축을 나누지 않는 구조다. (각 시스템 내부 구현까지 확인한 것은 아니며 일반적 경향 수준의 근거다.)

> **주의** — "중복이니 별칭으로 묶자" 는 판단을 하지 말 것. 단, 그리드 값은 4px 그리드와 `spacing` 스케일 위에 두는 관례를 지킨다(현재 `0 · 12 · 16 · 20 · 24` 전부 해당, 스케일 밖 값 0건).

별칭의 유일한 실익은 "그리드 값이 스케일 위에 있음을 강제" 하는 것인데, 이미 지켜지고 있어 `build.mjs` 수정 비용을 들일 이유가 없다.

**`web` 에 남은 것** — `screen`(4)뿐. Artboard 파운데이션의 재료다.

## 2-9. Artboard 는 디자인 토큰으로 두지 않는다

`web.screen` 4개(`desktop` 1440×960, `mobile` 375×635)를 **삭제했다.** `web` 그룹은 이것이 마지막 항목이어서 함께 사라졌다.

**설계 캔버스는 토큰이 아니다.** 디자인 토큰은 코드가 읽어 화면을 그리는 값인데, 이 4개는 그렇지 않았다.

| 근거 | 확인 내용 |
|---|---|
| 제품이 소비하지 않음 | JSON 별칭 0건 · `preview.html` `var()` 0건. 그런데 CSS 에는 4개가 노출돼 있었다 |
| 높이는 웹에서 쓸 수 없음 | 반응형 레이아웃에 고정 높이는 없다. 뷰포트 높이는 사용자가 정한다 |
| 실기기 값이 아님 | `375×635` 의 635 는 어느 기기와도 안 맞는다(SE2 667, X 812). `1440×960` 의 960 도 임의 높이 — Figma 프레임 값이다 |
| **실제 오용 흔적** | 제거된 옛 `container.maxWidthXl` 이 **1440px** = `screen.desktop.width` 와 동일했다. **설계 폭을 컨텐츠 최대폭으로 착각한 것**으로 보인다. 실제 컨텐츠 최대폭은 `layout.maxWidth` = 1080px 다 |

마지막 항목이 결정적이었다 — `--screen-desktop-width` 가 CSS 에 있으면 `max-width: var(--screen-desktop-width)` 로 잘못 쓰기 쉽다. **토큰으로 노출하는 것 자체가 위험**이었다.

### 설계 기준 캔버스 (기록용)

값 자체는 팀 합의로서 의미가 있으므로 여기에 남긴다. **코드에서 참조하지 말 것.**

| | 폭 × 높이 |
|---|---|
| 데스크톱 | 1440 × 960 |
| 모바일 | 375 × 635 |

> `635` 와 `960` 의 출처는 확인되지 않았다. 특정 기기나 사내 규격이 근거라면 이 판단을 재검토할 수 있다.

**검토했다 버린 것 — JSON 에 남기고 CSS 출력만 제외.** `$extensions` 로 design-time 표시를 하고 빌드가 걸러내는 방식이다. Figma 플러그인이나 시각 회귀 테스트 뷰포트로 쓸 수 있다는 이점이 있으나, **지금 이 값을 소비하는 도구가 하나도 없어 가정에 불과하다.** 필요해지면 그때 되돌리는 편이 싸다.

### 부수 정리

`web` 그룹이 없어져 `build.mjs` 의 `cssVar()` 에서 **web 래퍼 평탄화 로직을 제거**했다(`pathArr[0] === 'web' ? …`). `breakpoint`·`layout` 은 이미 최상위로 승격돼 있었으므로 CSS 변수·값 집합은 바뀌지 않았다.

---

## 3. 소비자 주의사항

### 3-1. 행간 상속 동작이 바뀌었다

배수(`1.5714`) → 절대값(`1.375rem`) 으로 바뀌면서 **상속 동작이 달라졌다.**

- 요소마다 유틸 클래스를 직접 붙이는 경우 — 영향 없음
- 컨테이너에 클래스를 걸고 자식이 **다른 글자 크기**를 갖는 경우 — 행간이 자식 크기에 비례하지 않고 절대값 그대로 상속된다

컴포넌트를 "컨테이너에 걸고 상속" 패턴으로 설계할 계획이라면 이 결정을 재검토해야 한다.

### 3-2. 색은 라이트 600 / 다크 400

새 컴포넌트에서 유채색 텍스트가 필요하면 반드시 semantic 을 쓴다. primitive 를 직접 참조해야 하는 경우에도 라이트는 600, 다크는 400 규칙을 지킨다.

### 3-3. 미사용 단계

semantic 이 참조하는 것은 400 과 600 뿐이다. 200 · 300 · 500 · 700 은 현재 미참조지만 **완전한 스케일을 갖추기 위해 의도적으로 유지**한다. 미사용이라는 이유로 지우지 말 것.

---

## 4. 미결 항목

| # | 항목 | 상태 |
|---|---|---|
| ~~1~~ | ~~**`accent.bg` 7종의 용도**~~ — **2026-09-01 해소(0-22)**. 장식용 면으로 확정. 글자를 올리지 않으므로 흰 글자 5.1:1 가정이 사라졌고, 라이트·다크가 같은 값인 점을 고려해 두 테마 면대비 차이가 가장 작은 **500** 으로 옮겼다(차이 0.11). |
| ~~2~~ | ~~**semantic 계층 비대칭**~~ — **2026-09-01 해소(0-16)**. 결함이 아니었다. semantic 은 테마 축을 담는 그릇이고 비색 토큰에는 그 축이 없다. 실재하는 결함(컴포넌트가 원시값을 직접 고름)은 미결 17 로 옮겼다. |
| ~~11~~ | ~~**다크 elevation 의 근본 한계**~~ — **2026-09-01 해소(0-24)**. 전제가 틀렸다. 다크 그림자는 라이트의 71~87%(작은 단계는 87%)이고, 면 차이까지 더하면 총합은 다크가 더 강하다. 실제로 부족한 곳은 **두 테마 공통으로 카드 윗변**(앰비언트 0.035 < 기준 0.04)이라 **보더로 해결**한다. 토큰 변경 없음. |
| 10 | **`spacing` 사용 가이드 부재** | 토큰별 `$description` 이 없다. 값 하나에 용도를 1:1 로 묶기 어려워 **컴포넌트 구현 단계로 미루기로 결정**했다 — 추측으로 채우지 않는다. 실제로 반복되는 값을 보고 역할을 뽑는다. `radius` 와 달리 의도된 공백이다. |
| ~~3~~ | ~~**`compact` 범위**~~ — **2026-09-02 해소(0-25)**. 미완이 아니라 의도였고, 다만 기준이 기록돼 있지 않았다. 기준은 크기가 아니라 조건이다 — **뷰포트에 따라 값이 달라져야 하는 토큰만 담는다**(display·heading 6종=넘침 방지, `label-xs`=밀도). 20px 이하 나머지 21종은 좁은 화면에서도 넘치지 않으므로 대상이 아니다. **모바일에서 본문을 더 작게 만들지 않는다.** 규칙을 `typography` 그룹 `$description` 과 산문에 적었다. |
| ~~4~~ | ~~**`ios.fillSecondary` · `ios.glassBackground` 의 역할**~~ — **2026-09-01 해소(0-18)**. `ios.*` 3개를 제거했다. `accentPrimary` 는 완전 중복, `glassBackground` 는 다크가 깨져 있었고(2.51:1), `ios.` 접두사 자체가 테마 외의 두 번째 축을 이름에 밀어 넣은 것이었다. |
| ~~5~~ | ~~**`cool.neutral30` · `atomic.*` 네이밍**~~ — **2026-09-01 해소(0-23)**. 둘 다 제거했다. `atomic` 은 `status` 중복이었고, `cool.neutral30` 은 이름이 값과 어긋나며(30 vs 700) 다크에서 3:1 도 못 넘었다. `coolGray` 계열도 `gray.550` 으로 흡수해 없앴다 — 별개 계열이 아니라 gray 스케일의 빠진 단계였다. |
| ~~14~~ | ~~**`opacity` 24개 용도 미기록**~~ — **2026-09-01 해소(0-17)**. 용도를 지어내지 않고 전수 조사한 **실제 참조처**를 적었다. 24/24 작성, 참조 0인 단계 없음, 값 변경 없음. |
| ~~12~~ | ~~**`ratio` 그룹 구조가 어긋남**~~ — **2026-08-27 해소(0-13)**. 평탄화 + 12개로 정리. |
| ~~13~~ | ~~**`gradient.maskSize` 용도 미기록**~~ — **2026-09-01 해소(0-18)**. 5개 중 4개가 `spacing` 과 같은 값이라 새 눈금이 아니었다. 제거하고 페이드 길이는 `spacing` 을 쓴다. 48 결번도 이걸로 설명된다. |
| 19 | **플랫폼 축 미도입** | `COMPOSITION` 의 modifier 는 `theme` 하나뿐이다. 플랫폼별로 값이 갈리는 역할(`safeArea` 4개)은 지금 **키 안에 플랫폼이 박혀 있다**(`safeArea.status.ios`). 임시 표현이며, 실제 iOS·Android 타깃이 생기면 테마처럼 **modifier 로 올려** `dist/tokens.ios.light.json` 처럼 갈라 내야 한다. **역할 이름에 플랫폼 접두사를 붙이지 않는다** — `color.ios.*` 가 그렇게 만들어졌다가 제거됐다(0-18). 소비자가 없는 지금 만들면 파일만 3배가 되므로 미룬다. |
| 16 | **미디어 스크림 없음** | 사진·영상 위에 어두운 층을 깔아 글자 대비를 확보하는 수단이 없다. `elevation.dim` 은 균일한 모달 백드롭이라 대체가 안 된다. 미디어 컴포넌트 착수 시 만든다(0-15).<br>**어디에 두나 — `gradient` 안이 아니다.** `gradient.fade` 는 색이 없는(`currentColor`) 모양 레시피여서 primitive 에 있을 수 있다. 스크림은 특정 색을 갖고 AA 검증 대상이므로 **semantic** 이다 — `shadow` 가 같은 이유로 semantic 에 있는 것과 같다.<br>**형태 — 새 그라디언트 토큰이 아니라 색 토큰 하나면 된다.** `currentColor` 는 알파까지 실어 나른다(실측). `background-image: var(--gradient-fade-bottom); color: var(--color-scrim-…)` 로 모양은 재사용하고 색만 새로 둔다.<br>**불투명도** — 흰 사진(최악) 위 흰 글자 4.5:1 을 보장하는 임계 alpha 는 **0.5347**, `opacity` 눈금에서는 **`60`** 이 처음 통과한다.<br>**단서** — 위는 stop 2개짜리 단순 선형 페이드 기준이다. 밴딩을 피하려 곡선을 다듬어야 하면 새 그라디언트 토큰이 필요하고, 그때 두 가지가 걸린다 — `build.mjs` 가 `$type:"gradient"`(스펙 9.7)를 처리하지 못하고, `net.infobank.ds.alpha` 가 **토큰당 하나**라 stop 별로 다른 알파를 담을 그릇이 없다. |
| ~~17~~ | ~~**컴포넌트 토큰 계층 없음**~~ — **2026-09-03 해소(0-30)**. `component.json` 을 `typography.json` 과 같은 자리에 두고 `control.*`(치수) · `transition.*`(전환) 14개를 만들었다. 컴포넌트가 아니라 **역할** 단위로 이름 지었고(폼 한 줄에서 높이가 맞아야 한다), `height` 가 아니라 **`minHeight`** 인 것은 WCAG 1.4.12 때문이다. 새 원시 값 0개. 파운데이션은 `Size` 를 신설했다. |
| 21 | **숫자 폭 고정(`tnum`) 수단 없음** | 값이 갱신될 때 숫자가 좌우로 흔들린다. 차트 축 레이블 · 타임스탬프 · 조밀 테이블 · 배지 · 카운터 · 가격이 대상이며, `label-xs` 의 용도와 정확히 겹친다(0-25).<br>**DTCG 에 타입이 없다** — 스펙 전문 검색 0건(0-26). 만들면 `cssRecipe` 예외가 1건 는다.<br>**형태 — 타이포 토큰의 하위 속성이 아니다.** `tnum` 은 스타일과 직교한다(같은 `label-xs` 가 차트 축에도 텍스트 라벨에도 쓰인다). `link` 의 밑줄처럼 `$extensions` 에 박으면 숫자 아닌 라벨까지 강제된다. primitive 에 `fontFeature` 그룹을 두고 빌드가 `.tnum` 유틸리티를 내어 **조합**하는 쪽(`class="label-xs tnum"`)이 맞다.<br>**Chart·Table 착수 시 만든다** — 어느 스타일에 필요한지는 컴포넌트를 봐야 나온다. |
| ~~23~~ | ~~**컨트롤 경계선 토큰 없음**~~ — **2026-09-03 해소(0-38)**. 새 토큰이 아니라 **`line.strong` 의 라이트 알파를 `{opacity.52}` → `{opacity.80}` 로 올려** 닫았다(1.97 → **3.09**). 다크는 이미 3.50 이라 손대지 않았다 — 같은 역할이 테마마다 다르게 동작하던 것이 결함이었고, 새 자리를 만들면 그 결함이 남는다. `line.control` 을 신설하는 안은 **다크에서 `line.strong` 과 값이 완전히 같은 토큰이 생긴다**는 점에서 버렸다. `line.*` 7개에 `$description` 을 채워 `strong` 이 컨트롤 경계 전용임을 명시했다. 임시로 `label.alternative` 를 쓰던 4자리(Text Field · Text Area · Search ×2)를 교체했다. |
| 24 | **파일 확장자·미디어 타입 미정** | DTCG 2025.10 은 확장자 `.tokens.json`(또는 `.tokens`)와 미디어 타입 `application/design-tokens+json` 을 말하는데, 우리는 `primitive.json`·`semantic.light.json`·`dist/tokens.light.json` 을 쓴다.<br>**결정한 적이 없다.** 처음부터 그렇게 만들어졌고 근거가 어디에도 없다. 지금 바꾸면 소비자를 전부 고쳐야 한다 — `build.mjs`(COMPOSITION·NOT_A_SOURCE) · `preview.html`(브라우저에서 JSON 을 직접 읽는다) · `checks/audit.mjs` · CI 워크플로.<br>**의미가 있는 쪽은 미디어 타입이 아니라 확장자다** — 외부 도구(Figma 플러그인·Style Dictionary·에디터 스키마)가 `.tokens.json` 을 보고 DTCG 문서로 인식하는 경우가 있다. 우리가 그런 도구를 붙일 때 실익이 생긴다.<br>**외부 소비자가 생길 때 정한다.** 지금은 소비자가 우리뿐이라 바꿀 이유가 없고, 바꾸지 않기로 한 것도 아니다. |
| 22 | **파운데이션 목록이 네 곳에 복제** | `build.mjs`(FOUNDATIONS) · `guide.html`(FOUNDATIONS) · `preview.html`(FOUNDATIONS · SECTIONS). **뒤 셋은 빌드가 검사하지 않아** 하나만 고치면 감사·CI 는 통과하는데 뷰어 내비에서 빠진다 — Motion 신설 때 실제로 그 상태를 만들었다(0-27).<br>`guide.html` 의 목록은 데이터와 대조하는 **의도된 교차 검증**이라 지우면 안 된다. 필요한 것은 제거가 아니라 **네 목록이 같은지 보는 검사**다. `tokens/checks/` 에 스크립트 하나면 되고 `lint:hardcode` 와 같은 자리에 붙는다.<br>지금은 손으로 맞춰 일치한다. 파운데이션을 다시 건드릴 때 함께 처리한다. |
| ~~18~~ | ~~**무채색 팔레트 설명 없음**~~ — **2026-09-01 해소(0-17)**. `gray` 20 · `coolGray` 3 + 그룹 설명 1건을 실제 참조처 기준으로 작성했다. 값 변경 없음. |
| ~~15~~ | ~~**번들의 알파가 스펙 밖 표현**~~ — **2026-09-01 해소(0-19)**. 번들만 native `alpha` 로 해소했다. 소스는 참조를 유지하고, 별칭은 스펙이 지원하는 표현이라 그대로 둔다 — **표현 불가능한 것만** 해소한다. 확장을 통째로 지운 사본으로 값이 온전함을 확인했다. |
| ~~7~~ | ~~**10px 단계 추가 필요**~~ — **2026-09-02 해소(0-25)**. 적혀 있던 근거 2건은 무효였다(Bottom Nav 는 이미 `label-sm` 12px, Data Visual 은 컴포넌트 부재). 용도를 밀집 UI 크롬으로 다시 세우고 **11px = base `label-xs` · 10px = 그 compact** 로 들였다. 배치는 빌드 제약(자간 1:1 · 티어 어휘)이 결정했다. 10px 은 아이콘·수치와 짝지어진 보조 라벨 전용이라는 규칙을 함께 못 박았다. |
| 8 | **타이포–컴포넌트 매핑은 잠정** | 각 타이포 토큰의 `$description` 에 대표 컴포넌트를 적었으나, 컴포넌트가 아직 없어 크기만 보고 배분한 추정이다. 특히 `title-lg` / `title-md` / `title-sm` 의 경계가 약하다. 컴포넌트 구현 단계에서 다듬는다. |
| 9 | **상위 3단계에 대응 컴포넌트 없음** | `display-lg`(60) · `display-md`(40) · `heading-xl`(36) 은 계획된 컴포넌트 29 종 어디에도 맞지 않는다(최대 수요는 Popup Full Screen 제목의 32px). 페이지 레이아웃 전용으로 남긴다. |

> **20 은 결번이다.** `DESIGN.md` 를 만들 때(0-20) 테마 축 처리를 잠시 "미결 20" 으로 부르고 같은 절에서 해소했다. 표에 오른 적이 없으므로 번호를 재사용하지 않는다.

---

## 4-1. 접근성 기준 — 무엇을 근거로 삼는가

`CLAUDE.md` 가 요구하는 **WCAG 2.2 AA + KWCAG 2.2** 의 실제 조항을 확인했다(2026-08-24).

### KWCAG 2.2 (33개 검사항목 / 14지침 / 4원칙)

| 항목 | 내용 |
|---|---|
| **5.4.3 텍스트 콘텐츠의 명도 대비** | 텍스트와 배경 간 **4.5:1 이상**. 예외 — 로고, 장식 목적 콘텐츠, 초점 시 대비가 커지는 콘텐츠. **큰 텍스트 예외 — 18pt 이상 또는 14pt 굵은 폰트면 3:1 까지 낮출 수 있다** |
| **5.4.4 콘텐츠 간의 구분** | *"이웃한 콘텐츠는 시각적으로 구분될 수 있도록 제공해야 한다."* **수치 기준이 없다** — 준수 방법으로 ①테두리 ②구분선 ③무늬 ④배경색 명도대비 ⑤줄·글자 간격 ⑥기타를 든다. WCAG 에 대응 조항이 없다 |
| **6.1.2 초점 이동과 표시** | 키보드 초점은 논리적으로 이동하고 **시각적으로 구별**되어야 한다 |
| **6.1.3 조작 가능** | 대각선 길이 **6.0mm 이상** 권장, 컨트롤 테두리 안쪽 1px 이상 여백 |

**KWCAG 2.2 에는 WCAG 1.4.11(Non-text Contrast)에 해당하는 검사항목이 없다.** UI 컴포넌트 경계·그래픽 객체의 대비 요구가 부재한다.

**반대 방향의 차이도 있다(2026-09-02 추가).** **KWCAG 5.4.4 는 WCAG 에 대응 조항이 없다.** 처음 조사(2026-08-24)에서 5.4.3 · 6.1.2 · 6.1.3 만 보고 이 항목을 빠뜨렸다. 두 기준은 포함 관계가 아니라 **서로 없는 항목이 있는 관계**다 — WCAG 에만 1.4.11 이 있고, KWCAG 에만 5.4.4 가 있다.

### 이 사실이 바꾼 판단 두 가지

**① 보더 토큰의 근거를 정정했다 — 그리고 그 정정을 다시 정정한다(2026-09-02).** 처음에는 "떠 있는 면이 1.4.11 미달"이라고 봤다. 그 진단은 여전히 틀렸다 — KWCAG 에 1.4.11 대응 조항이 없고, WCAG 1.4.11 도 원문이 *"user interface components 를 **식별**하는 데 필요한 시각 정보"* 라서 모달·카드 **컨테이너 면** 자체는 대상으로 보기 어렵다.

그러나 이어서 내린 결론 — *"보더는 규정 준수가 아니라 실무적 선택"* — 은 **틀렸다.** **KWCAG 5.4.4 가 정확히 이것을 요구한다**("이웃한 콘텐츠는 시각적으로 구분될 수 있도록 제공해야 한다"). 조항을 못 찾아서 근거가 없다고 적은 것이지, 근거가 없던 게 아니다. **보더는 KWCAG 5.4.4 준수 수단이다.**

**다만 새로 지켜야 할 수치가 생기지는 않는다.** 5.4.4 는 5.4.3 과 달리 **수치 기준이 없고**, 준수 방법의 첫 번째가 바로 "테두리를 이용하여 구분"이다. 0-24 에서 카드에 보더를 두기로 한 결정이 그대로 5.4.4 를 만족한다. 아래 ② 의 "면끼리 3:1 은 불가능" 판단도 영향받지 않는다 — 5.4.4 가 요구하는 것은 비율이 아니라 **구분 가능성**이다.

**② 면끼리 3:1 은 애초에 불가능하다.** 라이트에서 흰 면과 3:1 이 되려면 상대 면이 `gray.600`(`#595B61`) 수준이어야 하고, 다크 모달이 dim 위에서 3:1 이 되려면 `gray.500`(`#989BA1`) 이상이어야 한다. 둘 다 그 테마를 포기하는 값이다. **면은 지각 힌트, 경계는 보더** 로 역할을 나눈다.

### 큰 텍스트 예외는 쓰지 않는다 (보수적 적용)

두 기준 모두 큰 텍스트에 3:1 을 허용하고 기준선도 같다(18pt=24px, 14pt bold=18.67px). 우리 타이포 27개 중 **7개**(display 2 · heading 4 · title-lg-strong)가 해당한다.

그러나 **색 토큰이 어느 크기와 조합될지 정의가 없으므로 예외를 적용하지 않는다.** 전 색상에 4.5:1 을 적용하고, 미달이 났을 때만 "큰 텍스트로 쓰면 구제 가능"을 참고 정보로 표시한다. 기준보다 엄격한 쪽이며 의도적이다.

## 4-2. 텍스트 검사 면을 4개로 넓혔다

이전에는 `bg.normal` 위에서만 검사해 **카드·모달 위의 텍스트가 사각지대였다.** `normal` · `normalAlternative` · `elevated` · `elevatedAlternative` 4개 면 전부로 넓히자 검사가 50건 → **158건**이 되고 미달 3건이 드러났다.

| 테마 | 토큰 | 면 | 대비 |
|---|---|---|---|
| 라이트 | `label.alternative` | `bg.normalAlternative` | 4.42 |
| 라이트 | `label.alternative` | `bg.elevatedAlternative` | 4.42 |
| 다크 | `label.alternative` | `bg.elevated` | 4.35 |

**알파만 올려 해결했다** — 라이트 `gray.700 @69% → @70%`(최저 4.55), 다크 `gray.400 @70% → @74%`(최저 4.71). base 색은 그대로이고 새 토큰도 필요 없었다. 기존의 라이트 69 / 다크 70 이라는 근거 없는 비대칭도 함께 정리됐다.

> `opacity.69` 는 이 변경으로 미참조가 됐다. 원본 14단계에 속하므로 삭제하지 않고 남긴다.

## 4-3. 포커스 토큰 — 컴포넌트 없이 정할 수 있는 만큼만

KWCAG 「6.1.2 초점 이동과 표시」가 *"초점을 받은 콘텐츠는 시각적으로 구별되어야 한다"* 를 요구하는데 **링 색 토큰이 없었다.** `preview.html` 도 임시로 `--color-accent-fg-blue` 를 쓰고 있었다.

컴포넌트가 없어 링의 모양·위치는 정할 수 없지만 **색과 두께는 지금 정할 수 있어** 세 개만 만들었다.

| 토큰 | 값 | CSS 변수 |
|---|---|---|
| `color.interaction.focus` | 라이트 `{color.blue.600}` · 다크 `{color.blue.400}` | `--color-interaction-focus` |
| `focusRing.width` | 2px | `--focus-ring-width` |
| `focusRing.offset` | 2px | `--focus-ring-offset` |

색은 **라이트 600 / 다크 400** 규칙을 그대로 따른다. 4개 면 전부에서 3:1 을 넘는다(라이트 최저 4.79, 다크 최저 5.02).

### 링은 반드시 띄워야 한다

컴포넌트 면 위에 직접 그리면 대비가 무너진다.

| 링을 얹는 대상 | 라이트 | 다크 |
|---|---|---|
| 페이지 배경 (offset 있음) | 4.79~5.13 ✓ | 5.02~6.03 ✓ |
| `primary.normal` | 2.61 ✗ | 2.99 ✗ |
| `accent.bg.violet` | **1.00** ✗ | 1.60 ✗ |

`accent.bg.violet` 은 링 색과 **완전히 같은 색**이 되어 사라진다. 그래서 `focusRing.offset` 이 선택이 아니라 필수다. 이 사실을 `CLAUDE.md` 접근성 항목에도 적었다.

두 겹 링(안쪽은 표면 색, 바깥은 포커스 색)이 더 안전하지만, **그 조합은 컴포넌트 레벨의 구성이라 토큰으로 만들지 않았다.** 의미 단위 elevation 토큰(미결 3번 계열)과 함께 다룰 문제다.

### 지금 정할 수 없는 것

- **조작 영역 크기** — KWCAG 6.1.3 은 **6.0mm 이상**(물리 크기), WCAG 2.5.8 은 **24×24 CSS px**. 단위가 달라 각각 확인해야 하고 둘 다 컴포넌트 치수 문제다.
- **2.4.11 Focus Not Obscured** — 스티키 헤더 등이 포커스된 요소를 가리는지, 레이아웃 문제다.

---

## 5. 검증 방법

색을 바꿨으면 `node tokens/build.mjs` 로 재생성한 뒤 대비를 재검증한다. 확인할 것:

- 텍스트 4.5:1 — `label`(strong/normal/neutral/alternative) · `status` · `accent.fg` · `ios.accentPrimary`
- 비텍스트 3:1 — `atomic` · `accent.bg`
- 반투명 토큰은 **배경에 합성한 뒤** 계산한다
- 라이트/다크 격차가 벌어지지 않았는지

`tokens/preview.html` 을 정적 서버로 열면 전 토큰을 라이트/다크로 눈으로 확인할 수 있다.

```bash
cd /Users/mac/iNext/DS && python3 -m http.server 4321 --directory tokens
```

{
  "name": "@infobank/ds",
  "version": "0.1.0",
  "private": true,
  "description": "iNext Design System — 모노레포 루트",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:tokens": "npm run build:tokens -w @infobank/ds-tokens",
    "build:designmd": "npm run build:designmd -w @infobank/ds-tokens",
    "lint:hardcode": "npm run lint:hardcode -w @infobank/ds-tokens",
    "check": "npm run check -w @infobank/ds-tokens"
  }
}
